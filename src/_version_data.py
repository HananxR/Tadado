"""Version data — single point of truth updated by the release process.

This file contains *only* the version string and release highlights.
It is regenerated by ``generate_db.bat`` during release — do not edit
the data here by hand; instead update ``CHANGELOG.md`` and run the
release script.

Usage (always import via the public ``src.version`` module)::

    from src.version import __version__, get_release_highlights

"""

from __future__ import annotations

__version__ = "0.2.6"

# ------------------------------------------------------------------
# Release highlights — user-facing feature summaries per version
# ------------------------------------------------------------------

_RELEASE_HIGHLIGHTS: dict[str, dict[str, tuple[str, ...]]] = {
    "0.2.6": {
        "新增": (
            "任务列表与标签管理面板双向联动: 点击任务行高亮对应标签(加粗+着色+前置排序)；点击标签项任务列表按标签前置；标签列悬浮提示显示完整标签",
            "活动分析标签云优化: 活动数为 0 的标签不再预选，仅在存在活动的标签中自动勾选",
        ),
        "优化": (
            "延后处理天数调整: 右键菜单延后处理选项从 +1/+2/+5/+7/+10天 改为 +1/+5/+7/+10/+20/+30天",
            "快速计算器平铺: 弹窗去除「时间粒度」下拉，6 个截止选项（今天/明天/本周日/一周后/本月末/下月今天）一次性平铺展示，预览格式改为「标签 (日期 时间)」",
            "version.py 数据与逻辑拆分: 版本号数据移至 <code>_version_data.py</code>，发版时仅更新数据文件不再修改逻辑文件",
            "界面精简: 删除任务管理侧栏无用 logo 按钮",
        ),
        "修复": (
            "标签面板选中样式: selection 使用 accent 半透明背景，避免白底白字不可见",
            "标签重命名/合并: 覆盖全部任务（含已归档），批量页面表格同步刷新",
            "已勾选 ID 残留: <code>load_tasks</code> 时清空已勾选 ID，避免切换分区后勾选状态泄露",
        ),
    },
}
