# 安全策略

## 报告漏洞

如果你发现安全漏洞，请**不要**提交公开 Issue。

请发送邮件至 **hanxy8413@gmail.com**，我们会尽快处理和回复。

## 支持版本

| 版本 | 支持状态 |
|------|---------|
| 1.0.x | ✅ 支持 |

## 安全模型

Tadado 是本地桌面应用，所有数据存储在本地 SQLite 数据库中。请注意：

- 数据库文件（`tadado.data`）未加密存储
- 分区密码仅用于 UI 层面的访问控制，数据库文件本身可被直接读取
- 请妥善保管你的数据库文件，避免未授权访问
