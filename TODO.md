# Tadado 待办任务

> 最后更新：2026-09-15 · 2.0 界面重构（7 阶段 · 分轮执行）
> UI 原型：[resources/ui-mockup/tadado-2.0.html](resources/ui-mockup/tadado-2.0.html)
> 数据保全：全部阶段零 DB 变更（`PRAGMA user_version` 恒为 8）
>
> **2026-09-15 分支分家**：上面「2.0 界面重构」那几条线（阶段 1~7、`docs/architecture/`、
> Python 侧 428 个用例）都随代码归档到了 **`archive/pyversion`** 分支 —— 本分支
> 已无 `src/` `tests/` `docs/`，那几节的链接只对归档分支有效。
> 主线继续追的是文末「🖥 桌面版（`desktop/` · Tauri）工作线」一节。

## ✅ 已完成 — 阶段 1（2026-09-12）

- [x] SelectionContext（分区/选中任务/时段共享上下文）
- [x] 视图注册表（5 页 + 旧视图名别名 edit→tasks 等）
- [x] NavShell 侧栏（工作/洞察/管理分组 + 设置齿轮 + Ctrl+1..5）
- [x] 页面构建逻辑迁出 MainWindow 至 `src/ui/views/`
- [x] 标题栏极简化 + 置顶按钮（本地实现，TODO phase5 迁 WindowShell）
- [x] 午夜崩溃修复（`_refresh_report` 幽灵调用）
- [x] 死代码清理：main_window 2,075 → 1,351 行（-724）
- [x] 测试基建：qapp 上移 conftest（offscreen），修复 QCoreApplication 崩溃；当时 169 用例全绿（现 322）

## ✅ 阶段 1 收尾（2026-09-12）

- [x] **DB 保全抽查**：新增 `tests/test_db_invariants.py` — `user_version` 恒为 8、任务表列不变量、3 条任务 raw_md 往返稳定
- [x] **GUI 冒烟**（无头替代）：新增 `tests/test_main_window_smoke.py` — offscreen 真实构建 MainWindow，覆盖侧栏切页、分页、数据刷新链路；真机（有显示器）验证待补
- [x] **文档同步**：DESIGN.md（新增 1.3.1 导航骨架 + 窗口/快捷键更新）、resources/help/manual.html（侧栏 5 页导航）
- [x] **无头测试基建修复**：`tests/conftest.py` 新增全局 fixture，短路 `QMessageBox` / `QFileDialog` 模态入口（offscreen 无人可点 → `exec()` 永久阻塞，曾致全量 `pytest` 挂死超时）；修正 `test_task_drawer` 任务字符串的日期/标签顺序；387 用例 43s 全绿

## ✅ 阶段 2 — TaskService 单缝整合（2026-09-12）

- [x] TaskService 增 `save_task` / `create_tasks_bulk` / `update_tasks` / `recalc_activity_counts`（均为「信号只发一次」）
- [x] 写路径全收敛：TaskEditPanel / TaskDialog / SettingsDialog / TagPanel / MultiTaskDialog / TimelineDetailDialog 不再触碰 `service._repo` / `service._bus`（无 service 时保留只读回退路径）
- [x] **修复 TaskDialog 幻影 `_signal_bus` 必崩**（task_dialog.py；回归测试 `tests/test_task_dialog.py` 锁定）
- [x] 删除 MainWindow 旧刷新管线（`_refresh_all_views` / `_build_filter_with_sort` / `_on_data_changed` / `_select_and_load_task` / `_on_task_selected` / `_on_filter_changed` / `_on_quick_preset` / `_on_progress_filter` / `_on_carousel_clicked` / `_update_status_bar` / `_on_status_clicked` / `_on_escape` / `_on_task_selection_changed` / `_on_model_data_changed` 等 20 个方法）及重复的控件/总线信号连接；SignalBus 刷新改由 FilterCoordinator 独家订阅（绑定方法，销毁自动断开）
- [x] 分页 UI 移交 FilterCoordinator（新增 `page_changed` 信号 + `go_prev_page` / `go_next_page` / `total_pages`；初始页尺寸读配置）
- [x] 消除批量操作双重查询（批处理槽内 `_on_data_changed()` 显式调用 + `BatchController.data_changed` 均与总线信号重复，已删除）

## ✅ 阶段 3 — 时间轴视图替换任务页（2026-09-14）

- [x] `src/ui/timeline/timeline_model.py`：`TimelineModel` / `TimelineData` / `TimelineRow` / `resolve_range`
  - 7 种粒度（today/yesterday/week/last_week/month/last_month/30d）、命中裁剪、跨度与裁剪计算、排序（截止/优先级/计划起点）、状态/搜索/归档过滤
  - 纯 Python 无 Qt 依赖，29 用例覆盖（`tests/test_timeline_model.py`）
- [x] `TimelineTableView` + Gantt 委托（色条=起止区间、填充=进度、端点=截止、今天竖线、悬停详情）
- [x] `TimelineController` 接入任务页（总线订阅 + 50ms 去抖 + 粒度/排序/过滤 API + `SelectionContext` 写入）
- [x] 单击选中写入 `SelectionContext`；双击 → 维护抽屉（阶段 4），过渡期同步载入 TaskEditPanel
- [x] **旧列表退役**：任务页移除 `TaskListView` / `TaskListModel` / `BatchToolbar` / 分页控件及 7 个批量槽；`FilterCoordinator` 不再依赖列表（改为「过滤控件 → 辅助部件刷新」），冒烟测试改断言时间轴
- [x] 删除孤儿 MultiTaskDialog / TimelineDetailDialog / task_list_panel（+ `__init__` re-export，grep 证据：全项目 0 引用）
- [x] 时间轴工具行：粒度 + 搜索 + 状态 chips + 优先级 + 排序 + 图例（承接旧 FilterBar 全部过滤能力）
- [x] 删除 FilterCoordinator 及旧过滤部件（FilterBar / StatusBadge / QuickOverview / ProgressBar）；总线刷新与选中由 `TimelineController` 独占
- [x] 时间轴默认**包含已完成任务**（`TimelineController._include_archived = True`）—— 分区 `archive_days=0` 时「完成即归档」，默认排除会让任务一勾选完成就从视野消失；状态 chips 计数改为同口径（含归档），避免「已完成 0」与绿色色条并存的矛盾
- [x] 基建修复：`TaskService.dispose()` 显式解绑 `SignalBus` —— 它是普通 Python 对象而非 `QObject`，Qt 不会自动断连，残留实例会在 repository 关闭后继续回调（`app.py` 退出流程与测试 fixture 均已调用）

## ✅ 阶段 4 — 维护抽屉 + 总览页（2026-09-14）

- [x] 总览页收尾修复：① 总线订阅从 `refresh_theme()` 移入 `__init__`（此前主题未切换过时总览页完全不响应数据变化，且每次切主题重复连接 → 一次事件触发 N 次刷新）；② 焦点时间轴改用与任务页一致的口径（包含已完成 / 已归档任务）；③ 合并问候语与时间轴的重复全量查询；④ `create_task` 补写 `completed_at`（此前 md 导入 / 快速新建的 DONE 任务该字段为 `None`，「本周完成」恒为 0）

- [x] TaskService 增 `append_activity` / `get_recent_activity` / `get_urgency_distribution` / `get_due_stats`
  - `append_activity` 只发一次信号（状态变更优先 `task_status_changed`）；12 用例覆盖
- [x] TaskDrawer：滑入动画（`QPropertyAnimation`）/ Esc 关闭 / 保存自动收起 / 活动时间线 + 撰写器
  - 双击时间轴或图谱节点打开；`tests/test_task_drawer.py` 10 用例
- [x] 总览页：问候 + 快速新建 + 4 统计瓦片（点击跳转带过滤）+ 焦点时间轴（6 预设）+ 近期活动 + 紧迫度分布
- [x] 删除孤儿 DashboardController
- [x] 分析页槽迁入 `AnalysisController`（12 个方法 + 导出链路；页面构建后 `attach()` 注入部件，控制器对未注入部件空值短路以支持懒构建）

## ✅ 阶段 5 — WindowShell（仅 Windows）（2026-09-14）

- [x] `src/utils/win32_hotkey.py`：`parse_accel` 纯函数（修饰键位或 + VK 映射）+ `register_hotkey`/`unregister_hotkey`/`is_supported`/`current_accel`；51 用例覆盖
- [x] `config` 增 `general.hotkey` / `general.pin_on_top`（`_deep_merge` 自动补齐，无迁移）
- [x] WindowShell：`_HotkeyEventFilter(QAbstractNativeEventFilter)` 消费 `HOTKEY_MESSAGE` 触发回调
- [x] WindowShell 收编 show/raise/activate（`wake()` / `toggle_visibility()`）；`grep` 确认 MainWindow 内已无裸 `show()/raise_()`
- [x] SystemTrayManager 改收 shell（构造参数 `shell=` → `shell.toggle_visibility()`）
- [x] 设置对话框增热键 / 置顶项；标题栏置顶按钮改接 `WindowShell.set_pinned()`

## ✅ 阶段 6 — 任务图谱 + 设置页签（2026-09-14）

- [x] md_parser 增 `ParsedTask.links`（`[[任务名]]` 提取、标题保留字面量、边界用例）
- [x] formatter 往返稳定锁定（含链接字面量的 `format(parse(raw)) == raw`）
- [x] `src/services/task_graph.py`：`TaskGraphService.build_graph` + `GraphNode`/`GraphEdge`
  - task/tag/partition 节点、tag case-insensitive 归并、`[[链接]]` 边、`hide_done`、`only_related`、确定性排序；16 用例覆盖
- [x] 图谱渲染：**改用自绘**（`src/ui/graph/graph_view.py`，非 `QGraphicsScene`）— 力导向（seed 可复现）/ hover 高亮 / 双击直达任务 / 过滤 chips
  - 变更理由：节点/箭线在数百量级时自绘比场景图更可控
- [x] SettingsDialog 页签化：常规（含热键 / 置顶）/ 分区 / 关于

## ✅ 阶段 7 — 性能 + 主题注册表（2026-09-14）

- [x] repository 批操作 set-based SQL（`batch_*`/`refresh_overdue_status`/`recalc_all_activity_counts` → 单事务 `executemany` + id 分块 IN，`user_version` 恒为 8）
- [x] 总线刷新 50ms 去抖（`TimelineController.schedule_refresh` / `flush_pending_refresh`）
- [x] 时间线/隐藏页 dirty 延迟刷新（`TimelineController.set_visible` + `HeatmapModel`/`CalendarHeatmapWidget` 的 pending 回放）
- [x] 热力图模型缓存（`HeatmapModel` 按 (year, tags, partition) 缓存 + `invalidate()` 失效；4 用例覆盖）
- [x] theme_registry（`src/utils/theme_registry.py` 弱引用注册表）；部件构造时 `register_theme_aware(self)` 自助登记，`MainWindow.refresh_theme` 由逐个 `hasattr` 列举改为 `refresh_theme_all()` 统一广播；5 用例覆盖
- [x] 裸 hex 收敛核查：31 处中仅 `main_window.py` 标题栏提示色属真·UI 语义色（已收敛）；其余为品牌色（`icon_draw`/`splash` 品牌渐变，注释标明主题无关）、启动画面独立配色、HTML 文档 CSS，均不适用 design_tokens

> 「功能对照遗留」清单已于 2026-09-15 撤销：它比的是**已归档的 PySide6 原型**，
> 11 项里 7 项桌面版已经做掉了（md 实时预览、草稿模式、批量新建、二次确认、
> 标签改名合并、活动搜索、md 导入），剩下的不再当作独立待办，逐条并入下面

本轮（2026-09-16）继续：

- [x] **改状态必须留痕**（`pages/shared.ts` 的 `setTaskStatus`）：总览勾选框 / 任务页右键菜单 / 管理页批量 / 抽屉状态按钮这四处此前各自改 `task.status`，**没有一处写活动记录** —— 勾了「完成」，近期活动里没有这一条，「本周完成」也数不到它（完成时间只能退化为结束日，于是「刚完成的这个任务结束日在上周」被漏掉）。现在四处收口到 `setTaskStatus` 统一补一条 `status` 活动；「本周完成」改按这条活动的日期算（`completedOn`），没有活动时退化到结束日。总览那句写死的「较昨日 +2 / 较上周 +3」一并撤掉，换成点开轴就能对上的真实值
- [x] **总览焦点时间轴的档位窗口不再写死**：删掉「本周 = 9/7–9/13」这类日期表，改读 `data/timeline.ts` 那一份定义，与任务页、设置里的「任务视图」同一个窗口 —— 同一个按钮名两页给不同窗口，换一天打开就整体错位
- [x] **当日轴按日期区间取任务**：以前只看「有没有填 at」，于是结束在 9/12 的任务会杵在 9/16 的轴上写着「08:30」，而旁边卡片写着「今日到期 2」：两个数各自都没算错，摆一起就是同一个「今天」两种口径。现在判据是起止日期盖住这一天；过期未完成的从轴上挪到独立一条「已过期未完成 N 项未清」并标出超了几天（以前钉在轴最左侧，那里只放得下 4 条，第 5 条起静悄悄掉进列表），计数也拆成「这一天的 N 项」与「过期 N 项」两个数
- [x] **活动页「导出」真的落一个 csv**（`shell/download.ts`）：以前只弹一句「已导出（演示）」，点了什么都没发生 —— 这比没有这个按钮更糟，用户会去找一个不存在的文件。管理页 md 导出一并走同一条下载路径，文件名带真实日期
- [x] **年份不再写死 2026**：问候条与两处导出文件名改从天数里读年份（`data/time.ts` 的 `isoDay`），跨年之后不会一直说 2026
- [x] `npm run e2e` 之前不触发构建，`vite preview` 跑的是 dist 里的旧包，曾让改动「看起来没生效」。现在 e2e 先跑 `npm run build`
- [x] **焦点时间轴改成「活动驱动」**：轴上只列这一天的活动记录（含新建任务那一条），没动静的任务不占位置 —— 未完成、当前粒度下接下来也没有活动的就不显示。以前判据是「起止日期盖住今天」+ 有没有填 `at`，于是结束在 9/12 的任务会杵在 9/16 的轴上写着「08:30」，而旁边卡片写着「今日到期 2」：两个数各自都没算错，摆在一起就是同一个「今天」两种口径。这条轴回答的是「发生了什么」，不是「该做什么」——后者归任务页和四张指标卡。连带：气泡不再带勾选框（勾一条已经发生的事没有意义）、计数改报「N 条活动」、甘特档同样只画窗口内有活动的任务（条本身仍是起止区间，排的是**计划**）
- [-] ~~「这一天是怎么过的」日回顾~~：**不做**。全库检索「回顾 / 这一天是怎么过」0 命中，从未实现过；且与活动报告页（热力图 + 标签筛选）重合，属于同一个需求的第二张皮
- [x] **气泡挤在一起时同侧自动多开一条道**（`overview.ts` 的 `spreadPills`）：分道只能在进了文档之后做 —— 气泡的 left 是百分比，真实像素要等容器有宽度才量得出来，量不到（页面不可见）就保持原样。**不采用「重合就切甘特」**：甘特的粒度是**天**、当日轴的粒度是**分钟**，今天的十几条活动切过去全落在同一格，照样叠着，还顺带把时刻弄丢了。重合是「同一粒度里放不下」，解法只能是加道。`.tdt` 高度在 CSS 里写死，多开的道由 JS 撑开，否则新道会溢出卡片（基准高度当天 172px，后来总览分两列时降到 150 —— 见「一屏到底」那条；两处现在共用一个 `AXIS_H` 常量）
- [-] ~~焦点时间轴做成鱼骨状~~：**不做**。它想解决的「跨窗口任务」和「条太挤」有更便宜的解法；代价是起止日期要靠斜刺端点读、对齐刻度的精度掉一截，而这条轴的用途恰恰是「什么时候有什么」
- [x] **设置模块精简**：撤掉「安全」「自动化」两组 —— 前者只剩「分区口令」，而它是**某个分区**的属性，摆在那儿得先挑分区；后者只有一行只读说明（逾期标记每次加载顺手扫一遍，本就不给开关），一行占一整组不值当。空闲锁定并入「窗口」，自动归档并入「任务与归档」。从五组两页变成两个页签
- [x] **分区能增 / 删 / 改名 / 各设各的口令**（`data/partitions.ts` 的 `addPartition / renamePartition / removePartition / bootPartitions`）：列表存库、启动读存档 —— 用户自己分过的区不能被内置那四个盖回去。删的时候任务先迁到别的分区：不迁就等于凭空消失，各页都按当前分区过滤，而一个已经不存在的分区永远不会成为「当前」。口令从「常规」挪进分区页签，跟着分区那一行走
- [x] rail 底部的分区切换器不再只画一次：订阅 `onPartitionListChange` 重画 —— 只画一次的菜单里，新建的分区根本切不过去
- [x] **自动归档**：设置里「关 / 7 / 30 / 90 天」，已完成任务结束 N 天后收进归档（`data/store.ts`）。按**结束日**算而不是完成日：完成日要从活动记录里反推，而解析函数在 pages 层，store 在 data 层，反过来 import 会把依赖方向搞乱；结束日只可能晚于完成日，晚几天收比把还在用的数据收走安全。手动「取消归档」的任务本次会话内不再被自动收走（`keepActive`）
- [x] `seg` 是无状态的，选中态得调用方 `setValue` —— 归档 / 空闲锁定原先点了档位高亮不动，看着像没生效（时间轴粒度那一行是因为有外部订阅才对的）
- [x] 种子数据里写死的「今天 15:03」在早上打开就成了「还没发生的活动」，还因为时刻最大排在所有活动最前面。`mock.ts` 的 `clampSeedClock` 把整批「今天 HH:MM」压到 5 分钟前以内，间隔等比保留（密集时段照样挤成一簇）
- [x] 过期未完成不再连带吞掉它**今天的活动**：以前一个 `continue` 让「过期」和「今天有活动」互斥 —— 给一个已过期的任务补一条进展，那条进展在轴上反而消失了，越忙一天轴越空。现在过期只作为任务维度的提示单独列一行
- [x] **自动归档与空闲锁定改成按分区**（`store.ts` / `lock.ts`）：它们和口令一样是**某个分区**的属性 —— 工作区的东西比个人区更需要自动挡一层、过期任务也更该收走。摆成全局要么得先挑分区（多一层），要么取一个两头都不对的折中值。存储从单个值改成 `Record<分区 id, 值>`；空闲到点只锁**当前**分区（`lockCurrent`，原来是 `lockAll`），切分区时重新起计时。「任务与归档」因此退回「任务视图」，只剩时间轴粒度
- [x] **有任务的分区不给删**：不做「自动迁到别的分区」—— 迁去哪儿用户没得选，等于替他决定一批任务的归属；真要留着那些任务，他自己先移走更清楚。所以按钮置灰并写明剩几条。空的分区照删
- [x] **设过的口令能改**：忘了旧的在别的分区里重设一个即可，**不校验旧口令** —— 它挡的是路过的人，没有「证明你是你」的必要；要旧口令反而多出一个「忘了就彻底进不去」的死结（`lock.ts` 顶部注释同步改了）
- [x] **设置里的「时间轴默认粒度」撤了**（`data/timeline.ts` / `pages/tasks.ts`）：它其实是任务页工具行那个 seg 的**第二个入口**，且改的是当前视图、不持久化（刷新回到「本周」）—— 叫「默认」名不副实，进设置还得解释为什么改了不记住。真正的入口就在时间轴正上方，顺手。两处共享一份状态要靠互相订阅防回声（`setTimelineRange` 里「值没变就返回」就是为挡这个环），现在单入口，广播层一并删掉，改完直接 `render()`。「任务视图」这组因此空了，整组撤掉 —— 常规页签剩「外观」「窗口」
- [x] **设置不再分页签，一页到底**（`shell/settings.ts` + `index.html` + `styles/shell.css`）：分「常规 / 分区 / 关于」三页时每页只剩两三行，翻页成本比滚动高，还得记「口令在哪一页」。现在整页是 外观 → 窗口 → 分区 → 关于，`.set-tab` / `.set-tab.active` 那两条 CSS 一并删（`renderTab` → `renderGroup`）
- [x] **切到别的模块自动收起设置抽屉**：`mountSettings` 里 `subscribePages`，与「编辑任务」抽屉（`taskDrawer.ts`）一致 —— 设置讲的是外壳与分区，翻页后还挂在那儿，看上去像新页面长出来的一层，也说不清它属于谁
- [x] **分区配置按上一版的样式合并成一行**：`工作 · 19 条 🔒 … [自动 ▾][口令][改名][删除]`。自动归档 / 空闲锁定默认关、是少数人用得着的那类设置，收在「自动 ▾」里、展开了才建那两个 seg（四个分区不会再各挂两个常年不动的控件）；两项只要有一项不是「关」，按钮染强调色，收着也看得出这个分区动过默认值。条数与锁挪到名字一侧 —— 抽屉只有 420px，摆右边会和四个按钮抢位置
- [x] **分区能指定默认分区**（`data/partitions.ts` / `shell/settings.ts` / `styles/shell.css` 的 `.part-dot`）：启动时进哪个区，和「现在在哪个区」是两件事 —— 切分区是随时都在做的事，默认不跟着切。以前只有 `active` 且不存盘，每次打开都落列表第一个，把自己常用的那个排在第二位的人每次都得再切一次。存 `partitions.default`，`bootPartitions` 里 `active` 落默认分区；删掉的正好是默认就退回第一个。UI 是行首一个**单选点**（一组里只能有一个，画成按钮的话「默认 / 设为默认」两种文字来回切反而看不清）；设到上锁的分区会 toast 提醒「启动时先要口令」
- [x] **导出全局统一：md / txt / xlsx 三选一**（新增 `data/export.ts` `data/xlsx.ts` `shell/exportMenu.ts`，改 `pages/manage.ts` `pages/activity.ts`）：以前两个页面各导出各的（管理页 .md 方言、活动页 .csv），「导出」在同一个应用里是两种互不相干的东西。现在 **md 与 txt 内容完全一致**（同一段文本，只有扩展名不同，与 python 版导出的排版一致），xlsx 是同一批数据的表格形态；文件名主体由调用方给、扩展名在 `buildExport` 补。xlsx 自己生成（zip store + CRC32 + `inlineStr`，无第三方依赖）：为一个按钮往依赖里塞几百 KB 不值当，而它是用户明确要的格式；e2e 里逐项校验 CRC —— 下载成功不代表包是好的，CRC 写错时 Excel 才报「文件已损坏」
- [x] **活动导出的排版：标签作标题、任务有序、活动无序**（`pages/activity.ts`）：md 与 txt 同一份文本 —— 标签本身就是分组标题（不再套 `##`），下面是**有序列表**的任务，任务底下的活动是**无序列表**（缩进三空格，对齐有序列表的内容列才会被当成嵌套）。xlsx 仍是扁平表：标签 / # / 任务 / 状态 / 时间 / 内容。管理页的任务导出不动 —— 它是 md 方言行（可回导的规范格式，DESIGN 2.11），改成分组就导不回来了
- [x] **任务管理：删除「导入 .md」**（`pages/manage.ts`）：方言本来就带不回状态（导出再导入会把已完成变成待办，见 `data/markdown.ts` 顶部），而任务页的批量新建框吃**同一套方言**、当场就能看见解析出几条 —— 先存成文件再导回来只是多绕一圈。「导出 .md」改名「导出 ▾」并走统一的三格式菜单，且跟着**当前筛选**走（表格上摆着状态 / 归档两个筛选，导出「这个分区全部」会让人以为筛选没生效）
- [x] **活动分析：导出与「范围」同一行、作为独立按钮钉在该行最右**（`pages/activity.ts` + `styles/pages.css`）：从左排起是范围组（标题 + 时间档位 + 日期框 + 区间说明 —— 那个区间是范围的产物，跟着范围走），从右排起是导出按钮；两者之间由弹性空白分开，它按这个范围取数所以同行，但不是又一组时间档位。**坑**：`.grow` 是按容器作用域定义的（`.card-h` / `.tools` / `.batchbar` … 各写一条），范围条里漏了这条 → 那个 span 宽 0，导出紧紧黏在区间说明后面（类名在、位置没变，看图才发现）。补 `.act-filter .grow { flex: 1 }`，e2e 也从「有 grow 类」升级为**量真几何**（按钮右边缘贴着范围条右内边距）。取数/排版是模块级函数 `datedRowsOf` / `tagStatsNow` / `queryGroups` / `exportTable`，不捕获 mount 的局部变量（页面随时 remount，捕获就会停在打开页面那一刻）
- [x] **标题栏左上角只留软件名**（`index.html` / `styles/shell.css` / `shell/titlebar.ts`）：LOGO 与版本号徽章都撤了 —— 图标在任务栏 / 托盘 / 安装包上已经见过一遍，标题栏里再来一张是同一句话重复三遍；版本号挪去设置 → 关于，那里由 Tauri 填**真实**版本（`#set-ver`，拿不到才退回写死的 0.1.0），不再是一枚写死的徽章。`.logo` / `.ver` 两条 CSS 一并删，`renderVersion()` 与 `getVersion` 的 import 跟着走
- [x] **设置里的「窗口置顶」撤了**（`shell/settings.ts` / `styles/shell.css`）：它是标题栏图钉的**第二个入口**，而图钉就在那儿一键切换 —— 与其他设置项不同，它是「现在就要这个窗口浮上来」的动作，不是一条要翻两层菜单去改的偏好。图钉保留（唯一入口），`pinControl` 与 `onPinChange` / `togglePinned` 的 import 一并删。顺带：上一轮为这一行加的 `SettingRow.hint` 与 `.set-lb` / `.set-hint` 也随之删掉 —— 没有第二行用得上它，留着是没人走的机制（「窗口」组现在只剩「全局热键」一行只读值）
- [x] **「常驻置顶」改名「窗口置顶」并补一句说明**（名字随后随本项一起撤了，改名只存在于 git 历史）（`shell/settings.ts` / `titlebar.ts` / `index.html`）：那四个字说不出它在干什么 ——「常驻」听上去像开机自启，「置顶」又像把任务钉在列表头上。它其实是窗口浮在所有应用之上，所以标签照实说，底下小字「浮在其他应用之上，切到别处也看得见」（`SettingRow.hint` + `.set-lb` / `.set-hint`）。图钉 `title` 与 toast 文案同步改
- [x] **锁屏上加「设置」出口**（`shell/lock.ts` / `panels.ts` / `shell/settings.ts` / `styles/controls.css`）：锁屏盖住整个应用，而重设口令不需要旧口令（只挡路过的人），所以没有这个入口就是**死循环** —— 想改口令先得进得去，进得去又得先知道口令。默认分区上着锁又忘了口令，等于开机即锁死。做法是「让位」而不是「解锁」：`openSettings` 若发现锁屏开着就 `suppressLockScreen()`（body 加 `.lock-suppressed`，只把抽屉 210 / 弹窗 220 抬到锁屏 200 之上），主区 / 侧栏 / 标题栏照旧被盖着点不到；`closeSettings` 配对 `restoreLockScreen()`，期间清了口令锁屏就自己收起。lock.ts 不能直接 import settings.ts（settings 依赖 lock，是环），所以 panels.ts 新增 `registerOpen` / `openPanel`，按 id 中转
- [x] **托盘菜单：重新划定为三项**（`src-tauri/src/lib.rs` + `shell/trayBridge.ts`）：最终是 `设置 / 显示窗口 / 退出 Tadado`，三项等距、**不加分隔线**（试过加：Windows 原生菜单的分隔线自带 8–10px 上下留白，插在中间会让那一处明显比其它相邻项松，看着像没对齐）。撤掉的：「收起窗口」（与「显示窗口」永远只有一项有效，而静态菜单不会变灰；左键单击托盘仍是切换显隐）、「新建任务」（点它只能把主窗口叫出来再弹一层浮层，而用户要的是「只有那个新建框、后面不出主界面」—— 那得另开一个窗口，不值当。**Rust → 前端的事件通道**保留：新增 `shell/trayBridge.ts`，Rust 只负责把窗口叫出来再 `app.emit("tray", "设置")`，真正的动作（开抽屉）在前端；浏览器预览里 listen 会失败，静默忽略
- [x] **设置里新增「开机自启动」**（`shell/autostart.ts` + `shell/settings.ts` + tauri-plugin-autostart）：Rust 侧 `.plugin(tauri_plugin_autostart::init(...))`，capabilities 加 `autostart:allow-enable/disable/is-enabled`，前端包一层「浏览器里也不会炸」的门面。设置面板是同步渲染的，而真实值要问系统（异步）—— 先渲染成「关」、拿到再翻过来；**问不到时（浏览器预览）整行标成 `.sw.disabled`**，而不是摆一个点了没反应的开关。原来那一组叫「窗口」，现在改叫**「启动」**（开机自启动是进程层面、全局热键是窗口层面，都是「怎么把它叫起来」）
- [x] **压测数据的时间关系修正**（`data/mock.ts` 的 `stressTasks`）：原来「创建任务」那条活动落在 `min(截止日, 昨天…)` 上，于是**截止日已过的任务，活动时间恰好等于它的截止日**（连钟点都一样，因为 `due` 和它共用 `hour`）—— 导出去看着像「创建于截止日」。现在按 `创建日 ≤ 开始日 ≤ 截止日` 生成，且创建日一定在过去（未来截止的任务也是过去某天建的）。e2e 加一条：导出里的活动时间**都不晚于今天**（活动记的是已经发生的事）
- [x] **压测数据的覆盖策略**（`data/store.ts`）：`STRESS_EXTRA > 0` 时让**种子说了算** —— 缺的补进来、已有的 `bulk-*` 用种子的版本覆盖。不这么做的话，改了生成逻辑后库里那份不会变（id 已存在就不补），刷新看到的还是旧数据。覆盖范围限定 `bulk-*`：那批是自动生成的演示数据，不是用户资产；手写的 28 条原型任务仍尊重库里的版本
- [x] **压测数据：种子任务 28 → 100 条**（`data/mock.ts` 的 `STRESS_EXTRA = 72` + `stressTasks()`）：看「一屏画多少条」这类上限（ROW_LIMIT / GANTT_LIMIT / FEED_LIMIT）在大批量数据下的样子。**生成而不是手写**（100 条字面量没人维护得动），取值全部由序号推出 —— **确定性**，每次启动是同一批，随机会让「刚才那条去哪了」没法回答。状态跟日期对得上（过去的要么完成要么逾期，未来的才是进行中 / 待办）；活动日一律落在**过去**（未来的活动会排到「刚刚」前面）。启动补齐：`bootStore` 在 `stressExtra() > 0` 时把种子里缺的按 id 补进库（改完常量重启就看得见，不用清库）；正常情况仍以库为准，设回 0 后这 72 条**不会自己消失**，要清就删 `tadado.data`（桌面端）/ localStorage 的 `tadado.tasks.v1`（浏览器）
- [x] **`clampSeedClock` 的调用时机**：种子拆成 `SEED_TASKS`（唯一实例）+ `TASKS`（深拷贝）之后，必须**先压时钟再拷** —— 拷完再压只改到种子，副本里仍是「今天 15:03」这种还没发生的时刻，它会排在「刚刚」前面，把刚建的任务挤出近期活动
- [x] **总览的数字点开必须对得上（窗口自适应）**（`pages/tasks.ts` 的 `fitWindow`）：以前任务页**先按档位定窗口、再拿窗口砍任务**，于是总览写「逾期 4」、点进来只有 3 条 —— 第 4 条的起止落在窗口外，它没丢，只是没被画出来，而用户只能认为数字是假的。顺序反过来：**先筛，再让窗口去迁就筛出来的这批**（窗口可能比档位宽，表头一直写着真实区间）。同理修掉「点近期活动 / 焦点时间轴 / 图谱跳过去，抽屉开了、列表里却没有它」—— 定位的那条若排在 `ROW_LIMIT` 之外，会提到最前面，保证列表里有它
- [x] **chips 数字 = 点开后的行数**（`poolTasks`）：状态 chips 的数按「优先级 + 搜索」算、唯独不含状态本身。以前按 `activeTasks` 全量算，于是「逾期 4」点进去 3 行
- [x] **优先级筛选**（`focus.ts` 的 `showTasksWithUrgency` + 任务页 dropdown）：总览「紧迫度分布」→ **「优先级分布」**（术语跟着编辑面板的「优先级 P0–P3」统一），每一档可点击 → 跳任务页并按该优先级筛出。以前这一整块点了没反应。`jumpToTask` / `showTasksWithFilter` 现在都会把**两个筛选维度一起复位**（留着上一次的「只看逾期」再定位一个进行中的任务，就是抽屉开了、列表里没有它）
- [x] **「本周完成」tile → 「已完成」**：点它进的是任务页的「已完成」筛选，那里列的是**全部**已完成；名字说本周、点开是全部，两个数对不上。本周的数挪到副标（「本周 N · 较上周 +x」）
- [x] **近期活动行更丰富**：时间 + **状态色点** + 标题 · 进展 + 右端**标签**（一行只有一句标题时右侧空着也是空着）
- [x] **任务管理的导出与活动分析统一**（`data/export.ts` 抽 `groupedText` + `ExportGroup`；`pages/manage.ts` 的 `exportGroups`）：两处都是「标签 → 任务（有序）→ 该任务的活动（无序）」，md / txt 各一套符号，生成逻辑**共用一份**（各写一份必然分叉）。管理页的任务按**第一个标签**分块 —— 一条任务只出现一次（这是任务清单，同一条出现两遍会让人以为有两件事；活动分析那边按标签查活动，天生会重复，所以那边做了去重）。**管理页原来的 md 方言（`- [ ] 标题 #标签 ⏰09-25`）不再用于导出** —— 那是为了能导回来，而导入 2026-09-17 已撤，约束消失。顺带删掉随之无人使用的 `tasksToMarkdown`，并把 `data/markdown.ts` 顶部说明改写成「现在只管单条编辑框与批量新建两处往返」
- [x] **活动时间改成真时间戳（模型层）**（`data/types.ts` 的 `At` + `data/time.ts` 的 `stampOf` / `dayOfStamp` / `minuteOfStamp` / `nowStamp` / `stampText`）：以前 `Activity.at` 存的是**展示串**（`"刚刚"` / `"今天 09:12"` / `"昨天 17:20"` / `"09-11 09:30"`），于是 —— 库里写着「昨天」，明天打开同一条就成了「前天」；界面和导出都要靠**正则反解这句话**才算得出日期（`activityDay`、`activitySortKey`、`RELATIVE_DAYS` 一整条链路）；「今天更新了几个」这句统计是拿 `at.startsWith("今天")` 数出来的。现在存 epoch 毫秒，归日是一句除法，相对词一个不留（界面 / 导出 / 库里都是 `09-17 14:19`）。**注意用 `nowStamp()` 而不是 `Date.now()`**：天数基准是「本地日历日的 UTC 型 anchor」，直接混真实 UTC 时刻会差一个时区（本地 09:12 存进去、显示成 01:12）。种子里仍写人话（那 28 条要和原型逐字对照），构建 `SEED_TASKS` 时由 `seedStamp()` 一次性换算 —— 顺序是先压时钟（`clampSeedClock`）再换算，反了就没得压。「无日期活动」这个概念随解析函数一起消失了（`undatedCount` 及相关文案删除）。**旧数据在读入口归一化**（`data/store.ts` 的 `normalizeStamps` + `data/time.ts` 的 `parseStampText`）：改造前活动时刻存的是展示串，直接当数字用会算出 `NaN-NaN 05:42`（那个 05:42 是 `new Date("09-16 13:42")` 按本地时区解析、取 UTC 再差 8 小时的结果，看着像真的）。**没有在显示层做「—」兜底** —— 那只是把「数据不合法」藏起来；模型的不变量是「at 永远是有效时间戳」，所以在读入口修一次并写回（相对词的换算是有损的，换不出的落到任务创建日上午）。e2e 把旧串塞回 localStorage 再 reload，验证它被归一化成数字、界面不出现 NaN
- [x] **活动导出的 md / txt 从「同一份文本」改成「同一套层次、两套符号」**（`data/export.ts` 的 `ExportTable.text` → `{ md, txt }`；`pages/activity.ts` 新增 `exportMarkdown` / `exportPlain`）：三层结构 = 标签分块 → 任务有序列表 → 活动无序列表。md 里标签行写 `#后端`（**`#` 后不留空格** —— 留了空格 md 就把它当一级标题渲染成一行大号字，比任务本身还抢眼；不留空格时它只是一句普通文本，仍然一眼看得出分块的开头）、`1.` 任务、缩进 **3 空格** 的 `-` 活动 —— 3 格正好对齐 `1. ` 的内容列，缩 2 格会变成「懒惰续行」、缩 4 格直接变代码块）；txt 一个 md 符号都不用（`【后端】` / `1)` / `·`，任务缩 2 格、活动缩 5 格对齐），因为它的用途就是丢给不认 markdown 的地方（记事本、工单），在那里 `# 后端` 会被原样显示、`- ` 是一串小横杠。文件头那行 `<!-- tadado · … -->` 元信息去掉（范围 / 条数在界面上本来就看得见）。管理页的任务导出**不动**：它导的是可回导的方言行（DESIGN 2.11），把 `- [ ]` 换成 `·` 就再也导不回来了
- [x] **四张表都分页、档位统一为 20/30/50/100**（`pages/pager.ts` + `pages/shared.ts`）：在管理页表格 / 任务页时间轴 / 活动分析报告之外，总览「近期活动」也加上了分页（一次查询可能上百条，全倒进卡片会把下面整页撑开）。四张表共用同一个 `pager()`，所以「第几条到第几条 + 翻页 + 每页几条」在哪儿都长一个样；档位只有一组 `PAGE_SIZES=[20,30,50,100]`，默认每页都是 20（四个常量分开是为了想单独调某张表时不必改全局）。近期活动原来是 `FEED_LIMIT=7` 截断，现在改成 `FEED_PAGE_SIZE` 分页
- [x] **三张表都分页**（新增 `pages/pager.ts`，管理页表格 / 任务页时间轴 / 活动分析报告共用一份）：抽出来是因为三处都要「第几条到第几条 + 翻页 + 每页几条」，各写一份迟早出现一处翻到最后一页夹不住、一处换了每页条数没回第一页。默认每页：管理页 12（`PAGE_SIZE`）、任务页 25（`TASK_PAGE_SIZE`）、活动报告 20（`REPORT_PAGE_SIZE`），档位共用 `PAGE_SIZES=[12,25,50,100]`。任务页的 25 以前是**截断**（只画前 25 条，翻不到后面），现在能翻页；窗口按**当前页**的行去撑（`fitWindow`），所以翻页时表头区间跟着变。定位某个任务时会先算出它在第几页再翻过去（跨页跳转仍能看到它）；换筛选 / 排序 / 档位 / 搜索词 / 标签 / 范围都回到第一页
- [x] **显示条数：常量集中 + 每页条数有入口**（`pages/shared.ts` + `pages/manage.ts`）：`ROW_LIMIT=25`（任务页时间轴**上限**，截断）、`PAGE_SIZE=12` / `PAGE_SIZES=[12,25,50,100]`（管理页表格**每页**，有翻页器）、`GANTT_LIMIT=20`（总览甘特）、`FEED_LIMIT=7`（近期活动）。**不进设置** —— 上限是渲染上限不是偏好；而「每页几条」的入口就放在**分页器上**（下拉 12/25/50/100），它只影响眼前这张表，不该让人去设置里翻。截断了就把三个数都写出来（「前 25 条 · 筛出 30 · 共 42」），别让人以为「只画了这么多」＝「只有这么多」。管理页那个 `PAGE_SIZE=12` 以前是写死在 manage.ts 里的局部常量，所以「改常量」对它根本不生效 —— 一并收进 shared.ts
- [x] **两条 flaky 用例的真因：新建的任务不在当前页**（`e2e/smoke.mjs`）：「不带 # 的多个标签能入库」「标签独立成行」偶发失败，报的是「(没有标签)」这种看不出所以然的信息。真因不是时序而是**分页**：100 条数据下这张表按截止排序，今天截止的新任务排在几十条逾期任务**后面**，多半落在第 2 页 —— `locator` 在当前页一行都匹配不到。改成先搜出来再断言（与「批量建出两条」同一种处理），并在断言后清掉搜索词免得污染后面的用例。之前两次都当成 flaky 放过去了 —— 那种「偶发」往往只是**条件没凑齐**
- [x] **打包 Tadado2 v1.0.0**（`npm run tauri build`）：release 编译 4m06s，出两个安装包 —— NSIS `Tadado2_1.0.0_x64-setup.exe`（2.6 MB，双击即装）与 MSI `Tadado2_1.0.0_x64_en-US.msi`（3.6 MB，给静默/企业分发），都在 `src-tauri/target/release/bundle/` 下；另拷了一份便携版 `bundle/portable/Tadado2.exe`。**验证不是「能打开就行」**：读主程序的版本资源，确认 `ProductName = Tadado2`、`FileVersion` / `ProductVersion = 1.0.0` —— 这正好回答之前那个「版本号没变」的疑问：当时是 Rust 二进制没重编译，不是改错了地方。安装后主程序叫 `Tadado2.exe`；`target/release/desktop.exe` 那个名字来自 Cargo 包名（故意没动，见改名的说明）
- [x] **方言支持「活动行」：活动清单可以直接导入**（`data/markdown.ts` + `pages/tasks.ts` + `pages/overview.ts` + e2e）：活动分析导出的清单是「标签 → 任务 → 活动」三层，而导入通道此前只认任务行 —— 把清单粘进批量新建，活动会一条不剩地丢掉（在一份真实清单里那是 82% 的行）。现在方言多认一种行：**缩进在任务下面、以 `MM-DD HH:MM` 开头的一行**属于该任务的时间线（缩进 ≥2 格、`- ` 前缀可选，两种导出写法都能直接粘）。时刻交给现成的 `parseStampText`（它本来就认这个写法），活动文本一个字符不改。配套三条：① 带了活动行就**不再**补那条假的「导入任务」；② 活动按「新的在前」排一次（数组的既有约定）；③ 总览「今日到期」加 `due !== null` 判据 —— 迁移来的旧数据里有一批**没有截止**的任务，它们的 end 只能落今天，只看 end 会把「没有这个信息」显示成一个具体的日子。e2e 两条：带活动行的粘贴按一条任务预览、导入后活动进了时间线且新的在前
- [x] **新增 skill：`tadado-activity-import`（活动清单 → 可导入方言）**（`~/.claude/skills/tadado-activity-import/`：`SKILL.md` + `scripts/convert.mjs` + `convert.bat`）：设计原则是**先对账，再动手** —— 默认只解析只报告、不写任何文件；认不出的行必须停下问用户；源里每一行活动都要有去向（输出 / 并入续行 / 重复剔除），数字不平就不许写出；需要拍板的事（同名合并、截止缺失、标签超 3、优先级无来源）集中列出来问。**端到端核对抓出一个静默错误**：截止挖掘用了两条正则，后一条把前一条的结果覆盖回箭头**左边**的旧日期（`截止时间 A -> B` 读成了 A）—— 靠"把转换结果再喂回应用真正的解析器数一遍"才看出来。这是这轮最值钱的一步：**转换器的输出必须能被下游的解析器验一遍**，「数量对」不能只是自己说的
- [x] **改名 Tadado2 + 版本从 v1.0.0 起 + 重写「关于」**（`tauri.conf.json` `Cargo.toml` `package.json` `index.html` `src-tauri/src/lib.rs` `shell/settings.ts` + `styles/shell.css`）：`productName` / 窗口标题 / 标题栏品牌字 / `<title>` / 托盘 tooltip 与「退出 Tadado2」/ 开机自启那行的 tooltip 全部对齐；版本号四处一起提到 `1.0.0`（tauri.conf / Cargo / package.json / 设置里的兜底值），显示时补 `v` 前缀（Tauri 回话给的是纯数字）。**故意不动**：`identifier`（`com.tadado.app`）—— 它决定 `%APPDATA%\com.tadado.app` 这个数据目录，改了等于让用户现有的库、窗口位置、设置全部「消失」；同理数据库文件名 `tadado.data` 与 localStorage 的键、Cargo 的 package name（开机自启的注册项按它走）都保持原样。导出文件名前缀改成 `tadado2-`。「关于」重写成按实际功能写的介绍（`SettingGroup` 加 `intro`）：应用名 + 一句话定位 + 「功能」「特色」两份清单（对着五个页面与真实能力逐条列，不写技术栈、不提旧版本），e2e 断言两处：标题栏品牌 = `Tadado2`、关于面板的名/定位/两份清单/版本号都在
- [x] **真机验证的插曲：一次误判，和一条因此加上的自检**（`db.ts` 的 `sqliteApi`）：`npm run tauri dev` 起来正常、100 条数据照常显示，但我直接读库文件头（第 60–63 字节）读到 `user_version = 0`，于是断定「tauri-plugin-sql 上 PRAGMA 写不生效」，还把版本号搬进了 kv 表。**这个结论是错的** —— 应用当时还开着，最新写入躺在 951KB 的 `-wal` 里没 checkpoint，重启（关闭时 checkpoint）之后主文件里就是 1。**外部读到的值可能是陈旧视图，别拿它去推翻一个还在运行的进程**。最终回到 PRAGMA 单一来源（与 Py 版同一套，版本跟着数据库文件走），并加一条**写完读回核对**：PRAGMA 万一被忽略是静默的，而这套机制静默退化（每次启动重跑一遍迁移）要等某天真加了破坏性迁移才会暴露，那时它已经在用户的库上跑过好几遍了。更一般的教训：界面正常只说明「没抛错」，机制性的写入需要能独立核对
- [x] **存储版本与迁移：版本号 + 线性迁移链**（新增 `data/schema.ts`，改 `db.ts` `store.ts` `partitions.ts` `shell/lock.ts` `main.ts`）：桌面端此前**零机制** —— 只有一句 `CREATE TABLE IF NOT EXISTS`，没有任何版本号。现在照搬归档分支上 PySide6 版那套（`src/models/migrations.py`，那边跑到 8；配套文档 `docs/database-migration-technique.md`）：`MIGRATIONS: {from, to, sql?, run?}[]` 线性链，启动时读 `PRAGMA user_version` 顺序跑完写回。规矩照搬：**只能加不能改**（只 `ALTER TABLE ADD COLUMN`，不删列、不改类型）。`0 → 1` 就是「确保这套表在」—— **老库的 user_version 读出来也是 0，于是它一启动就被带上版本号，数据一条没动**，这是存量库零成本升级的路子。浏览器预览没有 SQLite，版本号落在 kv 的一个 key（`tadado.schema.version`），但迁移链是同一条 —— e2e 因此能真的验迁移
- [x] **读入口归位：`data` 列里的 JSON 也得有人管**（`data/schema.ts` 的 `normalizeTasks`）：业务字段整条塞在 `tasks.data` 里，所以「表结构」有版本号管、「列里的结构」只能靠读入口 —— 缺字段给默认值、类型不对的救回来、旧格式（活动时刻的展示串）换新格式、认不出的活动类型退成 `log`、连 id 或标题都没有的整条丢掉并计数。`JSON.parse(...) as Task` 原来是**断言不是校验**，少一个字段就是 `undefined` 一路带到界面（`tags.join()` 直接抛）。原先散在 `store.ts` 的 `normalizeStamps` 一并收编（两份归位逻辑并存的结果是「都修好了但没人知道哪份说了算」），kv 值也加了取证（`asStringRecord` / `asNumberRecord` / `asPartitionList`：存档形状不对就当没设过，以前是读出来直接用）
- [x] **库故障不再静默降级成「数据没了」**（`db.ts` 的 `connect` / `backend = "error"` + `main.ts` 的提示）：以前那个 `catch` 把「库打不开」和「浏览器里没有插件」当成同一件事，一律降级到 localStorage —— 后果是用户的库静静地不被读取、界面一个字不说、此后所有写入都落到 localStorage（数据被劈成两半，而表面上一切正常）。现在**先判环境再谈连库**（`isTauri()`，本来就是预期路径，不该产生告警），库故障是终态：不降级、不重试、不写入。另外读失败后**禁止写入**（`writeBlocked`）—— 少了这条，「坏存档原样留着」只是句好话：界面继续跑，用户随手一点就触发 `dataChanged` 全量覆盖，那份还能人工捞回来的存档当场没了。外壳用 toast 把原因说出来
- [x] **测试样例全部收进演示空间：100 条，其他分区为空**（`data/mock.ts` `data/partitions.ts` `data/store.ts` + e2e）：以前种子按标签匀到四个分区（「每个区都得有东西可看」），结果是每区都只有几条、看不出规模，压测也压不出东西。现在 `SEED_TASKS` 一律落在演示空间，规模 = 28 条原型手写 + 72 条压测生成 = **100**（`DEMO_TASK_COUNT`），默认分区也改成演示空间（否则开机进「工作」是一片空，像数据没加载）。**「自动清空」靠的是启动时归位**：老库里散在别区的种子由 `adoptSeedPartitions` 搬回演示空间（只动种子 id，用户自己建的任务一条都不碰）—— 光改种子只对全新安装成立。注意 100 里有 1 条是**已归档**样例（管理页归档列要有东西看），所以任务页列得出 99 条，e2e 两边一起数。e2e 顺带修了两处被这次改动暴露的问题：① 从总览点「逾期 38」进任务页停在上一轮翻到的第 2 页，看到的只是后半截 —— 换筛选/优先级就回第一页（`tasks.ts` 消费跨页请求时重置 `page`）；② `.card-b` 上的 `justify-content:center` + 滚动容器会把溢出的那半截推到**滚不回来**的一侧（被卡头盖住、点不到，e2e 卡在「点轴上那个簇」超时），改用 `margin: auto 0`。断言也从「当前页行数」改成「筛出来的总数」——100 条之后一页装不下，比行数会误判
- [x] **`Px` 徽标定稿：红 → 绿色阶 + 字重**（`styles/tokens.css` `styles/controls.css` `pages/shared.ts` + 截图重拍）：我先把 `Px` 做成了灰阶（P0 实心最深 → P3 描边），你的反馈是「不够醒目」，并建议「用**字体颜色**而不是背景色」。第一版按「红色渐变」做了，你随即纠正 —— 红阶四档反而分不开（**同一色相只剩深浅，四档之间没有台阶**），改成**红 → 绿**。实现上把现有语义色当停靠点：`--p0: var(--danger)`（红）→ `--p1: var(--doing)`（橙）→ `--p2: color-mix(doing 45%, done)`（橙绿之间）→ `--p3: var(--done)`（绿），**不引入新色相**，且写成 var/color-mix 而不是四个字面值 —— 深色主题换了那几个语义色，这一串在使用点自动跟着换（不必在两个主题块里各写一份）。颜色之外再叠一层**字重**（P0 800 → P3 500），徽标同时**去掉背景与描边**（就是红绿文字），因为「色块」那种画法会和状态圆点同样显眼。**代价写在代码注释里**：P0/P1/P3 会分别与「逾期 / 进行中 / 已完成」**同色** —— 红-绿这套本身就用掉了这三个色。区分靠形状与位置：状态是行首那个 8px 圆点，优先级是任务列**右端**的 `Px` 文字（截图里确认过，一左一右 + 一个是色块一个是等宽字，读起来不会混）
- [x] **导入后实测报的 11 条：先修确定是 bug 的五处 + 把任务页档位改成「进度透镜」**（`pages/activity.ts` `pages/tasks.ts` `data/timeline.ts` `pages/shared.ts` `styles/controls.css` `styles/pages.css` `pages/manage.ts` `pages/overview.ts` + e2e + 截图重拍 + `DESIGN.md` §4.2）：你导入 46 条旧数据后逐条报的问题，先让探索代理把 11 处实现读出来（事实，不是猜测），再逐条判「是不是 bug」。**判为真 bug 并已修的**：① **活动分析的勾选集不跟范围走**（`checked` 只在分区变化时重算）→ 改成「分区 + 范围」一起算，且**用户手动动过就尊重他的选择**（拿当前集合和上次自动默认比对，不需要额外的『动过』标记）—— 这就是「换档位后还得手动点全选」和「点热力图某天报告不联动」的同一个根因；② **总览跳任务页不清搜索词**（三条路径里只有『定位某条任务』那条清了）—— 搜索框留着上次的字，目标任务就不在结果里；③ **`Px` 与状态圆点颜色交叉** → `Px` 改**灰阶**（P0 实心最深 → P3 只留描边），状态圆点保留颜色，两个通道分开；④ 归档列的 `—` 加悬浮提示「未归档」；⑤ 总览那张卡写成「共 N 条**活动**」（和任务页的「共 N 条**任务**」不再看着像对不上）。**判为设计问题、要你定方向的**：#1 自动归档（机制存在，但按分区设、判据是**截止日 + N 天**、只在启动与改设置时扫）、#10 时间轴气泡写「创建任务」、#11 近期活动的口径 —— 这三条还开着。**最大的一批：任务页档位从「窗口下界」改成「进度透镜」**（你定的 A1：框选范围 → 只看该范围内**动过**的任务 → 想看细节点开抽屉）：档位加「**全部**」并设为默认（不然导入的老任务一进页面像集体消失 ✓✓），档位 ≠ 全部 时窗口**严格等于档位** + 超出的色条贴边打截断标记 + 数字注明口径（`筛出 17 · 共 105（上周有活动的任务里）`），「全部」时窗口跟任务跨度走 + 固定 22px 列宽 + 横向滚动 + 吸附首列。**期间踩出两件事**：（a）我第一版把 `checkedKey = key` 写在了比较**之前**，判断永远不成立（自己抓到的）；（b）`seg` 的高亮一直不跟点击走 —— 它是**无状态**控件、要调用方调 `setValue`，而任务页漏了这步，所以「点档位像没反应」其实有两层理由（窗口被撑开 + 高亮停在原处），总览那处因为整页重建所以没事。**实测数据**（探针，看完即删）：全部 20 行 / 48 天列 / 截断 0 / 滚动 1304 > 1058 ✓，上周 16 行 / **7 天列** / 截断 8 处 ✓ —— 窗口与透镜都按预期工作
- [x] **同步本机的 skill**（工作目录外：`~/.claude/skills/tadado-activity-import/SKILL.md`）：你要求「更新当前电脑上的 SKILL」。**先核现状**：它已经是「**指向仓库里那份工具、自己不带副本**」的形态（2.0.0，目录里只剩 `SKILL.md`，`scripts/convert.mjs` 与 `convert.bat` 都已不在）—— 所以不存在我担心的「两份实现分叉」。逐条对了一遍它的说法，绝大多数是**对的**（默认输出名 `<清单>-可导入.md` ✓、退出码 0/1/2 ✓、导入入口「任务管理页 → 数据迁入」✓、四种行型 ✓、铁律五条 ✓、复核三步 ✓）。**真正过期的只有「字段从哪来」那两行**：`起止区间` 与 `创建日` 还写着「落导入日」✗ —— 改成应用现在的行为（**最早一条活动的日期**），并补了一段：为什么不用导入日、精度是「第一次动手」那天、`start` 与创建日取同一个日期，以及**别去发明一个创建日字段**（这套写法里没有能装它的位置，值由应用算）。版本 2.0.0 → **2.1.0**
- [x] **导入的任务：创建日与起止改从「最早一条活动」取**（`data/markdown.ts` 解析收尾 + e2e + `README.md` / `tools/README.md` / `DESIGN.md`）：你问「导入 md 里 todo 任务的创建时间如何获取」，查下来是**写死的**（`created: today` / `start: today`）—— 迁进来的任务全变成「创建于导入那天」，而**创建日是这次迁移里唯一被整个抹平的时间维度**（截止 / 进度 / 状态都能从活动文本里挖到）。你问「能不能根据任务的第一条活动提取」，答：能，而且**不用动方言** —— 活动行的时间本来就在导入的 md 里，所以落在**应用端**算（工具端算完也没地方放：方言里没有表达「这是系统记录」的语法，塞成活动行会变成「人写的一条进展」）。实现就地复用了解析收尾那次遍历（它已经在补「导入任务」活动并把活动排成「新的在前」）—— **排序后最后一条就是最早那条**，不用另扫：`created` 与 `start` 都落成它，`end` 仍取截止。手写的行不受影响（没有活动行 → 刚补的「导入任务」就是今天）。**精度说清了**：给的是「第一次动手」那天，不是真正的创建时刻（旧版「创建任务」记录按上一条规矩不转移），对「建了长期搁着」的任务偏晚；要真实时刻得给写法加字段。**顺带修一条被撞假的断言**：「导入的活动行进了时间线，且按新的在前排」原来拿**整张抽屉**的文字比 `indexOf` —— 抽屉头部多了「创建于 08-25」之后它假红了（那个 08-25 比时间线里的 09-09 还靠前）；改成只读 `#task-drawer .tl-entry`（断言的本来意图），顺带那条的输出也更好读了
- [x] **核「系统记录是否会被搬过来」＋修 DESIGN §7.1 两处**（`DESIGN.md`）：你说「「延后处理: 截止时间」这种不是实际的活动信息，无需转移」—— 核了一遍，**默认行为正好是这个**（不是照代码推的，实跑证据）：`node tools/migrate-activity.mjs tools/sample-input.md --apply` → 对账 `源 11 = 输出 4 + 系统记录过滤 7`，生成的 md 里只剩 4 条人写的进展，报告另列「滤掉的系统记录」7 条（含 `延后处理: 截止时间 …`）。规则七条、行首锚定、默认全滤，`--keep-system` 才留（默认不开）。**但有一处必须说清**：那行是截止日期的**唯一来源**（输出里的 `⏰02-20` 就是从 `延后处理: 截止时间 A -> B` 取箭头右边的 B）—— 所以顺序是**先挖字段、再滤行**，不是一开始就丢掉。顺带修了 §7.1 两处：① 「v1.x 任务页「批量」导入」是**过期说法**（数据迁入早已移到**任务管理页**，上一轮改的，设计文档没跟上）；② 补上「先挖字段、再滤行」这条顺序不变量
- [x] **活动分析「标签筛选」的 全选/清空：先实测，再把空操作置灰**（`pages/activity.ts` + e2e + 截图重拍 + `DESIGN.md` §4.4）：你的反馈是「清空好像没用，全选状态再次点击就取消全选了、和清空的功能一致」。**先实测再下结论**（一次性探针，看完即删）—— 依次点：初始 → 全选 → 再全选 → 清空 → 全选 → 范围切「今天」，每步读「已选 N / 列表几个 / 盒子勾几个 / 报告几条」，结果：`6/6/6/20 → 6/6/6/20 → 6/6/6/20 → 0/6/0/0 → 6/6/6/20 → 3/3/3/8`。**两件事各归各位**：① 你观察到的「再点一次取消全选」**不存在** —— 那个按钮只 `add`、从不 `delete`（代码里就一行 `for (const item of activeTags) selection.add(item.tag)`）；② 但你说它「没用」是**对的，而且原因比表面更彻底**：默认勾选集就等于「当前范围内有活动的标签」（`checkedTags()` 的默认值），也就是左侧列表里那几个 —— 于是「全选」在默认状态下**永远是空操作**，只有在你手动取消过几个之后才重新有意义。**修法**：两个按钮都按「此刻有没有活儿干」置灰（都勾上时 全选 置灰、清空 可用；一个没勾时反过来；`activeTags` 为空时两个都灰）。**没有做成开关**（你话里那种「再点一次取消」的心智）：名字写着「全选」、点下去却把已勾好的全清掉，是比「点了没反应」更坏的一种意外 —— 要清空，旁边那个按钮的名字正好就是「清空」。**e2e 跟着改**（这里有个必须改的理由：原来那句「点全选」现在会点到**置灰按钮**上 → Playwright 直接超时）：换成「都勾上时全选置灰、清空可用」+「先取消一个 → 置灰解除 → 点它必须全勾回来（5/6 → 6/6）」。原来那种「在默认状态点一下全选」的写法**什么都没验到** —— 它点的是一个空操作，这也正是这个问题能长期存在的原因。**同一天稍后你定了另一个方向，已按它改完**：只留**一枚**「全选」开关（默认全选 → 用户可以逐个调整 → **再点一次 = 取消全选**），**「清空」撤掉**（`pages/activity.ts` + e2e 重写 + 截图重拍 + `DESIGN.md` §4.4）。我原先的判断是「不做成开关」（理由是名字写着全选、点下去却可能清掉已勾好的），你选了开关 —— 采纳它的根据更硬：**开关两边都有活儿干**，那个「点了没反应」的空操作从根上消失了；而「名字与效果暂时不一致」这一处，用**状态色**补上（全勾着时按钮带 `.on` 亮着，那一亮就是「现在是全选状态」的说明，不必再靠文字标签），悬停提示也跟着写「取消全选（清掉全部勾选）」。代价记录在文档里：清掉**部分**勾选要点两下（先全选、再取消全选）。只有列表里一个标签都没有时才置灰 —— 那时两边都没事可做。e2e 照开关语义验三段：`默认 6/6(on) → 取消 0(报告 0) → 全选 6/6(on) · 清空按钮 0 个`（最后那个「报告 0」是有意的：取消全选后报告得**真的空**，不是「看着没勾、其实还在筛」）
- [x] **「进行中」改成合并口径：待办 + 进行中**（`pages/shared.ts` `pages/overview.ts` `pages/tasks.ts` `pages/manage.ts` `pages/graph.ts` + e2e + 截图重拍 + `DESIGN.md` §4.1/§8.1）：你报「总览进行中 2，其中 1 个今日更新，点进去显示的 2 个是历史的、今天那条没出来，任务管理里也是」，并自己给出了诊断 —— 「只要按『进行中』状态统计了，其实待办也是一种进行中」。**你是对的，而且还有第二处**：① 三处都只数 `status === "doing"`（总览的 `counts.doing`、任务页 `visibleTasks`、管理页 `tableRows`），于是今天刚动过、但还没点「开始」的那条（仍是待办）既不进卡、也不进列表；② 更隐蔽的是副标那句「**其中** N 个今日更新」—— 它数的是 `activeTasks()` **全部未归档任务**，而卡上那个数只数 doing，「其中」是**假的**（那个 1 很可能是某条逾期的，点进去当然找不到，于是看起来就像「今天更新过的那条凭空消失了」）。**做法**：判据抽成 shared.ts 的 **`isOngoing(task)`**（待办 + 进行中）与 **`matchesStatus(task, filter)`**（除「进行中」外都是一对一），**四个消费方共用一个函数** —— 总览那张卡、任务页那排 chip、管理页的状态筛选，以及**图谱页**那个「进行中」过滤（它原本也只认 doing，是同一件事的第四处副本，一起收口）。**逾期故意不并进来**：它有自己的一张卡，两张卡数同一批等于把一件事说两遍，还会把「最早逾期」这个更急的信息挤掉 —— 于是三张状态卡把未归档任务**干净切开**：逾期 / 进行中（待办+进行中）/ 已完成。副标改成只数这一批。**e2e 两条不变量**：「其中 N 个今日更新」的 N ≤ 卡上的数字（子集不会比全集大，这一条直接抓住那个假「其中」）；任务页 chip「进行中 ≥ 待办」（钉住合并口径，退回只数 doing 时演示数据下是 17 < 23，必红）。实跑：`卡上 40 · 副标「其中 3 个今日更新」`、`进行中 40 · 待办 23`（40 = 23 + 17 ✓）、`总览「进行中」= 共 40 条`。**顺手修掉两条偶红用例的根子**：「多标签」那两条在填完搜索词后睡固定 350ms 再断言，而「建任务 → 数据广播 → 重画」是异步的，睡短了就落在重画之前，报出来的是「(没有标签)」—— 改成 `waitForSelector` 等这一行出现（等不到仍由那两条 check 报，失败信息不变）。这两条曾经被当成 flaky 放过两次，这次跑第一遍也红了、第二遍绿
- [x] **修 bug：导出不弹「另存为」，直接写文件**（`shell/download.ts` `shell/exportMenu.ts` + `src-tauri/src/lib.rs` + `capabilities/default.json` + `DESIGN.md` §4.9）：你说「活动分析 → 活动导出，没有让用户指定路径，直接写文件了」。属实，而且**两个页面都这样**（活动分析、任务管理都走同一个 `exportButton`）：以前一律 `<a download>`，桌面端它不问一句，直接落进 WebView 的默认下载目录 —— 导出成功之后还得去找文件在哪。现在桌面端先弹**系统「另存为」**（`plugin-dialog` 的 `save()`，默认名就是那份文件名、类型只给当前这一种），用户选哪写到哪；浏览器（`vite preview` / e2e）没有宿主对话框，仍走 `<a download>`（e2e 的 `waitForEvent("download")` 就是验这条，没动）。**一个副作用差点让它白改**：fs 插件的作用域**默认是空的**（`tauri.conf.json` 里没有 `plugins.fs.scope`，`FsScope::default()` = 无允许路径），拿对话框给的路径直接 `writeFile` 会被 `path forbidden` 挡下 —— 挡掉的正是用户自己刚点的位置，而且**不改作用域的话只在真机上才暴露**（e2e 跑在 chromium 里，走不到 Tauri 那一支，永远测不出来）。所以加了一条 Rust 命令 `allow_save_path`：写之前把**这一条**路径放进作用域，只放这一条、不放大整个目录，进程退出即失效。没选在配置里开一片通配作用域（`$HOME/**`）—— 那样等于把「整个用户目录可写」永久挂在应用身上，而这里要的只是「这一次选中的那个文件」。权限只加了两项：`dialog:allow-save`、`fs:allow-write-file`（xlsx 是二进制，走 `writeFile`；md/txt 的 `fs:allow-write-text-file` 本来就有）。取消对话框时**一个字节都不写、也不弹提示**。tsc / `cargo check` / e2e 全绿
- [x] **重新打包（第二次，仍是 v1.0.0）**（`npm run tauri build`）：本轮装进去的是这四件事 —— 总览「今日到期」可点且数字对得上、优先级标签 `紧急(P0)`、行首的进度饼、标签筛选那枚「全选」开关（e2e 全绿之后才打包）。产物在 `desktop/src-tauri/target/release/bundle/`：NSIS `nsis/Tadado2_1.0.0_x64-setup.exe`（2.59 MB）、MSI `msi/Tadado2_1.0.0_x64_en-US.msi`（3.62 MB）。**两个坑都按上次的记录处理了**：① **便携版不在 bundler 目标里**（`bundle.targets = "all"` 在 Windows 上只出 NSIS + MSI），所以 `bundle/portable/Tadado2.exe` 仍要**手工**从 `target/release/desktop.exe` 复制改名 —— 已刷新（7.67 MB，17:05），每次打包都得做，否则它名字不变、内容还停在旧二进制，看不出来；② **验证不是「能打开就行」**：读版本资源 `ProductName=Tadado2` / `FileVersion=ProductVersion=1.0.0`（证明 Rust 二进制真重编译了，不是复用了旧的），并到 `desktop/dist/assets/*.js` 里搜本轮的新文案「取消全选（清掉全部勾选）」—— 命中才说明打进去的是**最新前端**，不是上一次的 dist。**版本号没动**：我一度以为「内容变了就该升号」而把四处（tauri.conf.json / Cargo.toml / package.json / `settings.ts` 的兜底 `v1.0.0`）提到 1.0.1，**你叫停了** —— 「不要升版本，除非我明确告知」，已全部回退并记为准则：同版本号重打是默认，升号等你点头
- [x] **行首的「状态圆点」换成「进度饼」**（`pages/tasks.ts` `styles/pages.css` + e2e + 截图重拍 + `DESIGN.md` §4.2）：你说任务前面那个圆点「用来表示进度不容易理解」，并给了参考图（▶ / 扇形 / ✓ 一组圆饼）。**先纠正一处误会再动手**：那个圆点在代码里是**状态**（`dot.style.background = statusVar(task.status)`，逾期红 / 待办蓝 / 进行中橙 / 已完成绿），进度在**色条的填充**里（`fill.style.width = ${task.progress}%`），列表上确实一个进度数字都没有 —— 所以「不好理解」的根子是**一个裸色点只有颜色一个通道**，挂在任务名前面谁都会当成进度。**第一版做的是「行末百分比文字徽标」**（保留圆点），你的反馈是「效果不好」，改成了参考图那一套：**形状说进度、颜色说状态** —— CSS `conic-gradient` 画圆环 + 扇形（百分比由 TS 写进 `--pct` 一个变量，样式表里不出现 JS 拼的样式串），颜色仍走 `statusVar()`（于是它顺带把状态也接了过去），两端加两个更好认的记号：**待办 + 0% → ▶**、**100% → ✓**（实心圆 + 白勾；勾**必须固定白色**——跟着 `currentColor` 走会和底同色等于没画）。**零碎但都得做对**：① 圆点 8px → 饼 16px，任务列宽度 248 → **256**（`.tt-label` 与 `LABEL_W` 两个地方必须同步，差 8px 表头那条「今天」竖线就偏）；② `data-pct` / `data-mark` 把「画出来的值」在 DOM 上留一份可读的 —— 进度变成图形之后，读屏、调试、以及断言都得有个据；③ 我第一版加的 `.pct` 文字徽标与它的 CSS 一并撤掉（换成饼之后它就是多余的）。**e2e 新增一条**：每枚饼都带 0–100 的真实百分比、**满格的必须是 ✓**（那是「已做完」的意思，不是装饰），且首行那个数与悬停提示里那句「进度 N%」**是同一个**（两处各算一遍的话，改了其中一处就会出现「提示说 45%、饼说 60%」）。实跑：`首行 0% pie · 提示 0% · 共 20 枚`；管理页那条进度列不动（它是「条 + 数字」，本来就看得见数值）
- [x] **优先级标签改成「名字(Px)」**（`pages/shared.ts` `pages/overview.ts` `pages/tasks.ts` `styles/pages.css` + e2e + 截图重拍 + `DESIGN.md` §4.1/§8.1）：你要求总览的「紧急 / 重要 / …」带上编号，写成 `紧急(P0)`。**先看标签在几处出现**（5 处消费方，两处不能碰）：① `URGENCY_LABEL` 本身**不能改** —— 它会被拼进**活动记录的文本**（`taskForm` 写 `紧急 → 关注`、`taskDrawer` 读回来显示），那句话是**写进历史的数据**，读起来该像人话、不该塞编号；② 带彩色徽标的地方也不能用带编号的名字 —— 徽标本身就是 `P0`，再写一遍成了 `P0 紧急(P0)`，而且 e2e 有一条「抽屉里的优先级与列表徽标一致」正盯着那个徽标。**做法**：加一个显示用的 `urgencyText(level)` → `紧急(P0)`，只改「只显示名字」的三处 —— 总览的优先级分布五行、总览那行的悬浮提示、任务页的优先级筛选下拉（`P0 紧急` → `紧急(P0)`，这样两处文案逐字相同，e2e 那条「点总览某一档进任务页」的断言顺带变成了跨页对措辞的校验）。**规矩写进注释与文档**：有徽标 → 徽标 + 名字；没有徽标 → 名字里带编号。**顺带两处**：③ `.ubar .un` 的宽度 30px 只够两个字，不加宽的话括号里的编号会被挤到色条上 → 改 56px（**一行的宽度**，不是两个字的宽度）+ `nowrap`；④ 遍历档位时原来写 `URGENCY_LABEL.map((_, level) => …)` 再 `as Urgency` 断言，改成 `URGENCY_LEVELS: Urgency[]`（从 `URGENCY_LABEL` 推出来，档位数仍只有一处定义）—— 断言那种写法在档位悄悄多一个时不会报错。e2e 新增一条锁格式：`紧急(P0) / 重要(P1) / 关注(P2) / 普通(P3)`
- [x] **修 bug：总览「今日到期」写着 1 条却点不动**（`pages/shared.ts` `pages/overview.ts` `pages/focus.ts` `pages/tasks.ts` + e2e + 截图重拍 + `DESIGN.md` §4.1/§4.2/§4.7/§6.2）：你报「显示 1 条（其中 0 项已排时刻）但点不进去」。**根因在 `tile()` 的签名**：`(label, value, delta, variant, onClick?)` —— 「可点 / 悬停提示 / 点击监听」三件事同源（只在给了 `onClick` 时才成立），而这张卡**只传了四个参数**，于是它不是「点了没反应」，而是**整张卡静默失效**：`clickable` 类没有、title 没有、监听也没有，页面上没有任何地方说得出为什么。**但只补 onClick 不够** —— 设计里那条规矩是「数字既要点得开、又要**对得上**」，而任务页**没有任何筛选能表达「今天到期」**（状态那排只有 逾期/待办/进行中/已完成）。所以做的是完整的一条路：① 判据抽成 `pages/shared.ts` 的 **`isDueToday(task)`**，总览的卡数它、任务页那条筛选筛它 —— **同一个函数**（两处各写一份就是给「卡片写 1、点进去 0 条」留门；`due !== null` 那一条尤其不能省，见 §4.1）；② `focus.ts` 加 `showTasksDueToday()`（`TasksRequest` 多一个 `dueToday` 维度）；③ 任务页加 `dueTodayOnly` 开关 + 工具行末尾一枚**不属于状态**的 chip「今日到期 N」（数字同样按 pool 算：chip 上写几条 = 点亮后表里几行）。**两种筛选互斥**（点它就把状态复位「全部」，点状态那排就关掉它）—— 与 focus.ts 里「一次跳转只表达一个意图」同一条理由；它开着时状态那排**一个都不高亮**（列表不是「全部」那批，两个 chip 同时亮着说不清）。**chip 不只是好看，它是唯一的出口**：没有它，点进来看到 1 条，没人知道为什么、也不知道怎么退回去。**顺手修掉一处同类隐患**：跨页请求原来只复位搜索词与页码，**档位没复位** —— 它是「进度透镜」（非「全部」时只列该范围里**动过**的任务），上次停在「本周」时从总览点「逾期 40」进来只会看到本周动过的那几条，数字没算错但看着就是对不上；现在请求一律把档位复位成「全部」并 **`seg.setValue`**（seg 是无状态控件，这个坑 9/17 刚踩过一次）。**e2e 的判据改了写法**（这是它当初放过第四张卡的原因）：不再写死「逾期 / 进行中 / 已完成」三张，改成**遍历界面上所有的 `.tile`** —— 先断言四张卡都带 `clickable`，再逐张点开比「筛出的总数」。实跑：`今日到期:可点 · 逾期:可点 · 进行中:可点 · 已完成:可点`；`总览「今日到期」的数字 = 点开后筛出的总数 — 1 vs 共 1 条`；并加一条「任务页有『今日到期』这枚筛选且已点亮」
- [x] **重新打包，并把「重装会不会丢用户数据」核清**（产物在 `desktop/src-tauri/target/release/bundle/`；`README.md` 新增「升级、重装与卸载」一节、把「导出即可带走」改准成「备份 = 拷那个文件」、并在迁移一节加一句「两代数据库同名但结构不同，别直接拷」；`DESIGN.md` §7.2）：你要求重新打包，并问「同路径或同一台机器上安装，能否识别到/保留用户数据」。**打包**：`npm run tauri build` → 前端 885ms + Rust release 2m53s + 2 个 bundle；产物 `nsis/Tadado2_1.0.0_x64-setup.exe`（2.58 MB）、`msi/Tadado2_1.0.0_x64_en-US.msi`（3.62 MB）。**发现一个坑**：`bundle.targets = "all"` 在 Windows 上只产出 NSIS + MSI 两个，**便携版 `bundle/portable/Tadado2.exe` 不在 bundler 的目标里** —— 它是把 `target/release/desktop.exe`（主程序二进制叫 `desktop`，产品名才叫 Tadado2）手工复制改名的，所以这次打包后它还是 9/17 的旧二进制，名字一样、看不出来；已刷新，并把「每次打包记得刷新它」写进 DESIGN。**数据那件事不靠惯例推，从生成的安装脚本里核**（`target/release/nsis/x64/installer.nsi` / `target/release/wix/x64/main.wxs`）：数据在 `%APPDATA%\com.tadado.app\tadado.data`（这台机器上实存：60 KB，今天 11:05 写过），**不在安装目录里** → ① NSIS 默认 `currentUser` 装到 `%LOCALAPPDATA%\Tadado2`，卸载时**只有勾「删除应用数据」且不是升级**（`UpdateMode <> 1`）才删数据，升级永远不碰；② MSI 装到 `%ProgramFiles%\Tadado2`、`MajorUpgrade AllowDowngrades="yes"`（自动顶掉旧版、也允许装旧版），卸载只删自己装的目录与快捷方式，全文不出现 `%APPDATA%\com.tadado.app`；③ MSI 还会回头找 NSIS 写的 `HKCU\Software\tadado\Tadado2`，于是先装 NSIS 再装 MSI 会落在同一目录；④ 便携版与安装版**共用同一份数据**（都按 `identifier` 定位）；⑤ 换机器数据不会自己过去，拷 `tadado.data` 一个文件即可。**这也解释了 `identifier 不能改`** —— 数据目录名就是它。**另外查到**：`runMigrations` 只跑 `from === current` 的步骤，所以旧程序读新库不报错也不改库，但**下一次保存会把不认识的新字段写没**（保存是全量覆盖）—— 降级可用，别随后继续录数据。**待你定**：版本号仍是 `1.0.0`，与 9/17 那两个产物同名同版本、内容不同，分发前建议抬到 `1.0.1`
- [x] **总览问候条恢复写分区名**（`pages/overview.ts` + e2e + 截图重拍）：上一轮把它去掉了（理由是「一件事只留一处」），你要求恢复，并明确「按当前分区显示」—— 也就是**动态**，不是写死。那一行本来就是「今天」的一句话总结（`2026-09-20 · 周日 · 演示空间 分区 · 99 个未归档任务`），少一项不成句；口径取 `activePartition().name`，切分区时这一页跟着 `dataChanged` 重画。e2e 加的断言特意在**空分区**上验（`学习 分区 · 0 个未归档任务`）—— 写死的名字过不了这一条
- [x] **分区显示改到 rail（页头那枚章撤掉）**（`index.html` `shell/partition.ts` `shell/nav.ts` `styles/shell.css` + e2e + 截图重拍 + `DESIGN.md` §4.10）：上一版把当前分区做成页头的「分区章」（圆角药丸 + 文件夹图标 + 名字），你的反馈是「有点丑，能不能在左侧菜单栏增加显示」—— 认同：那枚章夹在标题与操作按钮之间，确实像贴上去的。**改到 rail 底部**（名字就在切换器上，切换与显示合成一处）：图标下面一行**分区名**（10px，居中，超长截断、全文在 title 里）+ **口令状态**。**关键是尺寸**：rail 是 64px（不是我上一轮以为的 52px —— 那个 52 是弹层定位值），56px 的按钮一行放得下四五个汉字，「演示空间」完整显示。**锁挪了两次，两次都是尺寸问题**：① 先挂在图标角上做 13px 角标 —— 那个尺寸下认不出是锁，看着就是个方点；② 挪到名字旁边 → 名字被挤成「演示空…」（56px 的名字行加一把锁就超了）；③ 最后落在**图标那一行**（这行有富余，锁能画到 11px）。页头 `partitionChip()` 与 `.pchip` 样式整块删除，e2e 那条「五个页面都有分区章」改成「rail 入口写着当前分区名 + 页头残留 0 处」（顺手守着它别再回来）。总览问候条仍不写分区名（一件事只留一处）
- [x] **分区可见性改版：「不用悬浮就知道我在哪个区」**（`shell/partition.ts` `shell/nav.ts` `pages/overview.ts` `styles/shell.css` + e2e + 全部截图重拍 + `DESIGN.md` §4.10）：起因是你提「分区选中后不够醒目，要悬浮才能看到当前分区的标签」，并给了两条要求（① 选中效果含锁 ② 非分区界面有醒目位置看到分区）。**先拍现状**（一次性探针，看完即删）：rail 底部只有一个文件夹图标，名字确实只在 `title` 里；弹层里只有一块淡紫底色、没有勾选、没有条数、锁是 emoji。**方案的核心判断**：rail 只有 52px 宽，名字**塞不进去** —— 所以「不用悬浮就看得见」这件事必须落在**页头**，rail 那个按钮负责「切」而不是「显示」。三层改动：① 五个页面页头各一枚**分区章**（文件夹图标 + 分区名 + 口令状态；订阅 `onPartitionChange` 与 `onLockChange`，切分区/解锁/自动上锁都跟着变）；② 弹层里当前项加**勾选标记**（底色同时被 `:hover` 用着，只靠底色分不出「当前项」和「鼠标停在这一项」）、每项加**条数**（用的是 CSS 里早就写好却一直没人用的 `.cnt`）、锁从 emoji 换成内联 SVG 且分**开锁（已解锁）/ 闭锁（锁着）/ 不画（没设过口令）**三态；③ rail 按钮右上角挂**锁角标**。另外把弹层里那句手动 `classList.toggle("on")` 删了 —— 勾选标记是画的时候带上的，手动只切 class 会让勾留在原来那一项上。**刻意没做**：分区章不做成按钮（切换在 rail 底部，点了没去处比没有更糟）、rail 不放文字。总览问候条里的 `xx 分区` 一并去掉（页头已有，一屏两处只会让人多想一下）。**证据**：探针造了「演示空间有口令且已解锁 + 工作有口令且锁着」的状态拍图核对三处锁；e2e 加三条断言（五个页面都有同一枚章、切分区后跟着变、弹层当前项带勾选且每项带条数），实跑全过
- [x] **跨日续跑 + 时间轴「现在」钳边界（两条产品侧的时间病，按「不违背设计 + 容易」修掉）**（新增 `shell/rollover.ts`；`shell/hotkey.ts` `pages/overview.ts` `main.ts` + e2e + `DESIGN.md`）：上一轮末尾报的两条产品侧发现，你的口径是「不违背设计、实现相对容易的自动帮我同步优化」。**① 跨日续跑**：`data/time.ts` 的 `TODAY` 是**模块加载时算一次**的，而整个数据层的「今天」都从它派生（`dayNumber` 补哪一年、档位窗口、热力图未来格子、逾期判定、`nowStamp()` 写活动时刻）；这个应用又是**托盘常驻**的 ——「关窗口」只是收起常驻，**开着过夜是正常用法**。不处理的话过了午夜整个应用还停在昨天：逾期不重算、新建任务默认截止是昨天、新活动的时刻也记在昨天，而问候语与轴上的「现在」读真实时钟，页面会自相矛盾（问候语说「早上好」、日期还停在昨天）。**做法**：每分钟看一眼本地日历日，变了就 `location.reload()` —— 走的是**设计里已经有**的那条路（`main.ts` 开头就写着「托盘由 Rust 侧在进程启动时创建，与 webview 生命周期解耦，不会随重载重复叠加」，重载本来就在这套架构的预期之内）。三条约束：只在 Tauri 里跑（浏览器预览/e2e 没有常驻这回事，而且 e2e 跑到一半被重载更难查）；有**未提交的输入**时跳过（模态框、抽屉里那段还没点「按 md 更新任务」的 md —— 批量草稿走 kv 丢不了，那段 md 没有别的地方存），不更新 `seen`、下一分钟再试。**没有走「把 `TODAY` 改成取值函数」**：那要动数据层与二十来个调用点，且每个派生值都得各自订阅一次「跨日」，漏一个就回到同样的问题。**连带修一处**：`setupHotkey` 原来是裸 `register()`，而 Rust 侧那份注册**不随 webview 重载消失**，直接再注册会以「已注册」失败 —— 于是每次重载都弹一句**假的**「热键不可用」；改成幂等（`isRegistered` → 先摘再注册），跨日重载因此踩不到这个坑。**重载路径不是新的**：e2e 里本来就有 4 处 `page.reload()`（默认分区、旧数据迁移、存档损坏），「重载后一切正常」早被验着了。**② 时间轴「现在」钳边界**：轴只覆盖 06:00–24:00，而 `axisPct` 不钳制 —— 凌晨 1 点的「现在」是 **-27.8%**，标记画到轴的左边之外（压在别的区块上或被裁掉，看着像页面坏了）；测试之所以没红是因为它用的是同一个公式，两边一起错。钳到 0–100 后标记贴在左端，标题里仍是真实时刻。对气泡无影响（它们本来就在 `pct < 1 || pct > 99` 时转到轴下面那条）。e2e 的 `pctAt` 同步钳制；顺带把顶部那个 `const URL` 改名 `PREVIEW`（模块作用域的 const 会顶掉全局 `URL`，`tools/screenshots.mjs` 上已经栽过一次）。**未加自动化断言**：跨日那套测不了（e2e 动不了时钟），靠的是代码里的注释与 DESIGN §5.2
- [x] **e2e 里跟时间有关的断言全扫一遍（七处，其中两条是每天／每年必红）**（`e2e/smoke.mjs`）：上一轮修问候语时留了一句「这一类的隐患还有几处」，这次不靠记忆，先把 e2e 里所有跟时钟沾边的落点列出来**分类**：写死日期 / UTC 与本地混用 / 跨边界（装载与断言之间跨过整点、午夜、月度）。七处逐个改，并在文件顶部立了一组 **日期助手 + 三条规矩**（要「今天」就现算；跟本地日历日对齐就别用 `toISOString`；挨着边界的断言容许「上一步的值」——上一分钟 / 上一小时 / 前一天），规矩写在代码里比写在提交信息里管用。**两条真会红的**：① 「指定范围默认是今天」用的是 `new Date().toISOString().slice(0,10)` —— 那是 **UTC** 日期，本机偏移 +480（东八区），本地 09-20 01:00 时它给的是 09-19，于是这条**每天 00:00–08:00 必红**（用 `node -e` 算过，不是靠等明天）；② 「导出的活动时间都不晚于今天」拿 `"MM-DD"` 直接比字符串 —— 1 月里 12 月的活动会显得「比今天晚」（跨年倒挂），**整个 1 月假红**，而那是去年 12 月的、早就发生了。另外五处：热力图月份天数写死 `2026`（2027 年再跑就会拿 2026 比，闰年更糟）→ 按格子自己带的年份算；「点热力图上的一天」断言写死 `"2026-09-"` → 改成从**被点那格自己的 title** 取日期（这样它验的是「报告切到了我点的那天」，而不是「现在是 2026 年」）；「现在」标记跨午夜时整段跨过去（23:59 是 99.9%、00:00 是 -27.8%）→ 容许上一分钟；问候语跨整点（上一轮已修）；`todayIso` 那份内联实现并进 `localIso()`。**顺带两条产品侧的发现**（不在 e2e 里，但同一类病，单独记在下面那条）
- [x] **分区隔离审计：五个页面逐个核对，修掉活动分析的泄漏**（`pages/activity.ts` `pages/overview.ts` + e2e）：你问「五个页面现在是不是都受分区字段控制」。**不靠印象，把全仓 `TASKS`（全量、不分分区）的引用一条条列出来分类** —— 读的、写的、本来就该全量的（`byId` 按唯一 id 查、`settings` 按分区计数、启动装载）。结论：**4 页是好的，活动分析漏**。它有三处 `TASKS.flatMap`：`allActivities()`（整页唯一数据源：热力图 / 活动列表 / 报告全从它来）、`tagStatsNow()`（左侧标签列表）、`checkedTags()`（勾选集）—— 切到空分区时，任务页 / 总览 / 图谱都空了，这一页还在统计**别的分区**的活动，连带把分区口令那道屏风一起绕过去。有意思的是总览那页有一句注释专门写着「只看当前分区：切了分区还看见别处的动态，属于『显示不全』的另一面 —— 显示多余」，说明这个坑被意识到过一次，但只在那一页修了。**修法**：活动分析改用 `partitionTasks()` 而不是 `activeTasks()` —— 归档只是「列表里不再显示」，那些活动确实发生过，全年热力图与报告把它们排除掉，历史的账就对不上了。**顺带一半的坑**：`checked` 是模块级缓存，原来只在同一个分区里延续（那个分支只会**删**、不会补），于是切到一个标签不同的分区时一个都没勾上 → 看到的是空报告，像是那个分区没有活动；加 `checkedFor`，换分区重算。**回归断言**：原来那条空分区测试**只看任务页**，正是它把管理页与活动分析两处漏洞一起放过去了 —— 扩成**五页齐扫**（总览的 `.feed-item`/`.tdt-p`、任务的 `.tt-row`、图谱的节点数、管理页的 `.mrow`/`.tagrow`、活动分析的 `.act-item`/`.tagrow`），**各页量各页的内容元素**，不量共用的数字；再加一条「切回演示空间活动分析又有数据」。**顺带炸出第三颗时钟雷**：问候语断言在跨整点时会假红（10:59 装载的页面说「早上好」，测试 11:00 算出「中午好」）。查下去是**真问题**：总览那个「每分钟重画」只在 `currentPage() === "overview"` 时执行，**切回这一页的瞬间不重画**，于是问候语与「现在」标记最多陈旧 60 秒；按仓库惯例（`graph.ts` / `tasks.ts` 都是「切回前台重画一次」）补上 `subscribePages(render)`，断言改成容许「上一小时的那个问候」——它真正要抓的是**写死的问候语**（晚上八点说早上好）
- [x] **修 bug：切到空分区，管理页还列着别区的任务**（`data/mock.ts` `pages/manage.ts` + e2e）：你报的现象是「总览 / 任务 / 图谱都空了，任务管理里还有数据」。真因不是「没清空」——数据本来就是对的，是**视图取数**：`manage.ts` 直接读 `TASKS`。而 `activeTasks()` 把「只看本分区」和「不看归档」两重过滤**揉在一个函数里**，管理页当初为了放开归档而绕开它，结果把**分区过滤也一起放开了**。最讽刺的是 `mock.ts` 里 `activeTasks()` 的注释**自己预言了这个 bug**：「漏掉一处的后果是『切了分区，某个页面还显示别的分区的任务』」。影响面是管理页 6 处（表格 / 标签卡 / 批量处置 / 标签改名 / 删标签）；**顺带一处安全问题**：分区口令只是屏风，而管理页能看到别区的任务，等于把屏风整个绕过去。**修法**：新增 `partitionTasks()`（本分区**全部**任务，含已归档），`activeTasks()` 由它派生 —— 分区过滤从此只写一处，「要放开归档就只放开归档」。**第二层**：只改取数还不够 —— 标签卡在 `mount` 时算一次就再没重建过（管理页没有 `subscribePages`，而表格会随 `dataChanged` 重建），于是切分区后表格空了、标签卡还列着启动那个分区的标签；把 `refreshTagCard()` 挂进 `render()`，并让「选中的标签已不在这一批里」时清空选中。**回归断言**：原来那条空分区测试**只看任务页**（`after === 0`），正是它把漏洞放过去的 —— 新增一条盯管理页（`.mrow` 与 `.tagrow` 都得是 0）。**顺带炸出另一颗雷（与本次改动无关）**：e2e 里写死 `2026-09-18` 当「今天」，今天 9-20 → 它成了过去 → 任务按设计自动逾期 → md 把截止挪回未来 → 逾期按设计退回待办 → 那条「状态不被 md 改掉」跟着红。改成取真实今天；并把该断言的两个判据**拆开报**（合在一个 `&&` 里时失败看不出是哪半边 —— 这也是当时能快速定位的原因）
- [x] **旧版自动写下的系统记录，迁移时过滤掉**（`tools/migrate-activity.mjs` `tools/sample-input.md` `tools/README.md` + e2e）：按你的要求，py 清单里那批**不是人写的**记录不再带进来 —— 七条规则：`[批量创建] …` / `创建任务` / `延后处理: 截止时间 …` / `状态变更为 X` / `状态 X → Y` / `进度 N% → M%` / `截止时间 X`。三条理由：① 它们的**结果已经写在任务行上**（方括号 = 状态、`:: N%` = 进度、`⏰` = 截止），本身不携带新信息；② 这套写法里的活动行导入后一律是 `kind: "log"`（**人手写的一条进展**），把系统记录带进去等于给它挂上「人写的」这个身份，还会落进「可改可删」那一类；③ 一条任务动辄几十条状态 / 进度变更，真正有价值的进展会被淹掉。**踩住一个顺序陷阱**：截止与状态正是**从这些行里挖出来**的，所以只能**先挖字段、再滤行** —— 实测输出里 `⏰02-20` 仍在（它来自被滤掉的那行延后记录），字段一个没丢。对账依旧要平：`源 11 = 输出 4 + 系统记录过滤 7 + 重复剔除 0`，并在报告末尾**列出被滤掉的例子**（少掉的东西必须能看见，不让它静默消失）。想全留着加 `--keep-system`。样例补了 4 条人写进展（否则滤完就没东西可看），e2e 那条断言跟着改成「**留下的只有人写的进展**（示例任务三 2 条）」——它横跨两个标签组共 4 行活动，其中 2 行是系统记录
- [x] **迁移链收口：把「工具的输出能被应用直接导入」变成机器验收**（`tools/migrate-activity.mjs` `tools/README.md` `desktop/e2e/smoke.mjs` `.github/workflows/desktop.yml`）：你重申核心是「py 活动分析导出的 md → tau 能直接导入的 md」，于是把上一轮我列的目录支持之类**外围砍掉**，只做服务这条链的六件事。① **端到端验收**（最要紧的一条）：冒烟里真跑一次转换工具产出文件 → 用 `setInputFiles` 喂进「数据迁入」→ 断言「共 3 条 · 0 行认不出」+ 导入后 3 条都在 + 打开抽屉数出 3 条活动。此前这条链**只靠人眼比一眼**：工具用自己那套正则（读旧格式），应用用自己的解析器（`parseTasksDetailed`），两者只在「文件」上碰头，没有任何机械保证 —— 现在解析走的是应用自己那份，不是在工具里再写一份副本。② **标题保留字符检查**：这套写法**没有转义机制**，标题里的 `#xxx` 会被应用解析成标签并从标题里删掉（`⏰`+日期、`::`+数字同理），而且**不报错** —— 造了一份带 `#` 的输入实测：列出「标题里带保留字符的任务（1）」并给退出码 1。③ 输出默认名 `xxx.dialect.md` → `xxx-可导入.md`（「方言」是被废弃的词，且这名字**会显示在导入对话框里**、还是你切分区时的对照物），函数名 `toDialect` → `toTaskLines`。④ **不覆盖已有输出**（除非 `--force`）：工具报「标签超过 3 个」但按原则不替用户删，所以用户多半会手改那份文件，重跑 `--apply` 会把手改的**静默抹掉**。⑤ 生成后打印**绝对路径**与**下一步**（切分区 → 任务管理 → 数据迁入 → 选它）。⑥ CI 的 `paths` 加 `tools/**` —— 那条端到端横跨两个目录，只监听 `desktop/**` 会漏。文档侧：`tools/README.md` 新增「**谁负责验证**」两层表（工具管源侧对账 / 应用管解析侧），并补上 `--force` 与保留字符那条
- [x] **「数据迁入」收敛成纯文件，这一屏压到三行**（`pages/tasks.ts` `styles/controls.css` `styles/pages.css` `data/markdown.ts` + e2e + 截图；**删除** `shell/draft.ts`）：你问「为什么还留着粘贴框」——那是我上一轮刻意留的（理由：「一次录十条」会没入口）。这个理由站不住：**你根本不用它**（连功能存在都忘了），而留着它会让这一屏同时服务「迁移」和「随手录十条」，名字重新变模糊。于是删掉粘贴框，对话框只吃文件。**连带删掉整个草稿模块**（`shell/draft.ts` + `.draft-bar` CSS + 两条 e2e 断言）—— 它唯一的消费者就是这个框，没有打字就没有草稿（按仓库惯例删死代码：之前删过 `tasksToMarkdown`、`registry.blocks`）。**随后你又指出这一屏还是啰嗦**（截图），再砍掉三行小字：一句话说明、代码样例、`支持 .md / .txt` —— 文件是转换工具产出的，用户不需要在导入这一屏再看一遍写法（语法写在 `tools/README.md`）。现在**只有三样**：`导入到「演示空间」` / `[选择文件…] 文件名` / `● 共 N 条`。**交互**：`<input type="file">` + `File.text()`（不用插件、不加权限，浏览器也能跑）；没选文件时导入按钮禁用并显示「还没选择文件」；选完立刻解析；**导入后给回执** —— `已导入 N 条到「演示空间」 · M 行未能识别`（不做回执的话，「好像导了」和「其实一条没进」在界面上长得一样）。e2e 五处从「填文本」改成写临时 md + `setInputFiles`（`display:none` 的 input 也能这么选），新增「没选文件时导不了、重开也不记着上次的文件」；顺带踩到一个 Playwright 严格模式冲突：`跳过重复，导入 0 条` 也含「导入」二字（它只是被隐藏、仍在 DOM 里），改用 `:text-is("导入")` 精确匹配
- [x] **「批量新建」→「数据迁入」，入口移到任务管理页**（`pages/tasks.ts` `pages/manage.ts` `pages/registry.ts` `styles/controls.css` + e2e + 全部截图重拍）：你一度想直接删掉它（「我怎么记得没有这功能」），讨论后改成**不删、而是改造成迁移通道**——因为它是**唯一**能「建任务 + 同时把活动时间线建出来」的写入路径（单条新建那条路 `TASKS.unshift({... activities: [创建任务]})` 会把活动整个覆盖掉），删了旧数据就搬不过来。**入口**：任务页页头 → **任务管理页**，与「导出 ▾」并排，改名「数据迁入」（一进一出，那页的主语本来就是「一批一批地处置」；页头的位置留给每天都用的「＋ 新建任务」）。**新增能力**：可选 md 文件（`<input type="file">` + `File.text()`，不用 plugin-dialog / plugin-fs、不加权限，浏览器里也能跑、e2e 能用 `setInputFiles` 真测）；读出来填进同一个框，解析 / 对账 / 草稿 / `Ctrl+Enter` 全部复用。**重复只提示、不替用户决定**：判重规则只有一条 —— 标题在**当前分区**里已存在；同名的不静默跳过，连同标签与截止一起列出来（只给标题的话用户没法判断是不是同一条），并给「跳过重复，导入 K 条」与「全部导入 N 条」两个按钮，默认动作是跳过（造出重复数据的清理成本比少导几条高）；整批都是重复时不显示跳过按钮（点了什么也不做）。**只告知当前分区**（`将导入到当前分区：「个人」`），不猜也不拦——旧版是按分区导出的，一个文件对应一个目标分区。**明确不做「解析失败自动跳过」**：那份清单里 82% 是活动行（见下方「方言支持活动行」那条的实测），静默跳过等于把历史全丢而界面上只显示「导入 N 条」——认不出的行仍然逐条列出来。**顺带修的两处**：① `.card-h` 没有 `flex-wrap`，右侧控件一多就把标题压成两半（「任务表格」竖着断成两截）——加按钮之后才显形，改成换行并让标题/说明不参与压缩（复查了活动分析那条「导出置最右」的几何断言）；② `DESIGN.md` §4.5 我**上一轮声称修了「延后 / 中止」却实际没改**（这次真改了），顺带把同一处那句过期的「**没有导入**」更新成「数据迁入与导出并排」
- [x] **「批量新建」改名 + 挪到页头（不改功能，改它被认出的方式）**（`pages/registry.ts` `shell/nav.ts` `pages/index.ts` `pages/tasks.ts` `data/markdown.ts` `shell/settings.ts` `styles/controls.css` + e2e + 全部截图重拍）：起因是你问「这个批量操作对吗？我怎么记得没有这批量添加的功能」。**功能一直都在**（`tasks.ts` 的 `openBatchCreate`，e2e 三条断言守着它），问题出在**名字与位置**：按钮只写「批量」两个字，夹在工具行的搜索与筛选之间 —— ① 不是动词，读不出「新建」；② 又和任务管理页真正的「批量操作」（标记完成 / 归档 / 取消归档 / 删除）撞词。**契约改动**：`PageSpec.action?: string`（一页一个主按钮）放不下第二个新建入口，改成 `actions?: PageAction[]`（`id` / `label` / `kind` / `title`），`onAction()` 相应带 `id`；任务页现在是 `[批量新建任务] [＋ 新建任务]` 并排（次要 vs 主要），工具行只剩查看与筛选。**交互优化**：`markdown.ts` 拆出 `parseTasksDetailed`，解析结果除了草稿还带 `skipped`（行号 + 原文，空行不算）—— 弹窗把**认不出的行逐条列出来**，以前它只体现为「条数少了一个」，粘 30 行的人看不出少的是哪几行、为什么少（这与迁移工具那套「先对账」是同一条规矩：少掉的东西必须能看见）；条数改用强调色（点创建前唯一的确认信息，不该是灰小字）、底部补「Ctrl + Enter 创建」、弹窗改宽版、粘贴框 138px 且可纵向拉伸（原来只露一行却写着「将创建 3 条」，自相矛盾）。e2e 跟着改：`.ph button` 现在有两个，得指到具体那个；新增一条「认不出的行逐条列出来」
- [x] **README 重写（九轮 grilling 定稿）+ 补上「留痕」这笔欠账**（`README.md` 全量重写；`data/types.ts` `data/schema.ts` `pages/taskForm.ts` `pages/taskDrawer.ts` `shell/settings.ts` `styles/pages.css` + e2e；新增 `tools/screenshots.mjs` 与 `resources/screenshots/` 9 张图）：起因是「README 写得很低端、没特色」。先问清九件事再动手 —— ① 第一读者是**评估者**、读完的动作是**下载**（技术细节退到文档链接）；② 视觉证据用**真实截图**（对着一份真实构建跑 Playwright，视口就是应用窗口的 1180×760，画面里的数据就是演示空间那 100 条样例，不另造数据）；③ 信任靠一条**可自证**的硬声明「安装包的权限清单里没有授予任何网络权限」（不是「承诺不联网」，是没给这个能力，读者能自己去 `capabilities/default.json` 核），SmartScreen 那段从「免责」改成「说明」（未签名 = 没买证书，与内容无关）；④ 加一段「**它不是**」（不是同步服务 / 不是团队工具 / 没有手机端 / 只有 Windows 构建），替读者省时间，也让 README 更短；⑤ tagline 定为「**任务是一行字，过程是一条线。**」；⑥ 首次体验明说「100 条样例在演示空间 + 清空路径」。**grilling 中途挖出一个真问题**：「关于」面板里写着「改状态留痕：完成、**改进度、改优先级**都会写进活动时间线」，但代码里只有**状态**留痕（`setTaskStatus` 是全应用唯一那条路径，注释还专门写了为什么）—— 进度（`taskForm.ts` 直接 `task.progress = next`）与优先级（直接 `task.urgency = level`）都不写记录。更说明问题的是 `progress` 这个活动类型**早就设计好了**（`types.ts` 的 `kind: "progress" {from,to}`、种子数据里的「进度 50% → 70%」、`schema.ts` 的解析、时间线的渲染全都现成），**只差接线**。于是选择补功能而不是删文案：优先级新开 `kind: "urgency"`（不复用 `log` —— 按 `types.ts` 的不变量，只有 `log` 可改可删，自动记录塞进去就等于允许用户篡改事实），进度用现成的 `progress`。**一个坑**：滑杆拖动时 `input` 连发、只有 `change` 才算提交，`from` 必须在**开始拖之前**抓一次，否则时间线会刷出「50→51、51→52……」一串噪声 —— e2e 因此断言的是「恰好一条」而不是「有一条」。顺带修掉另三处与代码不符：`DESIGN.md` §4.5 把批量操作写成「改状态 / **延后** / **中止** / 删除」（实际是标记完成 / 归档 / 取消归档 / 删除，延后与中止在桌面端根本不存在，是从 v0.x 特性清单抄来的）、根 README 的「…改优先级、**延期**都留痕」、`CONTRIBUTING.md` 的「桌面端目前只有 `tsc` 这一道自动检查」（`npm run e2e` 早就在了，CI 也在跑）。截图脚本同时拼出仓库的社交预览图（1280×640）
- [x] **设计文档按版本线拆开：两条线各自维护自己那份**（重写根 `DESIGN.md` 为 v1.x 的设计；Py 版那份随代码留在 `archive/pyversion` 分支；改根 README 与 `desktop/README.md` 的引用）：原来 1326 行的 `DESIGN.md` 全是 Py 版内容，而 `desktop/README.md` 里那句「窗口形态、导航骨架 | DESIGN.md 2.12 / 1.3.1」会让人以为它同时是 Tauri 版的权威设计 —— 实际读起来得在脑子里做减法（哪些段落还成立、哪些描述的是已归档的 Qt 实现）。按「**两个分支独立，各自路径下维护自己的文档**」重做：`main` 的 `DESIGN.md` 就是 **v1.x（Tadado2）** 的设计，Py 版的设计文档不在这边占位置。**动手前先核对了归档分支**：`git diff archive/pyversion -- DESIGN.md` 只有 18 行差异（就是我加的那段头部），说明那边是**完整副本**、main 这份可以放手 —— 差点在没确认的情况下覆盖掉唯一一份。中途曾把新文档放进 `desktop/DESIGN.md`，被否掉改回：`desktop/` 只放应用本身，文档与 README 同级。新文档 8 节：总览（技术栈与「无框架无运行时依赖」的理由 / 分层与依赖方向 / **启动装配顺序逐条讲为什么是这个位置** / 与 Py 版的关系含「identifier 不能改」）、视觉与样式（两个权威源、五层依赖方向、**一屏到底**含实测踩出的四个坑、窗口形态参数表）、数据层（存储形态与「为什么只立六个列」、版本与迁移含那条 `-wal` 误判教训、读入口归位、**故障不降级**的 backend 状态机、kv key 清单、演示数据与分区为何收进演示空间）、功能模块（五页 + 抽屉 + 分页与跨页定位 + **Markdown 写法含状态那处取舍并注明本文档曾写反过** + 导出排版为何不留空格/不加 BOM + 分区口令 + 设置一页到底）、外壳、构建与验证（e2e 的分组与判据写法、**为什么必须有 e2e**）、迁移与交付、附录（关键常量 / 依赖清单 / **已撤设计清单**）。文风沿用仓库习惯：每个决定写「为什么」，不罗列现状
- [x] **旧数据迁移工具收进仓库**（新增仓库根 `tools/`：`migrate-activity.mjs` + `README.md` + `sample-input.md`）：把 v0.x「活动分析」导出的清单转成 Tadado2 批量新建吃得下的 Markdown 方言，**活动时间线一条不丢**（不带它，导进来的任务只剩标题）。这个脚本原先只在本机的 `~/.claude/skills/` 下 —— 只有作者本人和那个特定 agent 环境能用，README 里「改写成方言」也就给不出一条能照做的命令。转换逻辑**逐字照搬**已验证过的版本，包括它踩过的那个坑：`截止时间 A -> B` 与 `截止时间 A` 两条正则必须互斥，少了负向断言就会把箭头**左边**的旧日期当成结果，每条延后记录都把截止覆盖回旧的（端到端核对时才看出来：due 写成 08-23，正确答案是 09-22）。收编时补了一处口径不一致：`merged`（同名任务跨标签组合并）写在「必须停下来问用户」里，却没计入 `pending` —— 报告里列着「请确认」而退出码给 0，照退出码走的调用方会当成干净通过。`sample-input.md` 是一份可跑样例（3 组 / 4 任务行 → 3 条 / 8 条活动），`node tools/migrate-activity.mjs tools/sample-input.md` 即可看到对账格式。**位置**：最初落在 `desktop/tools/`、并加了 `migrate:activity` 这个 npm script，后被要求移到**仓库根**（与 README 同级）—— 它服务的是整个仓库（把 v0.x 的数据搬过来），不属于 `desktop/` 这个应用；npm script 随之去掉，改直接 `node` 跑（脚本零依赖，不需要 `npm install`）
- [x] **一屏到底：主区不再滚，五页各自撑满**（`styles/shell.css` `styles/pages.css` + `pages/overview.ts` `tasks.ts` `activity.ts` `manage.ts`）：原来 `.main` 是 `overflow:auto`、`.page` 由内容撑高，看全一页就得整页滚（页头 / 卡片头 / 分页器全被滚走）。现在高度从 `.main` 一路 flex 传到列表：固定的块（指标卡、热力图、工具行）留在原地，会长的那块在卡片内部吃掉剩余高度并滚。**四个坑**：① grid 的行高默认由**内容**决定 —— 列表装了 20 行就把行高顶到 700px，容器被压扁后行高照旧（总览实测溢出 186px），要 `grid-template-rows: minmax(0,1fr)` 把行高钉在容器上；② 总览那几块外面还包着一层无名 div，块级容器不做 flex 分配 —— flex 链断在那里，`flex:1` 全落空，得给它一个 `.ov-page`；③ 卡片等高之后，时间轴上的绝对定位气泡多出几像素就会盖住下面那张卡，**被盖的按钮点不到**（e2e 卡在那一步重试到超时），撑满场景的 `.card-b` 要 `overflow:hidden`；④ 每一层都得给 `min-height:0`，漏一层这条链就断在那一层。顺带把任务页的 `max-height: calc(100vh - 268px)`（拿一串写死的层高估可视高度）换成同一条 flex 链。总览下半页分两列（左：时间轴 + 优先级分布，右：活动流），近期活动列表从 100px → 404px；`.tdt` 基准高度 172 → 150，与 `overview.ts` 的 `AXIS_H` 共用一个常量（多开的气泡道在这个基准上往外加）
- [x] **界面小字提示全量审计**（`registry.ts` / 各页面 / `shell/lock.ts`）：逐条核对「是否正式 + 是否与当前功能一致」，**与功能不符的直接删**。查出的真问题：批量新建的说明里还写着 `+1w`（循环字段早已删除，见 `data/markdown.ts` 顶部）—— 留着就是让人照一个不存在的语法写；图谱详情的「根节点」是图论术语（对用户无信息量，改「分区」，与图例一致）；图谱筛选「紧急与重要」改成**「高优先级」**（全应用讨论的都是优先级 P0–P3，这里单用一组形容词会让人以为不是同一个字段）。正式化的一批：空态「这一档里没有任务 —— 换个档位、筛选或清空搜索」→「区间内没有任务，可切换档位、调整筛选或清空搜索」、抽屉「还没有记录 —— 在下面写第一条进展」→「暂无记录，可在下方添加第一条进展」、「记一条进展…」→「记录一条进展」、「认不出任务行。写法：」→「无法识别任务行，格式示例：」、锁屏「口令不对」→「口令错误」、标签管理卡头与编辑区两处重复的操作说明各留一处并改书面语、总览页头说明补上漏掉的「优先级分布」。tooltip 与分区注解（「这条任务是什么 / 经历过什么」）本来就在说当前功能，保留
- [x] **活动报告：条数挪到标签切换组外面**（`pages/activity.ts`）：那个「N 条活动」是**当前标签**在这个范围里的条数，挂在「活动报告」标题旁边会被读成「整份报告共几条」—— 而报告是按标签翻页的，两个数不是一回事。现在排成 `活动报告 … ◀ #健康 ▶ 1 条活动 [搜索]`：放在 ◀ #标签 ▶ **这组的外面**（紧跟 ▶ 之后），不插进组中间 —— 插进去会把标签切换那一组拆散。e2e 锁三点：在组外、不在标签名旁、不在标题旁
- [x] **分区口令的设 / 改 / 清合成一个入口**（`shell/prompt.ts` 新增 `promptWithExtra` + `shell/settings.ts`）：按钮文字恒为「设口令」，设没设用**点亮**（`.btn.sm.on`）表示。以前按有无口令在「设口令」与「改口令 + 清口令」之间换按钮，用户每次都得先判断自己处在哪种状态，而答案只有他自己知道。点开是同一个弹窗：填新口令 = 换一个，留着不填 = 不改，`清除`（danger，靠左）则不再要口令 —— 清口令不再二次确认（弹窗里那个按钮就是明确动作，口令随时能重设，没什么会因此丢掉）。`promptText` 保持返回 `string | null`，新增的 `promptWithExtra` 才带 `extra` 标记
- [x] 口令浮层里「这是防路过的人瞄一眼，不是加密存储……」那段小字撤掉：同理的话 `lock.ts` 顶部已经写了一遍，在弹窗里再来一遍只是逼人多读一段。清口令的确认框也去掉了「之后进入这个分区不再需要口令」—— 标题已经说清了
> 桌面版的「剩余缺口」去取舍 —— 一份对着不存在的应用列的欠账，只会让人反复
> 去核一些已经落地的功能。

## 🧩 其他

- 真机（有显示器）GUI 冒烟 —— 从清单里撤了：它需要一台带显示器的人和几分钟手点，放在自动化够不着的清单里只会一直挂着。发布前自行过一遍：侧栏 5 页切换、置顶按钮、设置齿轮、托盘、主题
- [x] **UI 层 repository 直连归零**：只读部件（HeatmapModel / TaskTreePanel / CalendarHeatmapWidget / TagManagementPanel）与写路径（TaskDialog / SettingsDialog）全部改依赖 `TaskService`；透传宿主（BatchController / TaskListView）移除 `repository` 参数；删除孤儿 TaskEditPanel / StatusStatsBar / ActivityReportPanel；`MainWindow` 成为唯一持有 repository 的位置（**76 处 → 0 处**，`docs/architecture/repository-calls-analysis.md` 已重写）
- [x] **`#5 Service 去 Qt 耦合`**：4 个后台服务（`TaskScheduler` / `TaskArchiver` / `TaskRecurrence` / `TaskNotifier`）此前构造时硬编码 `QtScheduler()`、直连 `SignalBus` 单例 → 零测试。现全部支持构造注入（`TaskNotifier` 补 `signal_bus` 参数），新增 `tests/test_background_services.py` **21 用例**覆盖调度注册 / 归档阈值（0 与 9999 边界）/ 循环克隆（`+1d/+3d/+1w`）/ 摘要通知（启用开关、静默时段、逾期合并）
- [x] **dev 依赖补全**：`black` / `ruff` 加入 `[dependency-groups].dev`（此前只在 `[project.optional-dependencies].dev`，`uv sync --dev` 拿不到）；`uv sync --dev` 已验证可用。顺带用 `ruff` 清理全项目 lint：**52 → 0**（41 处自动修复 + 手工修复），其中揪出 4 个真实缺陷：
  - `task_list_view.py` 缺失 `TaskService` 导入（`from __future__ import annotations` 掩盖了运行时错误）
  - `task_dialog.py` / `batch_controller.py` 重复导入 `TaskService`
  - `design_tokens.py` **`"surface_raised"` 字典键重复** —— 后一处硬编码静默覆盖了前一处 token 引用，导致 `t.surface_raised` 永不生效
  - `about_dialog.py` 缺失 `QShowEvent` 导入；`app.py` 未使用局部变量
- [x] **CI（`.github/workflows/test.yml`）实际现在就是红的**（此前记「52 → 0」时那批 2.0 代码还没跑过 lint）：`ruff check src/ tests/` 报 4 处（2 × `F541` 多余 f 前缀 + 2 × `F401` 未用导入），已修到 exit 0。同时把第三方教程副本 `docs/fastapi-alembic-demo` 加进 `[tool.ruff] extend-exclude`（用 `extend-` 而不是 `exclude`，后者会覆写默认排除表把 `.venv` 拖进来），`scripts/create_package_db.py` 的 `sys.path.insert` 按文件豁免 `E402` —— 这样本地 `ruff check .` 和 CI 看到的是同一份东西
- [x] **修掉一条日期定时炸弹**：`TestCreateTask::test_create_done_sets_completed_at` 用的 md 是 `DONE <2026-09-14>`，而 `<date>` 紧跟状态关键字时解析成**计划日**不是截止日（见 `md_parser` 的单日期规则），于是 `completed_at` 落到「当前时刻」——撰写当天两者恰好同日，必然通过，次日起必然失败。补上时间让这条 md 真的带上截止日，`completed_at` 的「截止优先」口径才被覆盖到

## 🖥 桌面版（`desktop/` · Tauri）工作线

> 这一节是主线在追的：`desktop/` 是 2.0 界面的 Tauri + TypeScript 重写。
> 2026-09-15 起 PySide6 版整体归档到 `archive/pyversion`，与它**零数据共享**。
> 现状详见 `desktop/README.md`。

已完成：

- 窗口外壳（无边框标题栏 / 置顶 / 托盘 / 全局热键 `Ctrl+Shift+Space` / `Ctrl+1..5`）、主题、五个页面的交互逻辑（甘特时间轴、图谱、热力图、批量表格、标签改名合并）
- **修掉「双击、右键都没反应」**：`dropdown` 的 `setValue` 会回调 `onPick`，于是 `paint → setValue → onPick → paint` 无限递归 —— 抽屉节点建出来了却永远加不上 `.open`，表现就是点了没反应
- **右键菜单**（打开维护抽屉 / 标记完成 / 删除）：删除走 `shared.removeTask`，和抽屉共用同一条确认路径与同一套措辞
- ~~**时间轴按数据自适应**：窗口 = 粒度下限 ∪ 数据跨度 ∪ 今天~~ → **已推翻**，见下方「档位改成真正的聚焦窗口」
- **真实时钟**：`TODAY` 不再读 `DEMO_TODAY`（写死 09-12，害得时间轴上的「今天」指着 12 号星期六）。`DEMO_TODAY` 现在只负责排布演示数据本身
- ~~**快速新建搬到任务页**，替代页头「＋ 新建任务」~~ → **已推翻**，见下方「新建回到页头」
- 删除类操作的二次确认（`shell/confirm.ts`：抽屉单条 / 管理页批量 / 标签移除，默认焦点给「取消」，Esc 与点遮罩都算取消）
- 图谱：舞台按可用空间铺开（不再写死 900×520）、连线改弧线、节点按离中心的距离错峰入场、右上角加分区标注
- 设置面板 **AI 助手页签已移除**：7 行没有一行接了后端，其中「专用工作区」还指向已删除的目录

已完成（本轮）：

- **持久化**：启动时读库、改完落盘（`data/db.ts`）。Tauri 下走 `tauri-plugin-sql` 直连 SQLite（和原版同一个存储），纯浏览器预览降级到 localStorage —— 刷新不再丢数据
- **分区真正生效**：`partition` 进数据模型，`activeTasks()` 按当前分区过滤。以前 rail 底部的切换器只是弹个 toast
- **Markdown 导入 / 导出**（管理页）：方言实现在 `data/markdown.ts`，抽屉里的 md 源和导出共用同一份序列化
- **热力图对齐真实日历**（`pages/activity.ts` + `styles/pages.css`）：`.hm` 没给 `grid-auto-columns`，隐式列被 `justify-content: stretch` 拉到平分容器 —— 实测列距从 17px 变成 45px，而月份标签按 17px 算绝对位置，整条月份刻度都压在左边一小段；此外标签只按每列周日的月份判定，每月 1 号不在周日时就晚一格（08-01 在 07-26~08-01 那列，标却打到 08-02 那列）。另：「近一月」档位实际是 12 周 = 三个月，标签按周数说话改成了 5 周。冒烟新增断言把「标签列 vs 日历」钉住
- **图谱不再被切一半**（`pages/graph.ts`）：`.gpage` 那条规则从来没命中过任何节点（页面根节点是 `.page`），`.gwrap` 只能退回 min-height 320，而画布高度却按 `innerHeight - 268` 估算成 632 —— `overflow:hidden` 之下底下半张图没了。现在高度由 `#page-graph` 逐层传下来，画布按**量出来的**容器尺寸排布（工具行要先落地再量，否则又会差它那 36px），铺成一圈**椭圆**而不是按短边取半径的正圆 —— 节点占宽从 32% 提到 66%

本轮又推翻了上面两条结论，都是被实际使用打回来的：

- **档位改成真正的聚焦窗口**（`data/timeline.ts`）：档位从「本周 / 本月 / 近 30 天」换成「昨天 / 今天 / 上周 / 本周 / 上月 / 本月」，窗口严格按档位算，不再并数据跨度 —— 以前点「今天」看到的还是好几周，档位名和它实际做的事对不上。代价是窗口外的任务不进表，所以工具栏那个「N / M」计数必须一直看得见（它是「没有静默丢弃」的唯一交代）。窗口算法也搬进了 `data/timeline.ts`：档位名与它覆盖的天数必须是同一份定义
- **新建回到页头，工具行的「快速新建」撤掉**（`pages/tasks.ts` + `pages/taskForm.ts`）：那个框只能填名称和标签，建出来的永远是「待办 + 普通 + 无起止」，必须再开抽屉补一遍。现在页头「＋ 新建任务」开的是与编辑同一套的完整表单
- **抽屉重构**（`pages/taskDrawer.ts`，这个界面是软件核心）：① 分成「任务定义 / 活动时间线」两个有标题有边框的分区（以前两串内容平铺，读到中间分不清哪行属于哪一段）；② 时间补齐——开始只到日期、结束到年月日 + 时分，两者共用 `shell/dateTime.ts` 的同一个组件与同一套排布（列表上写着「⏰ 今天 15:00」，而抽屉里原本只有一个日期框，时分根本没有地方改）；③ 进度既能拖也能直接键入；④ 活动记录可编辑可删除（改过标「已编辑」；系统记录不给编辑入口）；⑤ 底部「删除 / 保存」整体撤掉——所有字段本来就即时保存，那个「保存」实际只做「收起抽屉」，名字在骗人。连带删掉 `shell/drawerPref.ts` 与设置里那行「保存后收起抽屉」
- **优先级终于出现在列表上**（`pages/shared.ts` 的 `urgencyBadge`）：列表行原来那个 8px 圆点是**状态**色（待办蓝 / 进行中橙 / 已完成绿 / 逾期红），优先级根本没画出来 —— 用户看到的「优先级圆点」其实在说状态，自然分不出谁更急。现在列表与编辑界面共用同一套 P0–P3 徽标
- **删掉「循环」字段**：它从来没有行为、界面上也没有编辑入口，只是随 md 文本空转。md 解析仍吃掉旧的 `+1w`（`LEGACY_REPEAT`），否则它会粘进标题变成「写周报 +1w」；存储是整份 JSON，不需要迁移
- **截止文案收敛成一处**（`data/time.ts` 的 `dueTextOf`）：md 导出改为直接读 `end` + `at`，不再 `due.includes("今天")` 倒推日期（加一个「明天」的写法就会导出 `⏰2026-明天`）

标签这一块的三处修正（用户报「维护了多个标签，列表里只有一个，图谱也没体现」）：

- **解析只认带 `#` 的写法**（`data/tags.ts` 的 `normalizeTags`）：`matchAll(/#[^\s#]+/g)` 把「学习 工作」解析成空数组，新建时又被兜底成单个 `#工作` —— 这就是「只剩一个」的根因。现在带不带 `#` 都认、去重、超 3 个明说被忽略了哪几个
- **图谱把标签集合写死在六个预置名里**（`data/mock.ts` 的 `TAG_NAMES`）：新标签不是节点，**挂在它上面的任务在图上根本不画**。现在标签集合按数据现算（预置的在前、新标签按字面接后面），多标签任务仍然只占一个节点，但连到自己拥有的**每一条**标签（原来只连「主标签」，第二标签完全不可见）；历史数据里没标签的任务归「未分类」
- **列表把标签和截止挤在同一行**：三个标签会被 `overflow:hidden` 静默裁掉。现在标题与截止同行、标签单独一行
- 连带：新建必填「任务名 / 标签 / 结束时间」，缺一个都不落库并提示缺什么（以前没写标签会偷偷塞 `#工作`）

- **总览焦点时间轴的「现在」改成读真实时钟**（`data/time.ts` 的 `nowMinutes()`）：`mock.ts` 里那个 `DEMO_NOW_MINUTES = 09:30` 是为了让标记和排在演示日期上的样例任务「说得通」，代价是这块界面永远在说错时间（晚上打开，标记停在上午九点半）。样例数据排在哪天是样例数据的事，界面上「现在」必须是真的；另加一分钟一跳（只在本页可见时重画），免得它变成「写死打开时刻」。e2e 按真实时刻算期望百分比来断言

剩余缺口：**已清空（2026-09-15）**。

最后这几条经确认不再追，一并撤了，留个下落免得有人按老清单又核一遍：

- **Rust 侧业务命令**：不接原版 CLI / 命名管道，前端直连 SQLite 就够（`lib.rs` 继续只有 `app_exit`）
- **演示口径**：总览「较昨日 +2」和活动页「导出」这两处仍是写死的，分别要历史快照表和真导出才谈得上改 —— 位置在 `overview.ts` / `activity.ts` 的注释里，不再单列
- **lint / format script**：CI 已覆盖类型检查、构建与冒烟，格式化暂不引工具
- **每日摘要气泡 / 归档天数 / 管理页多条件筛选侧栏**：原型对照欠的那三项，不搬
- [x] **批量新建**（任务页「批量」按钮）：一次粘贴多行，走同一套 md 方言，实时预览「将创建 N 条」，Ctrl+Enter 提交；Enter 留给换行
- [x] **活动报告搜索框**：只重画列表不 remount —— 整页重建会把输入框连焦点一起重建，「每敲一个字光标跳一下」的搜索框等于不能用
- [x] **抽屉可编辑**：标题、标签、截止（日期框 + 今天 / 明天 / 下周 / 清除）。此前三者全是只读节点 —— 建任务时手滑打错一个字，那条任务就永远错着
- [x] **逾期自动标记**（`store.refreshOverdue`）：`TODAY` 读真实时钟之后，状态还写死在数据里，过了截止日没做完的任务会一直显示「待办」。只从 `todo` 标 `overdue`，**不动 `doing`** —— 手动设的「进行中」下一秒被系统改掉，那个下拉框就像坏了
- [x] **分区口令 + 空闲锁定**（`shell/lock.ts`）：口令按分区设置，切过去挡一层锁屏；空闲若干分钟无输入自动重新上锁。定位是**隐私屏风不是加密** —— 忘了对不起，没法找回（设置时就把这句话写在浮层里）
- [x] **Markdown 源实时预览**（抽屉）：这个框以前能打字却没接任何处理，敲半天没反应，看着像坏的。现在边敲边把那行 md 摊开渲染，写回要显式点「按 md 更新任务」；状态不进 md，所以 `[x]` 不改状态，start 不在方言里所以也不动起点
- [x] **草稿模式**（`shell/draft.ts`）：快速新建与批量框里没提交的字跟着 kv 走（重启还在），页面上一条草稿条说明「字还在那儿」，提交成功立刻清掉 —— 不知不觉自动保存的输入，和丢了字的输入框长得一模一样
- [x] **设置面板死行清理**：删掉那排点了不会有任何反应的行（最小化到托盘 / 开机自启 / 归档 / 安静时段 / 已完成置底…），接上「保存后收起抽屉」（`shell/drawerPref.ts`），分区页改成活的条数与锁标记。原则同当初删 AI 页签：**一排看着能配、实则无用的开关，比没有这一页更误导人**
- [x] **CI**（`.github/workflows/desktop.yml`）：类型检查 + 构建 + 端到端冒烟。此前唯一的自动关卡是 `tsc`，而它看不出「点下去没反应」这类运行时故障
- [x] **焦点时间轴同样按任务聚合 + 气泡改「+N」**（`renderDayAxis` + e2e + DESIGN §4.1）：你指出「已过期未完成」要只摆 3 条折成「等 N 个」，并嫌「更新了 N 条记录」太长。一并做的：① 轴上气泡与展开的簇改成「任务名 `+N`」（绿字 `.tdt-cn`，N = 这个任务这一天更新了几条）—— 气泡是标记，一句完整的话会把整条轴挤满；② 「已过期未完成」只摆前 3 条、其余折成「**等 N 个**」（N = **总数**，含摆出的 3 条 —— 「A、B、C 等 N 个」的 N 就是全部），可点、落到逾期筛选；③ 顺手收了一个真问题：这一栏的判据原来是「未完成 && end < 今天」，比「逾期」宽（把「进行中但已过期」也算进去 —— 系统故意不动 doing），于是栏上 47、点过去清单只有 40，两个数都没算错摆一起就像坏了；现在与 `refreshOverdue` 同一个谓词（`status === "overdue"`），栏 / 指标卡 / 筛选清单三处同一个数。**调试中立的教训**：e2e 里判「活动是否落在今天」时用了 `toDateString()`（本地时区），而应用的约定是「时间戳的 UTC 分量 = 墙上时刻」（`dayOfStamp = floor(stamp/DAY_MS)`）—— 在 UTC+8 下「昨天 17:20」会被误判成今天凌晨；断言必须顺着被测代码的约定走
- [x] **总览近期活动：默认每页 50 · 头部报「前 N 个」· 单位「个任务」**（`shared.ts` 的 `FEED_PAGE_SIZE`、`pages/overview.ts`、`pager.ts` 新增 `unit` / `showTotal`、e2e + DESIGN §4.1/§4.7/§8.1）：你报「默认显示最近的 50 个活动，为什么显示了 88 个任务」。查下来 88 是**有活动的任务数**（按任务聚合后的总数，以前这里是活动条数），不是这一页的行数 —— 分页器当时写「第 1–20 条 / 共 88 条」，20 是默认档位、50 只是档位里的第三档。改动：① 默认档位 20 → **50**（`FEED_PAGE_SIZE`，**后续可能搬进设置**，真搬时改这一处常量；分页选项 20/30/50/100 不变）；② 卡片头恢复初版那句后缀，但内容从「共 88 个任务」换成 **「前 50 个任务」**（给的是窗口、不是总数 —— 旧写法会被读成「这一屏显示了 88 条」；末页收口成「前 88 个任务」，空表不带这句）；③ 分页器新增 `unit`（默认「条」）与 `showTotal`（默认报总数），这张卡传「个任务」+ 不报总数，于是只写「第 1–50 个任务」。另外三张表的文案一字未动
- [x] **分页器：档位下拉向上翻 + 样式减负**（`shell/menu.ts` 的 `openPanel`、`.menu.up`、`pages/pager.ts`、`pages.css` + e2e + DESIGN §4.7）：你报「分页数量显示溢出到下方、无法选择；分页样式繁琐」。**这是个真 bug**：`.menu` 原来只有 `top: calc(100% + 4px)`（永远向下展开），而分页器贴在卡片底部、那几张卡的正文是 `overflow: hidden`（撑满场景的硬要求）—— 菜单落在那条裁切线以下，**看得见一截、点不到任何一项**。修法：展开前先量，下方装不下且上方更宽裕就加 `.menu.up` 翻到按钮上方（落在卡片内部，既不裁也不挡人）；管理页 / 活动报告的分页器同一处受益。同时减负：① 只有一页时不摆 `◀ 1 / 1 ▶`（一个都按不动却最显眼）；② 档位下拉只写「50/页」（单位由旁边那句范围说明给，说两遍更挤）；③ 间距 10 → 6、上内边距 12 → 8，分页器里的档位下拉不再是「主按钮」长相。e2e 新增一条：菜单整块落在卡片以内 + 点一项真的生效
- [x] **任务页甘特重做：固定 32 天窗口 + 拖动平移 + 档位高亮 + 条按「创建→截止」**（`tasks.ts` 的 `GANTT_DAYS` / `ganttWindow` / `renderRow` / render / mount、`pages.css` 的 `.tt-band` `.seg` `.tt-range`、e2e + DESIGN §4.2/§8.1）：你的原话是「按昨天/今天/上周…呈现的效果都不好…按创建时间和截止时间创建进度条…点击快捷时间按钮时，除了原有的筛选任务外，重点突出这个时间范围内的当前任务的进度…32 天显示最美观，其他只需要拖动即可」。改法：① **窗口固定 32 天**（前 16 天 + 今天 + 后 15 天 —— 32 天没法两侧各 16，取整偏给「过去」，因为已经发生的事实要一直看得见），既不随档位变、也不按任务跨度撑开（`fitWindow` / `ALL_COL_W` 撤掉，横向滚动不再需要）；② **拖动平移**：在表格上按住横向拖 = 平移窗口、按天吸附（拖过半格才挪一天），监听挂在 `.tt-box` 而不是每次 render 都被换掉的 `.tt-scroll` 上；5px 以内算点击，拖过之后那次 click 在捕获阶段吃掉；③ **条的时间基准改成创建日 → 截止日**（原来「开始 → 截止」；⚠️ 开始日因此不再影响条形，字段仍在，属有意取舍），悬停小卡同步；两端出窗口贴边 + 截断标记，整条在窗口外时**两侧都打**；④ **档位只做筛选 + 高亮**：表头那一格、每行的日期带、条上落在这一段里的那几格（`.seg-in`，范围外压暗 `.seg-out`）三处一起说「这一段」—— 短档位（今天 = 一格）在 32 天里只有这样才看得出来；⑤ **拖远有出口**：今天不在窗口里时表头那句区间变成强调色「↺ 回到今天」，点它复位。e2e 五条：窗口恒 32 天且今天在第 17 格、拖两格 = 往过去挪两天、选「今天」三处高亮都出现、点档位后窗口对准这批任务（没有条被贴在窗口左缘）、拖远后一键回到今天
- [x] **焦点修正：点档位时窗口对准「这批任务」而不是「档位那段日历」**（`tasks.ts` 的 `focusAnchor()` + e2e + DESIGN §4.2）：你报「选『上月』时窗口是『上月 30 ～ 本月 21』，可任务都集中在上月末到本月初，视觉和任务对不上」。根因是自动聚焦用的是**档位首日 - 2 天**（对准日历上的那一段），而任务在别处 —— 于是高亮和日期带全在任务外面。改成 `focusAnchor()`：以这批任务**条形的跨度**（最早创建日 → 最晚截止日）**居中**，32 天装不下也照居中；只有该窗口与档位那段**没有交集**（某条任务的历史远在别处，跟着它跑会把档位整段甩出屏幕）才退回「档位首日 - 2」，一条任务都没有时同样退回；「全部」= 复位到「今天前 16 天」。只在点档位那一下算，之后一律尊重拖动
- [x] **修 BUG：标记完成没有「立马归档」**（`data/store.ts` 的 `dataChanged` + `e2e/smoke.mjs` + DESIGN §4.10）：你报「归档开着，但已完成没有立马归档」。根因：归档的重算**只在启动与改归档天数时**跑过（`bootStore` / `setArchiveDays`），而「逾期」是在 `dataChanged()` 里每次变更都重算 —— 两件事的判据都是「字段 + 今天」，凭什么一个实时一个不实时。于是点完「已完成」什么也没发生（其实规则生效了，只是要等下一次启动），看起来像那个设置不生效。修法：`dataChanged()` 里把 `refreshArchive` 与 `refreshOverdue` 并排放（未开自动归档的分区默认 0，这一步什么也不做）。e2e 新增一条端到端：给演示空间开 7 天 → **先证明样本有资格**（未完成 + 结束日已过 7 天，否则这条空跑）→ 在任务页标记完成 → ① 任务页不再列它（那正是「完成即归档」要的效果）② 存储里 `archived = true` ③ 管理页「已归档」那一档把它算进来（比的是分页器上的总数，不是这一页的行数）
- [x] **甘特条改回标准甘特：条 = 持续时间、单色、不画进度**（`pages/tasks.ts` 的 `renderBars`、`styles/pages.css`（删 `.tt-band` / `.seg-in` / `.seg-out` / `.tt-bar i`，加 `.seg-late` / `.late-mark`，状态底改**实色**）、`e2e/smoke.mjs` + DESIGN §4.2）：你说「进度和持续时间混合，导致甘特图本身可能失去价值，所以按照标准甘特图处理，不一定需要体现进度」。**同意，而且我这两轮先后试的两版都错在同一件事上**：先是「进度填充 + 范围内外焦点环」，再是「三段区域色」—— 都是往**一条**上叠两个量（位置 = 时间、长度 = 进度），读者分不清哪个是哪个；第二版还把进度整个挤走了。现在按标准甘特：**条 = 创建日 → 截止日，整块实色（颜色只编码状态）**，不画进度、不按档位切段；**进度回到行首那枚进度饼** + 悬停小卡 + 条内白字题注（够宽时）。**逾期仍按你要的**：条右端**渲染**延长到今天 + 原结束日处一道竖记，数据一个字节不动。档位只留**表头那几格**（`.tt-band` 与 `.seg-in/out` 全撤）。e2e：两条「条上不画进度/范围分段」（两个档位各量一次，`.seg-h`/`.seg-d` 必须为 0）+ 逾期那条（`.seg-late` + `.late-mark` + 条右端越过「今天」）；旧的 `bands` / `seg-in` 断言随之改写
- [x] **管理页「归档」列撤掉单条按钮，只留标**（`pages/manage.ts` 的归档列、`styles/pages.css` 的 `.arch-tag`（`.arch-toggle` 一并删）、e2e + DESIGN §4.5、desktop/README）：你说「归档列没必要显示『归档』按钮了吧，只显示归档标签即可，类似于『标签』或者『状态』」。对 —— 那一列和「状态」「标签」是同一类，本来就是**看**的。撤掉两轮前加的那枚单条按钮（它当时是为了解决「批量入口要勾选才出现」），动手的地方仍有两处：页头**按筛选**的那枚、以及**勾选之后**的批量栏；单条处置走「勾一行 + 批量栏」。e2e 改成：那一列里 `.arch-toggle` 数为 0、且这一档每行都带「已归档」标；单条恢复走「勾第一行 + 批量栏『取消归档』」，总数少 1
- [x] **管理页筛选合成一条单选**（`pages/manage.ts` 的 `VIEW_OPTIONS` / `viewOf` / `applyView`、`styles/pages.css`、`styles/controls.css`、e2e + DESIGN §4.5）：你说「全部状态~已完成+未归档+已归档，一行显示 6 个，单选是不是也可以，不需要归档标签」。可以，而且比我先做的「两组 + 小标题」「两组分两行」都对 —— 中间两版都撤了（`filter-stack` 与 `.chips-lb` 一起删）。现在是**一条**单选：`全部状态 / 逾期 / 待办 / 进行中 / 已完成 / 未归档 / 已归档`，每枚 = 一个视图，永远只亮一枚，**默认「全部状态」**（你上一条要的）。规则：状态那几枚不筛归档（两侧都算，哪条是哪条看「归档」列）、归档那两枚不筛状态；内部保留 `archiveFilter` 的第三态 `"all"`（状态那几枚在用）。**代价**：组合筛选（「已完成 × 已归档」）表达不出来了 —— 换来的是这一页的筛选一眼就懂。e2e：默认只亮「全部状态」、切到「未归档」后仍只亮一枚、页头批量按钮（跟着这枚走：已归档→「取消归档」，其余→「归档」）来回一趟真生效 + 空档给提示

- [x] **任务管理页：导出之后加一枚「归档」（按筛选执行）+ 归档筛选收成两档**（`pages/manage.ts` 的 `archiveBatchButton` + `ARCHIVE_OPTIONS`、`pages.css` 的 `.batchbar`、e2e + DESIGN §4.5、desktop/README）：你说「不是让你筛选归档列…建议在导出之后增加一个归档按钮即可，基于筛选条件已经可以实现批量筛选了；全部状态中关于归档的只有未归档和已归档」。我上一版理解错了 —— 做成了「常驻批量条 + 按钮标签里写『归档筛出的 N 条』」，那是把**筛选条件**抄进按钮标签里 ✗。已改成：① 批量条**回到勾选才出现**（那一排回答的是「我勾的这几条怎么办」，与「按筛选执行」是两种输入，不该挤在同一条里）；② 页头「**导出之后**」加一枚 **「归档」/「取消归档」**（标签与动作都跟着**归档档位**走：看「未归档」按归档、看「已归档」按取消归档；按**当前筛选、跨页**执行，不要求先勾，标签里不写条数），走二次确认（标题写明条数），恢复走 `keepActive`，筛出 0 条时给一句提示；③ 归档筛选**收成两档**：`未归档 / 已归档`（去掉「含归档」—— 混着看就没法按档位执行），导出也跟着这一档走。e2e：按钮位置（紧跟在「导出」后）、标签随档位切换、动作真生效（状态=已完成 × 已归档 那一档放回来再收回去，来回一趟回到原状）、空筛选给提示
- [x] **任务管理页：每行加单条「归档 / 取消归档」按钮**（`pages/manage.ts` + `pages.css` 的 `.arch-toggle` + e2e + DESIGN §4.5）：你说「任务管理上帮我增加手动归档按钮，对于已有数据用户自行抉择」。原来**手动归档只有批量入口** —— 「归档 / 取消归档」那两枚要**勾选之后**才随批量栏出现，只想收一条也得先勾上再点，看着像这一页没有手动归档这回事。现在「归档」那一列是「状态标 + 一枚按钮」：未归档行的按钮写「归档」（收走，任务页不再列它）、已归档行写「取消归档」（放回来，并 `keepActive` —— 本次会话不再被自动归档碰，否则刚恢复就没了）。行点击仍是打开抽屉，所以按钮要 `stopPropagation`。**已有数据的处置按你的口径**：不替用户决定 —— §4.10 那次「老库一次性冻结」把老库里没设过的分区写成「不归档」，一条都不会被自动收走；收哪几条由这两枚按钮说了算。e2e 两个方向各一条：取消归档 → 「已归档」总数 -1；归档 → 存储里 +1
- [x] **完成后归档：判据改「完成日」+ 档位改成 不归档/立即/7/30/90、默认「立即」**（`data/store.ts` 的 `archiveDays` / `completedOn` / `bootStore`、`pages/overview.ts`、`shell/settings.ts`、`e2e/smoke.mjs` + DESIGN §3.5/§4.1/§4.10/§6.2/§7.2、desktop/README）：你指出「已完成就直接归档，别再拿结束日算；设了 N 就用**完成时间** + N」。评估下来这是**修正**不是优化 —— 旧判据 `结束日 + N ≤ 今天` 两个方向都反：**拖到最后才做完**（截止 09-01、今天做完、7 天档）会被**当场收走**；**提前做完**（截止 12-31）反而要赖到明年。旧注释给的两条理由也都不成立：①「完成日要从活动里反推、解析函数在 pages 层」—— 那函数**早就有**（`completedOn`，总览「本周完成」在用），挪进数据层即可（位置问题，不是依赖方向问题）；②「结束日只可能晚于完成日」—— 这条正是错的那半边。改动：① 判据 = **完成日 + N ≤ 今天**（`completedOn` 挪到 `store.ts`，与 `refreshArchive` 同处）；② 档位 **不归档 / 立即 / 7 天 / 30 天 / 90 天**、**默认「立即」**（编码：`0` = 不归档、`-1` = 立即 —— 沿用 `0 = 不归档` 才读得懂老数据），名字从「自动归档」改「**完成后归档**」（「自动」说不出什么时候自动，7/30/90 也是自动）；③ 卡片改名「**已归档**」、副标改成说效果「任务页不再列它 · 点击查看」；④ ⚠️ **老库一次性冻结**：默认改成「立即」后，没设过的分区会拿到「完成即归档」—— 对一个用了很久的库等于开机把历史所有已完成任务一次收走，所以 `bootStore` 在**库里本来就有数据**时把缺席的分区显式写成「不归档」（它原来的行为），只有**新装**（含演示空间）拿到新默认；⑤ e2e 三条：默认档是「立即」、「立即」下勾完即离开 + 存储 archived=true + 管理页总数对得上、**反向**「7 天」下勾完仍留在任务页（少了这条，「把判据写成做完就收」也能让前一条绿）；交付口径断言改成**恒等式**（未归档 + 归档 = 100）不写死数字（种子里已完成多少由生成器决定）
- [ ] **待定：手动「取消归档」要不要持久**：现在它只记在 `store` 的 `restored` 里（内存集合），**本次会话**不再被自动归档碰 —— 但下次启动 `restored` 是空的，若那条任务仍是「已完成 + 结束日过了 N 天」，它会被**再收一次**。要么接受（下次启动再点一次「取消归档」），要么把 `restored` 落进 kv（那就得考虑它自己怎么清理：任务删了、分区换了、判据不再成立时该忘掉）。你问「已完成 / 归档 / 未归档的关系」时顺带发现的，没动代码
- [x] **总览加第五张卡「归档」**（`pages/overview.ts` + `focus.ts` 的 `showArchivedTasks` / `consumeArchivedRequest` + `pages/manage.ts` 加 `subscribePages` 消费 + `pages.css` 的 `.tiles` 四列改五列 + e2e + DESIGN §4.1/§4.10）：你追问「已完成不是应该增加吗？可以考虑增加归档数」—— 说得对，我上一轮那句「已完成掉数」其实藏着**同一个动作被记了两次**的错觉：开着自动归档时，「完成」与「归档」几乎同时发生，数字先加后减，看着像什么也没发生，而它只是**换了个地方**。现在把归档数摆出来，两处数字此消彼长，收走的那批始终看得见。这张卡也是唯一**不落在任务页**的：已归档的任务在任务页根本不列（归档 = 从「任务」界面收走），所以点它去**任务管理页的「已归档」** —— 那页是唯一能看见这批的地方；为此给管理页补了「切回来时看有没有请求」那一段（页面常驻，不补就不会重画）。**没有**把归档并进其它四张卡：那几张点开是任务页那批（未归档），并进去会立刻变成「卡上 19、点进去 18」。e2e：卡片数 4 → 5，并给这张卡单独写判据（落点那枚 chip 亮着「已归档」+ 那页筛出总数 = 卡上数字）。顺带把文档/注释里几处「四张指标卡」改成不带数字的说法（数量写死迟早要回来改）

- [x] **重拍 README 的界面截图**（`tools/screenshots.mjs`，2026-09-21 07:11）：这一轮界面改动（甘特重做 / 焦点时间轴 / 抽屉进度与时间线 / 配色与焦点）之后，README 里那 8 张图已经不匹配了 —— 按 §6.4 的规矩「图和界面不符比没有图更糟」重跑一次：`cd desktop && npm run build` 再用根目录的 `node tools/screenshots.mjs`，产出 `resources/screenshots/` 下 9 张（八个界面 + `social-preview.png`）。视口仍是应用默认的 1180×760、@2x、亮色、关动效，画面里的数据仍是演示空间那 100 条。**逐张核过**（不是「跑完就算」）：总览那张已经把两处新行为拍进去了（卡片头「按最近活动倒序 · **前 50 个任务**」、分页器「**第 1–50 个任务** · 50/页」）；任务页与页头自述一致（「填充为进度」、窗口 08-29–09-29 恰好 32 天、档位「本周」灯亮着、`筛出 4 · 共 99（本周有活动的任务里）`）；抽屉那张打开了历史最丰富的「优化列表页性能」（8 条记录，正序：最老的在最上面）。脚本自己也会打印它打开的是哪条、几条记录 —— 拍空历史等于没拍，这一行就是防这个的
- [x] **文档同步（第三轮）**（`CLAUDE.md` / `desktop/README.md` / `DESIGN.md` + TODO 本条）：按 `CLAUDE.md` 的「功能优化 → 文档同步」清单过一遍四份文档，补齐这一轮界面迭代的口径，并把几处**与代码不符**的说法改掉：① `desktop/README.md` 还停在「业务数据全是假的」「怎么接还没有结论」—— 数据层其实早已接通（SQLite + 迁移 + 读入口归位 + 故障不降级），种子才是唯一「假」的东西；文件清单也补齐了（`db.ts` / `schema.ts` / `time.ts` / `markdown.ts` / `export.ts` / `xlsx.ts` 等，以及 `pages/index.ts` 与 `registry.ts` 的分工）；e2e 一节从「约 20s、点一遍这些」改成真实覆盖与**约 2–3 分钟**，并写上「什么时候跑」那条新规矩。② `CLAUDE.md` 补 `npm run e2e` / `npm run tauri build`，加一节**验证 → 什么时候跑 e2e**（攒批跑 / 先 `tsc --noEmit` / 用户没让跑就不跑 / 红了先分类），把 `version.py` 与 Py 四层架构两节明确标成**只对 `archive/pyversion` 有效**（本分支没有 `src/`），并且改掉两处指向 `resources/help/manual.html` 的引用 —— 那个文件在本分支上**根本不存在**（是 Py 版的）。③ `DESIGN.md` §6.1 补打包命令与 e2e 时长，§6.2 补上这轮踩到的**四条断言规矩**（口径跟被测代码一致 / 别把「当前时刻晚于种子时刻」当前提 / 前提要自己造 / 别只数个数要量不变量）。④ 顺带记下：CHANGELOG 按封存约定不动
- [x] **重新打包 Tadado2 v1.0.0**（`npm run tauri build`，2026-09-21 00:40）：这一轮界面迭代（甘特 32 天窗口重做 / 焦点时间轴按任务聚合 / 抽屉进度搬进时间线 / 总览近期活动上限 50 / 分页器减负 / 小字提示全量核对 / 配色与焦点统一）之后重出安装包 —— NSIS `Tadado2_1.0.0_x64-setup.exe`（2.59 MB）与 MSI `Tadado2_1.0.0_x64_en-US.msi`（3.62 MB），都在 `src-tauri/target/release/bundle/` 下，便携版 `bundle/portable/Tadado2.exe`（7.68 MB）一并刷新。**验证照旧不看「能不能双击」**：读主程序版本资源确认为 `ProductName = Tadado2` / `FileVersion = ProductVersion = 1.0.0`；打包前跑完整 e2e（179 条断言全绿、0 失败）。**版本号没动**（仍是 1.0.0）：要不要为新一批改动升号是用户的事，这一条只做重新打包
- [x] **e2e 全量跑通（179 条全绿）＋跑出来的问题全部修掉**（`taskDrawer.ts` 一处类型收窄 + `e2e/smoke.mjs` 一批断言）：这一轮攒了甘特重做 / 焦点 / 配色 / 默认档位 / 抽屉进度与时间线 / 小字审计等改动，跑起来先被**一个编译错误**挡下（`taskDrawer.ts` 的 `activity.edited`：`activity.kind === "progress" && value` 这种写法收窄不掉 union，得写成整支 `if` + return —— 也就是说**构建本来就是坏的**）。修好后三轮把断言逐个对齐，暴露的三类问题：① **行为改了、断言没跟**：分页器档位不再带单位（`20/页`）、时间线改正序（导入活动那条要早的在前）、任务页默认档位「本周」后**旧导入数据会被筛掉**（那条端到端用例得先切「全部」）、只有一页时不摆翻页件（原断言在等一个不存在的 `▶`，直接超时）；② **我写错的断言**：排序下拉的勾选读到了**优先级**那个下拉（`.menu-item.on` 收起时也在 DOM 里，得限定 `.menu.open`）、`seg-in` 只数「有几个」（命中与否取决于那批任务的条形有没有跨过那格，改成结构性不变量：跨进档位的条必须带高亮段、格外的必须不带，并跳过窗口外的双截断桩）、进度编辑用例的夹具（那条记录的 `from` 取决于前面用例留下的进度，`from` 与上界相等时可改区间是空的 —— 现在先把进度压到已知的 30 再测）；③ **既有脆弱点（与本次改动无关，被凌晨那次运行暴露）**：种子里的「今天」活动带**固定时刻**（06:20 起），凌晨跑时那些时刻还没到，于是「新建的任务排第一条」这类断言不成立 → 改成量它**在第一页里**（这才是「事件驱动重画」要证明的）；判「活动是否落在今天」用了真实 UTC 天号，而应用的「今天」是**本地日历日** anchor（差一天）→ 按应用的口径算；老存档夹具写死 `09-20`，一过那天迁移出来的任务就被标逾期 → 夹具改成相对今天
- [x] **界面小字提示全量核对（第二轮）**（`pages/registry.ts`、`pages/tasks.ts`、`shell/settings.ts` + e2e + DESIGN §4.2）：你报「任务页那句『右键处置 · 批量迁入在任务管理页』不合理，是 Py 版的表述」，要求全应用核一遍。**7 处与现状不符，已全部改掉**：① 任务页页头说明（同一句）→ 改成说「这一页是什么」：`甘特时间轴：条为创建 → 截止、填充为进度 · 档位筛出这段动过的任务 · 按住拖动可平移`（手势交给悬浮提示）；② 任务管理页说明补上 `导出 / 数据迁入`（上一轮的指路话从任务页撤掉后，得由这一页自己说）；③ 总览说明 `今日指标` → `四张指标卡`（只有一张讲今天，另外三张被说小了）；④ 甘特气泡提示 `双击或右键打开维护抽屉` ✗ → `双击打开维护抽屉 · 右键处置（标记完成 / 删除）`（右键是**处置菜单**，不是打开抽屉 —— 照着旧话做会以为右键坏了）；⑤ 任务页空态 `…区间内没有任务，可切换档位…` ✗ → `当前筛选下没有任务 —— 可切换档位、状态，或清空搜索`（「区间」是窗口还筛行的年代留下的后遗症，现在窗口固定 32 天只画不裁）；⑥ 「关于」清单 `色条为起止区间…档位从今天到全年` ✗ → `条为创建 → 截止、填充为进度，窗口固定 32 天、按住可拖动平移`（**没有「全年」这一档**，条形基准也换了）；⑦ 「关于」清单 `批量迁入` → 与按钮同名的 `数据迁入`。**核对后确认没问题**：热键文案（`Ctrl+Shift+Space` 与 hotkey.ts 常量一致）、各页搜索框 placeholder、活动分析/图谱/管理页的所有悬浮提示与二次确认、抽屉里的分区注解与 md 示例、锁屏与设置各项。e2e 补两条：页头说明的**性质**（提甘特、不提别页入口名、不写操作手势）、「关于」清单与界面一致（无「全年」、有「创建 → 截止」与「数据迁入」）
- [ ] **空分区时总览没有一句空状态**（顺带发现，未做）：筛选与列表都空着，只有分页器那句「暂无」。属于**新增** UI 而不是文案不一致，要做请说一声
- [x] **「今天」竖线：只在今天落在窗口里时画 + 贴边对齐**（`tasks.ts` 的 `todayVisible` 判定与 `.tt-today.at-start/.at-end`、`pages.css`）：你提「今天在显示范围内才画那条线，否则跳过」。查下来这条规则**本来就成立**（那处一直有 `TODAY ∈ 窗口` 的判断），所以这轮做的是把它显式化并补上边界问题：① 判断复用同一个 `todayVisible`（同一件事只判一次，原来表头那句和竖线各判一遍）；② 今天贴到窗口两端时那个「今天」小签**贴边对齐** —— 居中的话它会伸进任务列（被吸附列盖掉半截）、越过右边界还会把表格撑宽。e2e 跟着量：今天在窗口里 = 1 条线，拖出去 = 0 条，回到今天又 = 1 条
- [x] **甘特配色/视觉升级**（`tasks.ts` 的 `--tt-wk` / `monthStarts`、`pages.css` 的 .tt-track / .tt-date / .tt-bar / .tt-band / .seg / .tt-today / .tt-moline + DESIGN §4.2）：① 条改成**胶囊**（9px 圆角）+ 未完成段内描边 + 填充段顶部高光（顺手记下一个坑：状态色填充得写 `background-color`，`background` 简写会把高光抹掉）；② **周末竖带**：表头那格上色，行里用「7 天周期、宽 2 天」的 repeating 渐变按 `--tt-wk` 对齐到第一个周六（不新增 DOM）；③ **月分隔线**：表头月首那格把「周几」换成「几月」+ 左分隔线，行里同列一条 `.tt-moline`；④ **今天**：表头底色 + 底线，行里 2px 竖线（透明度 .5、圆角）；⑤ **档位高亮三处同色同源**，条上那段改成「焦点环」（描边 + 外发光）而不是硬描边；⑥ 日期带改成上下留 3px 的圆角浮块、范围外压暗从 .62 收到 .5；选中行左侧加一条强调色。全部走 `tokens.css` 变量，深浅主题同一套规则
- [x] **排序下拉切不动（真 bug）+ 默认排序改「按优先级」**（`shell/menu.ts` 的菜单行回调、`tasks.ts` 的 `sortKey` 默认值 + e2e + DESIGN §4.2）：你报「默认按截止时间，切换选项无法切到其他选项上」。**根因在控件层**：`dropdown` 和 `seg` 一样是**无状态**控件，原来只有 `setValue` 会改显示，菜单行自己点了**不同步** —— 值确实换了、表也重排了，按钮上却还写着旧的那一项、勾也还在旧项上，看着就是「切不过去」（任务页的排序与优先级两个下拉都中招）。修法：**菜单行自己先 `apply` 再 `onPick`**（`setValue` 留给「别处改了值」的场合，如跨页请求复位优先级筛选），一处修好、以后新加的下拉不会再踩。默认排序同一条里改成**按优先级**（优先级相同的再按截止日期，「今天必须交的 P1」不会被埋在 P0 后面）。e2e 两条：默认「按优先级」、切两次之后**按钮文案与勾选项都跟着换**
- [x] **新建任务的「结束」默认今天 + 当前时刻**（`taskForm.ts` 的 `draftTask()` + e2e + DESIGN §4.6）：你说「结束时的时分默认是当前时间，目前是空，需要手动选择」。顺手修掉一个更隐蔽的错位：日期框本来就显示着今天（`draftTask` 的 `end` = 今天），但模型里的 `due` 是 null —— 字段**看着填好了**、点「创建」却说「还差：结束时间」。现在 `due`/`at`/`end` 一次给齐（`due` 由 `dueTextOf(TODAY, at)` 生成，与表单里的写法同源）；「结束时间是必填」仍在：点「清除」之后照样拦。e2e 拆成两条 —— 默认值（今天 + 当前时刻，允许跨一分钟误差）和「清掉之后被拦」
- [x] **任务页默认档位从「全部」改成「本周」**（`data/timeline.ts` 的 `current` + `tasks.ts` 首帧与跨页请求都对准结果集 + e2e + DESIGN §4.2）：你说「默认快捷时间按钮是『本周』，现在是『全部』」。这条翻过一次面，两次理由都写在 `data/timeline.ts` 了：曾经默认「全部」是怕停在某个范围让老任务像集体消失；甘特窗口改成固定 32 天之后，档位只剩「筛出这段时间动过哪几条 + 把这段高亮出来」，而这一页先要回答的是「**这周我推进了什么**」→ 默认改成「本周」。配套：① **首帧窗口也按这批任务对准**（否则档位说「先看这周」、窗口说「今天±16」，又各说各话）；② 从总览点数字卡/优先级/定位某条跳进来时，窗口同样重新对准结果集（和「清搜索词、复位档位」是同一类「别把目标挡在视图外」）；③ 数据没丢：工具行口径、一键切「全部」、空状态提示三处都在。e2e 两处跟着改：默认档位断言改「本周」，「启动 100 条」改从工具行那句「共 99」取数（分页器那个数是**筛出来的**，默认本周后会远小于 99）
- [ ] **甘特窗口天数（32）搬进设置**（待定）：现在是 `tasks.ts` 的 `GANTT_DAYS` 常量
- [x] **时间线改正序（追加模式）+ 编辑区两行**（`taskDrawer.ts` 的 `renderTimeline` / `startEdit`、`pages.css` 的 `.tl-edit-row` + DESIGN §4.6）：你提「时间线倒序读起来别扭，正序/追加模式更好」+「再次编辑时信息编辑区域太小」。① 渲染改成**正序**：最早的在上面、最新的贴着下面那行输入框，读到最底正好接着「记录一条进展」；**存储顺序不动**（`activities` 仍最新在前 —— md 里写明的那条约定，导出 / 统计 / 「后面那条记录」的上界判定都依赖它），只在渲染时反着铺。顺带修掉一个真隐患：就地编辑原来按 **DOM 下标**取卡片（`timeline.children[index]`），正序之后下标与数组下标刚好相反 —— 照旧写法点「编辑」会改到**另一条**记录，现在是卡片引用直接传进去。② 编辑区改成**两行**：第一行整行给说明，第二行才是进度值与保存/取消；`.tl-edit` 的内边距 2px→5px，编辑态两个按钮常显（不再等悬停）
- [x] **进度搬进活动时间线：草稿式提交 + 记录可再编辑**（`taskForm.ts` 的 `progressControl()` 增 `mode` / `pendingText` / `commit(text)` / `reset`、`taskDrawer.ts` 的 `.tl-prog` 与 `submit()`、`progressEditBound()`、e2e + DESIGN §4.6）：你报的两个 bug 都在这条链上 —— ① 「进度条调整后，还没来得及编辑信息，已经被提交了」；② 「进度相关（无其他活动信息）时不能再编辑」。改法：**拖动只改草稿，写一句说明按「发送」才落库**（控件上写着「待保存 65% → 70%」；光改进度不写说明不保存，给提示并聚焦输入框）——拖着就被记下来正是 bug ①；提交时那句说明**就是这条进度记录的 text**，说明与进度落在同一条上，不再分家。**进度记录现在可改可删**（以前只有 log 有入口，而带说明的进度记录长得像 log，于是 bug ②），编辑能改说明和值，但值有上界 = **后面那条进度记录的 to**（最新那条到 100）——时间线是单调的，改过头就出现「先 80 后 70」的倒走进度；改完把后面那条的 `from` 跟着挪，改最新那条时 `task.progress` 一起更新，删最新那条时进度退回它的 `from`。原则不变：**进度一律手动维护**。e2e 三条：拖着不落库且显示「待保存」、写说明后回车落成**一条**（带落差）、编辑超界被拦 + 合法值改完前后两条接得上
- [x] **总览近期活动加硬上限：只摆最近 50 条**（`shared.ts` 新增 `FEED_LIMIT = 50`、`FEED_PAGE_SIZE = FEED_LIMIT`、`renderFeed` 排序后截断 + e2e + DESIGN §4.1/§4.7/§8.1）：你说「我只想显示近期的 50 条，不想显示太多」。之前只把**每页**默认改成 50，总数仍是 88、还能翻到第 2 页 —— 缺的是**上限**这件事。现在上限是这张卡的定位（一眼看最近发生了什么，不承担「翻遍历史」，那归活动分析页）：聚合排序**之后**截断，所以「最近 50」是真的最近 50 个动过的任务；默认档位 = 上限 → 平时只有一页（一页时不摆翻页箭头），档位调到 20/30 时也只在这 50 条以内翻。e2e 改成**确认值**（`第 1–50 个任务` · 50 行 · 0 个翻页件 · 头「按最近活动倒序 · 前 50 个任务」）——88 → 50 这件事必须真的发生，写 `\d+` 就量不到
- [ ] **「最近 50 个任务」搬进设置**（待定）：`FEED_LIMIT` 现在是常量，方向是做成设置项；分页器上那个下拉保留为临时的每屏调节
- [x] **总览「近期活动」改成按任务聚合**（`pages/overview.ts` 的 `renderFeed` + e2e + DESIGN §4.1）：你指出「应该是按任务显示，而不是按任务的活动显示，避免多行针对同一个任务」。以前一条活动一行，同一个任务连着记三条进展就占三行、标题重复三遍 —— 扫一眼看见的只是「有个任务动了很多次」，它明明一行就说得清。现在**一个任务一行**：行内是它**最新**的一条活动，后面标「共 N 条」（不报数的话，「动过一次」和「动过八次、只看见最后一条」会长得一模一样，折叠就变成丢信息）；排序按**最新一次活动**倒序（聚成任务后「每条活动的时间」这个排序键就不存在了）；完整过程仍在抽屉时间线里，点这一行就到那儿。卡片头随之改成「按最近活动倒序 · 共 N 个任务」—— 分页翻的是任务数，单位要和它一致。对照：焦点时间轴**仍按活动**摊开是对的，那一块问的是「今天这几个钟点发生了什么」
- [x] **总览「近期活动」卡片头单位**（同上）：数的是任务数不是活动条数，避免与管理页「共 41 条」摆一起看着对不上
- [x] **管理页导出的「截止」列改成绝对日期**（`pages/manage.ts` 的 `exportDue` + e2e + DESIGN §4.9）：你问「任务管理那个导出有没有同步更新」—— 共用机制是同步的（同一个 `exportButton`、同一套 md/txt/xlsx 生成、同一条 `saveAs` 落盘），但有一处漏了：xlsx 的「截止」列直接写 `task.due`，而那是**给界面看的文案**（「今天 15:00 / 昨天」），于是相对词进了文件。今天导出的「今天」，明天打开就指错日子 —— 文件要存档、要发给别人。改成日期读 `end`（数据）、`HH:MM` 只在 `due` 里取（模型没有单独的截止时刻字段），与 `markdown.ts` 的 `taskToMarkdown` 同一条规矩；反过来按 `due.includes("今天")` 倒推日期的写法已经炸过一次（`⏰2026-明天`）。e2e 以前只量了活动分析那条时间戳，管理页这条**从来没量过**，所以一直不红；现在逐格读 xlsx 的 D 列（它用 `inlineStr` + 不压缩的 zip，原文躺在字节里，不用解压）。**先证伪过**：把实现还原成 `task.due` 跑一次，断言确实红了，报出来的是 `今天 / 今天 / 今天 19:30`
- [x] **任务列：第一行只放标题，截止与优先级并到标签那一排**（`pages/tasks.ts` 的 `renderRow` + `styles/pages.css` 的 `.lt1` / `.lt2` + e2e）：你提的是「任务列尽可能显示全任务标题」。以前标题与截止共用第一行、优先级徽标另占右端一列，标题只剩半列宽（约 120–150px），稍长一点就是省略号 —— 列表里看不出这是哪条任务。现在标题独占第一行（吃满整列 207px），第二行是「标签 + 截止 + 优先级」，截止 `margin-left:auto` 靠右、优先级仍钉在任务列右端（与行首那枚进度饼一左一右）。**挤不开时的取舍写进 CSS 注释**：先缩标签（缩掉一截还看得出是标签），截止与优先级 `flex: none` 永不缺席 —— 它们各占一个固定位置，被整条裁掉就再也找不回来。e2e 那条「标签独立成行（和截止分开）」是**整个反过来**的旧不变量，改成「标题独占第一行」并量严：只数 `.lt1 .dlt === 0` 还不够（哪天往第一行塞个别的它照样绿），连「第一行只有标题一个子节点」一起量；等不到那一行时**把现场打出来**（表行数 / toast / 库里有没有这条 / 搜索框的值）—— 只报「(没有标签)」看不出是没建成还是建了没画出来，这两种失败要改的地方完全不同（这条用例此前被当成 flaky 放过两次）

- [x] **最后一轮验收与重打包 v1.0.0**（`desktop/src/data/{schema,store}.ts`、`desktop/src/pages/{tasks,manage}.ts`、`desktop/e2e/smoke.mjs`）：一轮 `npm run e2e` 从红到绿 —— **196 项 OK / 0 FAIL**（108s），修掉三个真 bug 与几处「断言前提」，最后**同版本号**重打包。三个真 bug：① **「立即」档从来没被持久化过**：`asNumberRecord` 要求 `>= 0`，而 `ARCHIVE_NOW = -1`，于是「立即」写下去、读回来被当脏数据丢掉 → 启动时视为「这分区没设过」，再被下面那条冻结逻辑写成「不归档」（新增 `asArchiveRecord`，只把这一路放宽到 `>= -1`；`asNumberRecord` 留给空闲锁定那类真非负量）；② **新装的默认档位没落盘**（平时靠 `archiveDays[id] ?? ARCHIVE_NOW` 兜底），第一次启动后 `archiveDays` 仍是空的 → **第二次启动就被判成老库**、冻成「不归档」，`bootStore` 的新装分支现在把「立即」显式写进 kv；③ **管理页批量归档按钮的标签认错了判据**（`archiveFilter === "active"`）—— 点状态那几枚 chip 时归档维回到 `"all"`，于是**默认进这一页按钮就挂着「取消归档」**，改成 `!== "archived"`。顺带：`renderRow` 的 `rangeWin` 是死参数（档位高亮 2026-09-21 起只剩表头 `.tt-date.in-range`，`pages.css` 里也这么写着），删掉形参 + 调用点并改准两处过期注释。e2e 四处断言的前提随新口径失效，按「量整张卡的性质」而不是「钉一条固定样本」重写：总览第五张卡实际叫**「已归档」**而断言按「归档」找（读到 -1）；「已完成」0 条时没有分页器、不该再要求「第一页」；「近期活动按任务聚合」的原样本（冒烟新建的任务）在改状态那步被当场归档，而总览只列未归档 → 改量整张卡（每行有标题 / 标题不重复 / 有「共 N 条」报数）；迁入的 `[x]`「批量二」同理会被当场归档，改到管理页数「两条都入库了」；「归档」动作的基准换成**这一档的条数**（拿「刚才放回的那批」去比是两批人）。**性能实测量**（100 条种子、1280×860、含 ~30ms 稳定判定开销）：冷启动 45ms · 切页 78~95ms · 搜索筛出 125ms · 抽屉铺时间线 156ms · 档位重排 253ms（其中 120ms 是探针里固定的 sleep）—— 最重的一屏 765 个 DOM 节点，没有需要优化的一处。重打包：release 编译 2m37s，NSIS `Tadado2_1.0.0_x64-setup.exe`（2.59 MB）、MSI `Tadado2_1.0.0_x64_en-US.msi`（3.62 MB）、便携版 `bundle/portable/Tadado2.exe`（7.68 MB，由这一次的 `desktop.exe` 重新拷入）；读版本资源确认 `ProductName = Tadado2`、`FileVersion` / `ProductVersion = 1.0.0`。**版本号一个字没动** —— 按「同版本号重打」处理

- [x] **文档同步（第四轮）**（`CLAUDE.md` / `README.md` / `DESIGN.md` / `desktop/src/**` 的注释与界面文案）：这一轮验收修掉的三个归档 bug 与甘特条的口径，逐份核过 —— ① **`DESIGN.md`**：§3.3 补 `asArchiveRecord` 与「**合法值由消费它的那条路定义**」（`asNumberRecord` 把 `-1` 当脏数据丢掉，正是「立即」读不回来的根因）；§4.10 补「新装那条路必须把『立即』**落盘**」与「读档要允许负数」；§4.5 补批量归档按钮的判据是「**不全是已归档**」（写成 `=== "active"` 时默认进页就挂着「取消归档」）；§4.2 视觉语言里那条「档位高亮**三处**同色同源」与 §6.2 的「别只数个数」都以已经拆掉的 `.tt-band` / `.seg-in` 举例 —— 前者改成「**只剩表头那几格**」，后者换成现在的不变量（选「今天」**恰好 1 格** + 条上分段恒 0）。② **`README.md`**：「任务 · 甘特时间轴：色条是起止区间，**填充是进度**」✗ → 条 = 创建 → 截止、颜色只编码状态（进度看行首那枚**进度饼**）；安装后主程序名也从 `desktop.exe` 改回实测的 `Tadado2.exe`（`desktop.exe` 只是 `target/release/` 下的构建产物名）。③ **界面上那两处小字同样是文档**：任务页页头说明与设置「关于」清单里的「填充为进度」一并改掉（条 2026-09-21 就不再画进度），`pages.css` 两处还写着「未完成段 / 进度填充」的注释同步改准，`tasks.ts` 顶部那句「填充仍是进度」同改。④ **`CLAUDE.md`** 的打包命令补上便携版那一步 —— 它不在 bundler 的目标里，要手动把 `target/release/desktop.exe` 拷成 `bundle/portable/Tadado2.exe`（上一轮打包时就撞见过那份是凌晨的旧二进制）。改完重跑 e2e：**196 项 OK / 0 FAIL**（文案类断言钉的是「含甘特 / 不含别页入口名」「无『全年』、有『创建 → 截止』与『数据迁入』」，都跟着过）

- [x] **甘特条内不再重复任务名（并贴右端）＋ 写一条进展自动把「待办」推到「进行中」**（`pages/tasks.ts`、`styles/pages.css`、`pages/taskDrawer.ts`、`e2e/smoke.mjs` + DESIGN §4.2/§4.6）：你提的两条。① **条内白字只写进度**（原来写「标题 · N%」）：标题在左边任务列里已经占了一整行，条上再写一遍是同一句话说两遍，而条的宽度本来就紧张 —— 省下的都还给进度；宽度阈值顺带从 78px 降到 **40px**（文字只剩三四个字符，门槛太高会让大半条上是空的）。**并把它贴到条右端**（`left: 7px` → `right: 7px`）：进度是从左往右长的，数字放在推进的那一头，读起来才是「朝右推进到 N%」。⚠️ **没有做「按进度值横向定位」**（把 `80%` 摆在条宽的 80% 处）—— 那是把进度当成**位置量**画回条上，而条的长度是**时间跨度**，两个量再叠到一条上就回到了 2026-09-21 被推翻的那条路（「进度和持续时间混合会导致甘特图失去价值」）。e2e 两条钉住：文字只匹配 `/^\d{1,3}%$/`、且文字右缘距条右缘约 7px（挑宽度 > 90px 的条量，窄条本来就不写字）。② **写一条进展 = 这条活儿动起来了**（`taskDrawer.ts` 的 `submit()`）：新建任务默认「待办」，而「待办」与「进行中」的差别正是**有没有动过** —— 写下第一条记录时状态跟着推一档。**只推这一档**：已完成是既成事实、逾期是系统标记，都不该被一次写记录改掉。推进走 `setTaskStatus`（它内部已经有 `dataChanged()`，所以原来那句不再重复调 —— 重复调用会多广播一轮、页面白重建一次），于是这次推进自己也留一条「**待办 → 进行中**」，时间线里看得见「从这一刻起变成进行中」。e2e 一条端到端：先把样本按回「待办」（不按回去这条断言的前提就不成立＝空跑）→ 写一句进展 → 状态变「进行中」且时间线里有那条 `待办 → 进行中`。**e2e 全绿 / 0 FAIL**（约 110s）

- [x] **重新打包 Tadado2 v1.0.0（2026-09-21 15:12）**（`npm run tauri build`）：装进去的是这一轮的三件事 —— 甘特条内白字**只写进度、并贴条右端**、**写一条进展把「待办」推到「进行中」**，以及第四轮文档同步。release 编译 2m38s，三个产物都在 `desktop/src-tauri/target/release/bundle/`：NSIS `Tadado2_1.0.0_x64-setup.exe`（2.59 MB，双击即装）、MSI `Tadado2_1.0.0_x64_en-US.msi`（3.62 MB，给静默 / 企业分发）、便携版 `bundle/portable/Tadado2.exe`（7.68 MB，由**这一次**的 `desktop.exe` 重新拷入 —— 它不在 bundler 的目标里，见 CLAUDE.md 那条命令的两行注释）。读版本资源确认 `ProductName = Tadado2`、`FileVersion` / `ProductVersion = 1.0.0`。打包前 e2e 全绿；**版本号一个字没动**

- [x] **任务页撤掉「全部 / 昨天 / 今天 / 上周 / 本周 / 上月 / 本月」那排档位**（`pages/tasks.ts`、`data/timeline.ts`、`styles/pages.css`、`e2e/smoke.mjs` + DESIGN §4.2/§4.7/§6.2、`desktop/README.md`、`CLAUDE.md`）：你问「这些没有用的，如果有反而不容易理解，我是不是可以直接删除」。**判断：可以删，而且该删** —— 它 2026-09-20 的用途是「点快捷时间按钮时，除了原有的筛选任务外，**重点突出这个时间范围内的当前任务的进度**」（你的原话），而条在那之后就按你的要求改回了**标准甘特**（整块实色、不画进度、不按档位切段）：**这个用途没有承载物了**。剩下的那件事（只列这段时间**动过**的任务）读起来像「看哪一段时间」，实际会让任务成批消失 —— 于是变成「我看到的本周」与「我库里有的」两套口径，反而不容易理解。**改动**：① 工具行那排 seg、`TASK_RANGES` / `timelineRange` / `setTimelineRange`、`poolTasks` 里的范围筛选、口径说明那句「（本周有活动的任务里）」、表头那几格高亮（`.tt-date.in-range` 与它的 CSS）、跨页请求里的档位复位、首页的窗口对准（`focusNextRender` 只在跨页请求时举）—— 一并撤掉；② 表头从此是**三种状态**（周末 / 今天 / 月首）；③ `focusAnchor` 去掉「档位段」参数，只按这批任务的条形跨度居中。**想「看最近动过的」有更直白的地方**：总览的近期活动卡片本来就是按最新活动聚合的（前 50 个任务）—— 这一点写进了 DESIGN §4.2（**没有**另加「按最近更新」排序；要的话是另一件事，说一声即加）。**e2e 跟着清掉 15 处档位依赖**（切「全部」造页数、档位高亮那条、进度透镜那两条、「刷新回到默认档位」、「先切全部再搜」这类前提）—— 其中好几处本来就是**为绕开档位筛选而加的前提**，删掉之后反而更短。**e2e 全绿 / 0 FAIL**（105.9s）

- [x] **删掉「待办」状态（新建即进行中）＋ 甘特条改成同色相的明暗渐变**（`data/types.ts`、`data/{mock,schema,store,markdown}.ts`、`pages/{shared,tasks,taskForm,taskDrawer,overview,manage,graph,registry}.ts`、`styles/{tokens,pages,controls}.css`、`e2e/smoke.mjs` + DESIGN §4.1/§4.2/§4.5/§4.6/§4.8/§8、`README.md`、`desktop/README.md`、`tools/README.md`、`tools/migrate-activity.mjs`）：你提的两条。
  ① **「待办」删了**（`TaskStatus` 四档 → 三档：进行中 / 已完成 / 逾期）。⚠️ **连带定下的一件事**：`refreshOverdue` 以前**豁免 `doing`**（系统故意不把「正在做但晚了」标成逾期）——「待办」一删，继续豁免就**没有任何任务会逾期**、那张卡永远归零。所以口径改成**只豁免 `done`**：**逾期 = 未完成 + 过了截止**，「进行中」= 未过期且未完成。
  **历史值不抹掉**：新增 `StatusName = TaskStatus | "todo"` —— 活动记录里的 `from: "todo"` 是既成事实，时间线照旧显示「待办 → 进行中」（`STATUS_LABEL` 按它对，保留「待办」那一格）；而 `Task.status` 只有三档，老库里的 `todo` 在**读入口**归一到 `doing`（`schema.ts` 的 `normalizeTasks`，同样计数）。e2e 补了一条**真实老库形态**的样本（`status: "todo"`）钉住这条归一。
  **md 写法**：`[ ]` 仍然收（它是最通用的「没画勾」记号），但读进来算「进行中」—— 代价说在明处：**往返会把 `[ ]` 写成 `[~]`**（README 与 tools/README 都写了）。
  **蓝一起撤了**：`--todo` / `--todo-soft` 从 tokens 里删干净（三个主题块），连带 6 处 CSS 引用（`.tt-bar` / `.tdt-p` / `.tdt-chip` / `.fg-bar` / `.node.task` / `.st-*`）；时间线 `log` 图标原来借的是蓝底，改用中性灰 `--sus`（那一类记录本来就该最安静）。`statusVar` 放宽到 `StatusName`（`todo` → 与进行中同色）。
  **撤掉的东西**：状态 chips 一枚、管理页筛选一枚（7 → 6）、表单状态按钮一个（3 → 2）、右键菜单「标记为待办」→「标记未完成」、总览勾选框取消完成后回 `doing`、进度饼「未开始 ▶」的判据改成 `doing + 0%`；`isOngoing`（合并口径那个包装）与「写一条进展 → 把待办推到进行中」那段一起删 —— **后者失去了前提**（没有可推的起点了）。
  ② **甘特条改成同色相的明暗渐变**（`--bar-overdue/doing/done` 定义在 tokens.css）：只在**同一色相内部**推浅推深，所以 §4.2 那条「颜色只编码状态」没有被破坏；浅端混 `--surface`（随主题走 —— 写死混白的话深色主题里条会被冲成灰的），深端混一点黑；方向固定「左上 → 右下」—— 条内那枚白字进度**贴右端**，得落在深的那一半上才读得出来。收尾圆点仍是纯色（7px 铺一张渐变只会露出左上角那一小块）。
  **顺手修了一个真 bug**：总览当日轴那条「现在」竖线 `.tdt-now` **没有 `pointer-events: none`**（任务页同类的 `.tt-today` 一直有）—— 它横跨整条轴，压在簇气泡上时那个簇**点不开**。e2e 正是在这一步超时的（跑测试的时刻一变、竖线就滑到簇头上）；用户遇到的是同一件事。修法：竖线不吃事件、顶端小签（带「现在 16:40」的 title）单独放行。
  **e2e 全绿 / 0 FAIL**；`resources/screenshots/` 九张图重拍（甘特、总览图例、管理页 chips 都变了）。⚠️ 一个**观感后果**：逾期口径放宽之后，演示数据里过去截止的任务成片变红（48 / 80），任务页看着比原来「红」不少 —— 那是**数据分布**的锅（种子把截止日大量排在今天之前），不是配色的锅；要它柔和些就调种子的日期分布，说一声即办

- [x] **甘特配色（基色 + 渐变方向）＋ 逾期条进度被盖住的 bug ＋ 图谱漏掉已归档**（`styles/{tokens,pages}.css`、`pages/graph.ts` + DESIGN §4.2/§4.3）：你报的三条。
  ① **「进行中」的基色换上**：`#c07f2d` → `#d98b1f`（深色主题 `#dfa24e` → `#e8ab45`）—— 原来那个色偏黄、饱和度低，看着像芥末。它同时是优先级 P1 的停靠点，所以 Px 那条「红 → 橙 → 黄绿 → 绿」跟着变亮（结构没动）。条内白字补了一道很浅的 `text-shadow`：面是渐变的、顶端偏亮，白字得在亮的那一半上也稳。
  ② **渐变上一版确实看不出**：上一版是 `120deg` + 两端只差 15% —— 条只有 19px 高、几百 px 长，**斜向的跨度被长度稀释掉了**（你回「并没有渐变效果」）。改成**垂直 `180deg` + 三段（浅 → 本色 → 深）**、跨度拉到 26 个百分点：19px 内把明暗走完、每条都一样明显；中段钉在本色上，所以条心不发灰（两段式会把白与黑插值成灰）。
  ③ **逾期条上的进度数字被「逾期延长段」盖住了**（真 bug，不是没渲染）：延长段 `.seg-late` 是 `z-index: 2`，它从结束日一直铺到今天、正好压在条的右端，而进度数字也贴右端 —— 于是**只有逾期条**（红条）上的百分比整个看不见，进行中的条（没有延长段）都正常。修法：`.tt-bar b` 抬到 `z-index: 3`。
  ④ **图谱漏掉了已归档**：它读的是 `activeTasks()`（未归档），而默认「完成后归档＝立即」之下做完的任务**当场**就离开那个集合 —— 图谱上于是永远只剩「进行中」，看着像坏了。改成 `partitionTasks()`（含归档）：节点 87 → **107**、「已完成 20」都回来了。**归档只该影响任务页**（那页的定位是「手上的活儿」）；活动分析与管理页本来就含归档，图谱跟上。
  ⚠️ 四处都**重拍了截图**（`resources/screenshots/` 九张：`npm run build` + `tools/screenshots.mjs`），但**没跑 e2e**（按「没有指令不跑」）—— 我核对了现有断言：图谱那边没有写死「未归档」口径的数量（节点数都是动态量的），色值与层级那边也没有断言，所以**判断**不会红；那是判断、不是实测

- [x] **甘特条：进度请回来了（填充）＋ 渐变改近水平 ＋ 逾期条进度被延长段盖住 ＋ 图谱补「已完成」**（`styles/{tokens,pages}.css`、`pages/{tasks,graph}.ts` + DESIGN §4.2/§4.3）：你报的三条。
  ① **「不同进度的感觉差异不大」→ 进度回到条上**（`--fill` + `.tt-bar::before`）：整条仍是「长度 = 时间、色相 = 状态」，**填充段的长度 = 进度**，底是同色系很浅的底（未走完的那一段）。⚠️ 这与你 2026-09-21 定的「条不体现进度」**相反**（那次原话是「进度和持续时间混合会导致甘特图失去价值」）；这一版的做法是**只填长度、不叠别的** —— 位置与色相把「哪段时间、什么状态」说完，填充只回答「走到哪了」，三者不抢戏。白字进度改**四向描边**：它现在会压在两种底上（浅底 / 饱满色），浅底那边靠描边撑住。
  ② **渐变还是看不出 → 换近水平 + 加大跨度**：上一版是 `180deg`，而垂直方向只有 **19px** 的跨度，根本铺不开（条是细长的）；现在 `100deg`、浅端 62% + 白、深端 74% + 黑，明暗沿**长度**方向展开。
  ③ **逾期条「没有进度」的真因**（一次性探针 dump 出来的，不是猜）：逾期延长段 `.seg-late` 是**实色**、`z-index: 2`，而截止日在窗口左侧的逾期条**整段都是延长段** → 它把新加的填充层整个盖住了。改成**半透明**（`color-mix(… 55%, transparent)`）：填充段压在它下面照样是实色，未填充的那半透出浅底，「逾期延长了」交给 `late-mark` 那一道竖记去说。
  ④ **图谱补上「已完成」筛选**：过滤器 3 枚 → 4 枚（全部 / 高优先级 / 进行中 / 已完成）—— 取数改成含归档之后已完成的任务本来就在图上，缺的是那枚入口。
  ⚠️ 全程**没跑 e2e**（按「没有指令不跑」）；跑了 `npm run build` 与截图重拍（九张），`resources/screenshots/` 已是最新

- [x] **甘特条放弃渐变，回到「状态色纯色 + 进度填充」**（`styles/{tokens,pages}.css` + DESIGN §4.2）：你贴的图里那种「**渐变分层**」是我这边三段色标造成的 —— 浅 → 本色 → 深 在 **100% 填充的长条**上会露出**两个折点**，读起来像条被切成了几截，而不是一块完整的胶囊。试过的两个方向也都不成立：斜向 / 水平会被**长度稀释**（几百 px 走完那点差值），垂直只有 **19px** 的跨度、根本铺不开。所以按你说的**放弃渐变**：`--bar-overdue/doing/done` 三个渐变变量整段撤掉，条的面直接是**状态色纯色**（`--bar-state`），进度仍由**填充长度**说 —— 顺带把条与徽标、进度饼、优先级色阶重新对回了**同一份色值**（渐变那几版它们其实是对不上的）。
  **色相的选择**：保持三色 —— 「进行中」琥珀金 `#d98b1f`、「已完成」绿 `#3c8d5e`、「逾期」红 `#c24536`。理由是它们在**明度上对齐**（≈47–51%，并排时不打架）、**色相上拉开**（34° / 145° / 8°），而且都是**既有的语义色** —— 换任何新色都会让条与徽标 / 进度饼 / Px 色阶脱节（那三处用的是同一份值）。
  保留的两处：未完成那段的**浅底**（`color-mix(状态色 24%, --surface)`，它让「填了多少」一眼可见）与条内白字的**四向描边**（白字会压在饱满色与浅底两种底上）。截图已重拍（九张）

- [x] **第五轮验收：功能 + 性能 + 重打包（纯色版）**：`npm run e2e` **192 项 OK / 0 FAIL**（104.9s），并确认上一轮那条新需求**已有断言守着**（「条内白字只写进度（不再重复任务名）」—— 用 `N%` 的形状判，不依赖「左边那一格的选择器还是不是那个」）。性能探针（跑完已删）在 100 条种子 + 1280×860 下量**「从动作发出到肉眼可见」**：动作后等两帧（`rAF` ×2 ≈ 32ms），要内容铺好的点再补 `waitForFunction` —— 两帧与 `evaluate` 往返都算在读数里，所以是**偏保守的上界**。读数：**冷启动 172ms**（首测 208ms，含浏览器冷启）· **切页 66–67ms**（五页三次几乎一致）· **搜索筛出 32ms** · **切筛选重排 66ms** · **抽屉铺时间线 84ms**；DOM 总计 2886，最重的仍是**活动分析 765**（那 361 格热力图），任务页 546 / 总览 461 / 图谱 331。**全面优于上一轮**（切页 78–95 → 66、搜索 125 → 32、抽屉 156 → 84）—— 一半来自**纯色替掉了渐变**（少一层渐变绘制 + 少一次 `color-mix` 求值），一半来自探针口径（上一轮是固定 `sleep` 等稳定，本轮是 `rAF` 两帧 + `waitForFunction`，没有多余等待）。**没有需要优化的一处**，直接打包：release 编译 **2m53s**，`Tadado2_1.0.0_x64-setup.exe`（2.59 MB）、`Tadado2_1.0.0_x64_en-US.msi`（3.62 MB）、便携版 `bundle/portable/Tadado2.exe`（7.68 MB，由本次 `desktop.exe` 拷入）。版本资源核对：`ProductName=Tadado2`、`FileVersion` / `ProductVersion=1.0.0` —— **版本号仍未动**（同版本号重打）。另：上一轮用户提的两件事里的第二件（「新建默认待办 → 写一条活动转进行中」）在代码里已经落地 —— **「待办」这一档 2026-09-21 整个删掉了**（`data/types.ts`：新建任务直接是「进行中」，`StatusName` 多出的 `"todo"` 只为历史活动记录保留），所以「写一条进展自动转进行中」这个中间态已经不需要了

- [x] **`tadado-activity-import` skill 入库 + CHANGELOG 解封写 v1.0.0**（新增 `resources/skill/tadado-activity-import/SKILL.md`；改 `CHANGELOG.md`、`README.md`；本机 `~/.claude/skills/tadado-activity-import/SKILL.md`）：
  **① skill 进仓库**。它此前只存在于本机（`~/.claude/`，而 `.claude/` 在 `.gitignore` 里，任何分支都不跟踪）—— 意味着**随仓库分发不了**，换台机器就没了。按归档分支已有的 `resources/skill/tadado/SKILL.md` 同一约定，放到 `resources/skill/tadado-activity-import/`。它是「**只指向仓库工具、自己不带副本**」的形态（对应 `tools/migrate-activity.mjs`），这一点原样保留 —— 两份实现必然分叉，而这份的正确性靠的是「工具真输出喂进应用「数据迁入」、断言共 N 条 0 行认不出」那条端到端。
  **② 顺带抓到三处过期表述**：「待办」这一档 2026-09-21 已删，`tools/README.md` 早就改准了（`[ ]` 读进来算「**进行中**」），而 skill 里还写着 `[ ]` = 待办 —— 分别在「输出的 md 长什么样」「字段从哪来」的默认值、「必须停下来问用户」第 3 条。三处一并改成「落 `[ ]`、**应用读作「进行中」**」，并与 `tools/README.md` 的措辞对齐。版本 **2.1.0 → 2.2.0**（本机那份同步改；两份的**指令正文**一致，仓库那份另加了一段「这是仓库副本、拷到 `.claude/skills/` 才生效」的说明，并把工具路径写成了相对链接）。
  **③ CHANGELOG 解封**。顶部那段「2026-09-15 起本文件封存……桌面端还没有发布版本号」在打包 v1.0.0 之后已经不成立，改成「记两条版本线」并注明 v1.x 从这里往下记；补上 **`## [1.0.0] — 2026-09-21`**：五个页面 / 数据与写法（含「数据迁入」与迁移工具）/ 本地优先（SQLite 单文件、权限清单里没有网络权限、分区口令是隐私屏风不是保险箱）/ 外壳（托盘 / 热键 / 自启动 / 双主题），`Changed` 记「待办」删除与逾期口径放宽、「完成后归档」默认立即、管理页批量归档跟档位走、筛选合成一条单选，`Removed` 记任务页档位筛选与 v0.x 遗留的「全年」档位 / 「延后·中止」。
  **④ README** 两处登记：迁移一节末尾补「配套 skill」一段（说明它只指向仓库工具、拷到 `.claude/skills/` 生效），「文档」清单加一行路径。
  ⚠️ 这一轮**没跑 e2e**、也**没重新打包** —— 动的都是仓库文档与 skill（`resources/` 不在 `bundle` 的 targets 里，见表 `tauri.conf.json` 无 `resources` 字段），安装包不受影响

- [x] **迁移工具收进 skill：为了能单独被分享**（`tools/migrate-activity.mjs` + `tools/sample-input.md` → `resources/skill/tadado-activity-import/`；删 `tools/README.md`；改 `desktop/e2e/smoke.mjs`、`.github/workflows/desktop.yml`、`README.md`、`desktop/README.md`、`DESIGN.md`、`CHANGELOG.md`、`SKILL.md`）：
  **你指出的关键事实**：`tools/` 后续无法独立使用，只有打包进对应的 skill 才能**被分享**。这条**推翻了我上一轮的判断** —— 我当时按「仓库内一致性」（e2e 引用、CI 监听、`git mv` 的历史）判了「不收」，还把它当成原则写进了 SKILL.md（「不要复制一份到本 skill」）。那条原则的**前提是使用者与作者在同一个仓库里**，而**分享**场景下正好相反：别人拿到 skill 却没有这个仓库，工具根本跑不起来。
  **做法：移动，不是复制**（两份实现必然分叉）。`migrate-activity.mjs` → `scripts/`、`sample-input.md` → skill 根；`tools/README.md` **删除**（内容并入 SKILL.md —— 补上「每一段读成什么」的写法表、以及「默认不覆盖已有输出」的理由），`tools/` 只剩 `screenshots.mjs`（那是给 README 拍图的**仓库开发**工具，不属于这个 skill）。
  **SKILL.md 改成自包含**：顶部说明从「这是仓库副本、指向 `tools/`」改成「**本 skill 自带脚本**，整个目录拷走就能用」；「转换器在哪」重写为「就在本 skill 里」，并把**为什么自带而不是外链**写清楚（要紧的是能被单独分享）；用法里四条 `node tools/migrate-activity.mjs` 全改成 `node scripts/migrate-activity.mjs`，另补一条「拿自带样例预演」。脚本自身的注释与报错文案也去掉了「在仓库根执行」这个前提（它现在在哪跑都行）。
  **引用跟着搬**（7 处）：e2e 端到端那条改成 `join("..", "resources", "skill", "tadado-activity-import", "scripts", "migrate-activity.mjs")`；CI 的 `paths` 从 `tools/**` 改成 `resources/skill/**`（注释同改）；根 README、`desktop/README.md`（两处，含 CI 那句）、`DESIGN.md`（三处）、`CHANGELOG.md`（两处）。
  **验证三关**：① 新位置**手动预演**跑通 —— `源 11 = 输出 4 + 系统记录过滤 7`，退出码 1（有待确认项，正是样例该有的结果）；② `npm run e2e` **192 项 OK / 0 FAIL**（104.4s）—— 那条端到端跑的就是**新路径**，说明「工具 → 应用解析器」这条链搬完之后仍然成立；③ 本机 `.claude/skills/tadado-activity-import/` 整体从仓库拷了一份，并在**本机位置**单独跑通（这才是「分享」要验的那件事）。
  **打包**：release 编译 202.7s，三个产物 17:51 —— NSIS 2.59 MB / MSI 3.62 MB / 便携版 7.68 MB（由本次 `desktop.exe` 拷入），版本资源仍 `Tadado2 / 1.0.0`

- [x] **撤掉 `tools/` 层级：工具住在它所服务的地方旁边**（`tools/screenshots.mjs` → `resources/screenshots.mjs`，删 `tools/` 目录；改 `DESIGN.md` 两处、`desktop/README.md`、`desktop/e2e/smoke.mjs`、`desktop/src/pages/tasks.ts`×3、`desktop/src/pages/manage.ts`、`desktop/src/data/markdown.ts`、`README.md` 文档清单）：
  上一轮把迁移工具收进 skill 之后，`tools/` 只剩截图脚本一个文件 —— 你定了方向：**简化项目组织结构**。落点选 **`resources/screenshots.mjs`**，**不新增 `resources/tools/`**（多一层就把「简化」抵掉了）；它和产物目录 `resources/screenshots/` 同名前缀，一眼看得出配套关系。
  **顺带把「工具住哪」这条约定写进了脚本注释**：工具住在**它所服务的东西旁边** —— 迁移工具在 skill 里（服务的是**别人手里的旧数据**，要能被单独分享）、截图脚本在 `resources/`（服务的是**本仓库的 README 素材**，绑死本仓库，分享对它没有意义）。仓库根从此不再有 `tools/` 这一层。
  **引用清了 9 处**：文档 4 处（`DESIGN.md` 的命令示例与 §6.4 节标题、`desktop/README.md`、`README.md` 文档清单 —— 后者原来还留着一行指向已删的 `tools/README.md`，和 skill 那行合并成一条）；源码注释 5 处（`tasks.ts` 三条、`manage.ts`、`markdown.ts` —— 原先都写着 `tools/migrate-activity.mjs` / `tools/README.md`，改成指 `tadado-activity-import` skill）。
  **验证**：① 脚本在**新位置真跑一遍** —— 9 张截图全部重拍、写到 `resources/screenshots/`（这比跑 e2e 更对口，它本来就不在 e2e 里）；② `npm run e2e` **192 项 OK / 0 FAIL**（105.5s）；③ 全仓搜 `tools/` 引用**清零**（只剩脚本注释里那句「仓库根也不再留 tools/ 那一层」，那是有意的说明）。
  **打包**：185.2s，三个产物 18:05（NSIS 2.59 MB / MSI 3.62 MB / 便携版 7.68 MB），版本仍 1.0.0 —— 这轮动了源码注释，所以照例重打了一次

- [x] **便携版改成 zip 交付：`Tadado2-portable.zip`**（`resources/skill/tadado-release/SKILL.md`、`CLAUDE.md`、`DESIGN.md` 两处、`README.md` 安装表；并实际生成了一次）：你定的口径 —— portable **直接按 `Tadado2-portable.zip` 包提供**，不再给裸 exe。
  **为什么这个口径对**：7.7 MB 的 exe 直接发出去，用户拿到的是「一个不知道要不要双击的东西」；zip 3.3 MB（压缩率约 57%），一眼就是「下载 → 解压 → 运行」。而且 Releases 页面上三种产物并排时，「两个安装包 + 一个裸 exe」读起来像三类东西，改成 zip 之后它们才是同一层级的「三个下载」。
  **打包流程从一步变两步**（这是本轮真正的改动点）：`Copy-Item desktop.exe bundle\portable\Tadado2.exe` → `Compress-Archive … bundle\portable\Tadado2-portable.zip`。zip 里**只放 `Tadado2.exe`、且放在根**（不带 `portable/` 目录层，解压出来不让人多点一下）。
  **坑写进了 skill**：这两步漏了**都看不出来** —— 漏第一步得到的是上一轮的 exe（名字、版本号都一样），漏第二步得到的是装着上一次 exe 的 zip（zip 名字也一样）。核法给了两条：两个文件的**时间戳**都该是刚刚 + 打开 zip 看一眼条目确实只有 `Tadado2.exe`。
  **实测**：`Compress-Archive` 产出 `bundle/portable/Tadado2-portable.zip` = **3.33 MB**，条目确认为单个 `Tadado2.exe`（7.68 MB）。
  **文档同步 4 处**：`README.md` 安装表、`CLAUDE.md` 打包命令（补压 zip）、`DESIGN.md` 的产物说明与打包形态表、`tadado-release` skill 五处（步骤表 / 步骤 2 / 参考体积 / Release 上传清单 / 末尾的坑）。skill 已重新同步到本机。
  ⚠️ 本轮**只多了一个 zip**，exe 本身没变 —— 所以**没重新打包、也没跑 e2e**（`desktop/` 下一行代码都没动）

- [x] **第四份交付物：迁移 skill 的 zip**（生成 `bundle/tadado-activity-import.zip`；改 `tadado-release` skill 四处、`DESIGN.md`、`CLAUDE.md`、`README.md`）：你指出「少一个旧数据迁移的 SKILL」。
  **它为什么该跟 Release 一起发**：用户装上 Tadado2 之后，要让自己的 Claude Code / Codex 帮忙搬旧数据，就得有那份 skill —— 而它**不是应用的产物**，不跟 `npm run tauri build` 走，`bundle/` 里也不会自己出现。不发出去，用户只能回去找仓库 URL，而「拿仓库 URL 让 AI 去读」正是前几轮才废掉的那条路（`resources/skill/` 那份之所以做成自包含，就是为了这个）。
  **关键细节：压目录本身，不要压里面的文件** —— `-Path resources\skill\tadado-activity-import` 而不是 `…\*`：zip 里必须保留 `tadado-activity-import/` 这一层，用户解压出来才能直接放进 `.claude/skills/`；少了这层，`.claude/skills/SKILL.md` 是**加载不到**的。
  **实测**：`tadado-activity-import.zip` = **15.6 KB**（压缩后；里面 `SKILL.md`(13.2 KB) / `scripts/migrate-activity.mjs`(23.7 KB) / `sample-input.md`(0.8 KB)，解压出来约 37.7 KB），三条都在 `tadado-activity-import\` 这一层下面 —— 这一层就是它能被 `.claude/skills/` 加载的原因。
  **流程与文档同步**：`tadado-release` skill —— 步骤表、步骤 2（新增「第四个产物」整段，含命令与「为什么压目录」）、`bundle/` 的核对命令（扩展名加 `.zip`）、参考体积、Release 上传清单（改成四份，并注明「它体积最小却最容易被漏」）、末尾的坑（补一条）；`DESIGN.md` 的产物说明补第四份；`CLAUDE.md` 的打包命令补压 skill 包那一步；`README.md` 迁移一节加一句「Releases 里有一份现成的，解压到 `.claude/skills/` 即可，不用 clone」。skill 已重新同步到本机。
  ⚠️ 同样**没重新打包、没跑 e2e**（`desktop/` 没动）
- [x] **新增项目级 skill：`tadado-release`（发布流程）**（`resources/skill/tadado-release/SKILL.md`；`README.md` 文档清单登记）：你要一个「自动提交和推送打包的软件、并同步 skill」的 skill。写成**五步**：验收（e2e）→ 打包（三份产物）→ 提交（一次 commit）→ 推送（push / tag / Release）→ 同步 skill 到本机。
  **一处没有完全照办的地方**：你要的是「**自动**提交和推送」；我写成「**一次确认后全自动**」—— 先把 `git status --short` + `git diff --stat` 的摘要给用户看、等他点头，之后提交→推送→打 tag→发 Release 一口气做完，中间不再打断。理由：提交推送不可逆，而这个仓库工作区里长期叠着几十项跨轮改动（此刻 49 项），挑着提交几乎必然漏或混进不相干的东西。**铁律**照 Git 安全协议写死：绝不 force push、绝不 `--no-verify`、绝不改 `git config`、`--amend` 只在明确要求时（用前先核作者）。
  **写进去的坑**：便携版不在 bundler 目标里（漏拷 = 名字对版本对但是旧二进制）、版本号散在**三个文件**（`tauri.conf.json` / `Cargo.toml` / `package.json`）+ CHANGELOG、`resources/skill/` 改了但 `.claude/` 被忽略所以本机不生效、安装包 14 MB **不进 git** 走 Release。另附一张「常见情况」表：工作区干净 / 只动文档能不能跳打包 / e2e 红了但用户催打包时**不许带红构建** / 版本号没变却要发 Release 要拦住。
  **打包命令**：`gh` 2.93 可用、remote 是 `git@github.com:HananxR/Tadado.git`、tag 目前停在 v0.2.7（v1.0.0 还没打）

- [x] **发布 v1.0.0 —— `tadado-release` skill 的第一次实战**：按那五步走完 —— **验收**（`npm run e2e` 192 项 OK / 0 FAIL，105.3s）→ **打包跳过**（`desktop/` 这轮没动，四份产物已是 18:05–18:23 的，正是 skill 里写好的例外）→ **提交 8 条**（按层拆：数据层 / 页面 / 外壳 / 样式 / Rust / 测试 / 文档 / 工具，`60 files changed, 8096 insertions(+), 2426 deletions(-)`）→ **推送**（`main` 7544c12..acf75ec、`v1.0.0` tag）→ **同步 skill 到本机**（两个）。
  **Release**：[v1.0.0](https://github.com/HananxR/Tadado/releases/tag/v1.0.0)，说明直接摘 CHANGELOG 那一节（2713 字），四份产物一起传 —— `Tadado2_1.0.0_x64-setup.exe`(2.59 MB) / `Tadado2_1.0.0_x64_en-US.msi`(3.62 MB) / `Tadado2-portable.zip`(3.33 MB) / **`tadado-activity-import.zip`(15.6 KB)**。这是仓库第一次有 v1.x 的 tag（此前停在 v0.2.7）。
  **按 skill 铁律执行的几处**：提交前先把 `git status --short` + `git diff --stat` 与**分组方案**给用户过目、等他确认；推送等用户明说；tag 单独推；全程**没有** `--force`、**没有** `--no-verify`、**没有**改 config。
  **顺带踩到两个坑（记下来）**：① `git mv` 对**未跟踪**的文件不可用（`tools/` 整个目录从没被 add 过）—— 搬迁移工具时就撞上了，改用 `Move-Item`；② PowerShell 的 `Get-Content -Raw` 按 **GBK** 读 UTF-8 文件 → 摘 CHANGELOG 时整段乱码，加 `-Encoding UTF8` 才对（写出去也要显式 `UTF8Encoding $false`，避免 BOM）。
  **提交信息风格**：Conventional Commits，与仓库既有风格一致（`feat(desktop):` / `chore(desktop):` / `test(desktop):` / `docs:` / `chore:`）

- [x] **`tadado-release` 补一节「在这台机器上干活：PowerShell 的几个坑」**（`resources/skill/tadado-release/SKILL.md`）：第一次实战之后按你的要求补，那五条都是**这一轮真踩到的**，不是理论：
  ① **`Get-Content -Raw` 默认按 GBK 读** UTF-8 文件（摘 CHANGELOG 做 Release 说明时整段乱码，看着像文件坏了）—— 读要 `-Encoding UTF8`、写要 `UTF8Encoding $false` 避免 BOM，并写明「判断文件坏没坏要用编辑器 / AI 工具看，别因为控制台乱码就去修文件」；
  ② **`git mv` 搬不动未跟踪的文件**（`tools/` 整个目录从没 `git add` 过 → `fatal: not under version control`），退用 `Move-Item`，代价是 git 认不出这是一次「重命名」；
  ③ **`gh … --jq` 在 PowerShell 里被拆参数**（`accepts at most 1 arg(s), received 3`），改用 `ConvertFrom-Json`；
  ④ **`git push` 的进度输出走 stderr**，PowerShell 渲染成红字 + `NativeCommandError`，**报红不等于失败** —— 看 `7544c12..acf75ec  main -> main` 那一行判断成败；
  ⑤ **长日志别用 `Select-String` 全量过**（`npm run e2e` 几百行会爆内部缓冲、截断输出），先 `Tee-Object` 落盘再筛，跑完删掉那个临时文件。

