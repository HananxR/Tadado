# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Virtual environment
.venv/
.venv-linux/

# IDE
.idea/
.vscode/

# Data (user-generated)
# 只忽略**仓库根**下的 data/（Python 版运行时产生的库与导出）。不能写成 `data/`
# —— 那会连 desktop/src/data/ 一起挡掉，而那是桌面端的数据层源码（types / db /
# store / mock / markdown / export …），此前一直被误挡，仓库里缺这一层。
/data/
resources/tadado.data
resources/config.json

# Log files
resources/loginfo/

# Claude (local only)
.claude/
.agents/

# IDE / agent 的本地数据（会话、teams、automations），不进仓库
.codebuddy/

# Local docs (not for public repo)
RELEASE.md
Tadado公众号文章.md

# OS
.DS_Store
Thumbs.db
.github_token
releases/
pack_scripts/
Tadado.spec
installer.iss
ChineseSimplified.isl
