# Changelog

Tadado 版本更新日志。格式参考 [Keep a Changelog](https://keepachangelog.com/)。

> **本文件记两条版本线**（见 README 的「版本线」一节）：
>
> - **v1.x — Tadado2**（Tauri 2）：当前主线，源码在本目录 `desktop/`；
> - **v0.x — Tadado**（PySide6）：连着代码一起归档在 `archive/pyversion` 分支，
>   最后一条是 v0.2.7（2026-08-16）。下面那部分历史**原样保留**。
>
> 上面那句「2026-09-15 起封存」是当时的话：桌面端那时**还没有发布版本号**，所以先不往这里写。
> 现在有了 **v1.0.0**，从这里往下记。

---

## [1.0.0] — 2026-09-21

**Tadado2 首个发布。** 桌面端整体重写：Tauri 2（Rust）+ WebView2，单文件安装包 2.6 MB，
不需要 Python、Node 或任何运行库。与 v0.x **零数据共享**（数据目录不同，装新版不动旧库），
但沿用同一套设计语言（暖灰双主题、语义色令牌）与领域概念（分区、优先级 P0–P3、状态、
活动时间线）。旧数据走[迁移工具](resources/skill/tadado-activity-import/SKILL.md)搬过来。

### Added

**五个页面**

- **总览**：五张可点指标卡（今日到期 / 逾期 / 进行中 / 已完成 / 已归档 —— 卡上的数就是
  点开后的**筛出总数**）、焦点时间轴（今天动过什么）、近期活动（按任务聚合）、优先级分布
- **任务**：固定 32 天窗口的甘特时间轴（按住拖动平移、按天吸附）；色条 = 创建 → 截止、
  颜色只编码状态，进度由**行首那枚进度饼**与条内填充长度表达
- **任务图谱**：分区 → 标签 → 任务三层的**确定性**力导向图，回答「这条任务和什么有关」
- **活动分析**：整年热力图 + 分标签报告
- **任务管理**：批量处置、标签重命名与合并（合并即改为已有名称）、**唯一**能看到已归档任务的视图

**数据与写法**

- **一行一条任务**：`- [~] 标题 #标签 ⏰09-29 14:30 :: 80%`；紧跟在下面的缩进行就是挂在这条
  任务上的**活动**
- **活动时间线**：状态、进度、优先级的每一次改动都留一条记录 —— 回执里说得出「什么时候变成
  这样的」，而不只是「现在是这样」
- **数据迁入**：整份 Markdown 批量导入，**导入之前**逐条列出「认不出的行」与「当前分区里已
  有的同名任务」，不悄悄少掉
- **旧数据迁移工具**（[`resources/skill/tadado-activity-import/`](resources/skill/tadado-activity-import/SKILL.md)）：
  把 v0.x 活动分析导出的清单转成可导入的写法，**连活动历史一起搬**。先预演对账、认不出就停、
  默认不覆盖已有输出。它**住在那个可单独分享的 skill 里**（`scripts/migrate-activity.mjs`，
  零依赖），所以整份拷走就能用，不依赖仓库
- **Claude Code / Codex skill `tadado-activity-import`**：上面那份手册兼操作指南 ——
  逐项对账、认不出就停下问、跑不了脚本时**不许手工硬转**

**本地优先**

- **SQLite 单文件**：`%APPDATA%\com.tadado.app\tadado.data`。备份、换机器都是**拷这一个文件**
- **没有联网的能力**：安装包的权限清单里**没有授予任何网络权限**
  （不是「承诺不联网」，是**没给这个能力**，可自行核 [`capabilities/default.json`](desktop/src-tauri/capabilities/default.json)）
- **分区**：数据的隔离边界 —— 可单独设口令、空闲几分钟自动重锁。
  口令是**隐私屏风不是加密保险箱**，所以没有「找回」，直接重设
- 覆盖安装 / 换安装位置 / 从安装包换便携版反过来换 / 升级降级，**都不碰数据**；
  卸载**默认保留**，清掉它只有一个开关

**外壳**

- 暖灰双主题（浅色 / 暗色，同一套令牌的两端）、无边框窗口 + 自绘标题栏
- 托盘菜单（设置 / 显示窗口 / 退出）、全局热键、开机自启动、单实例

### Changed

- **「待办」这一档删掉**（用户：默认就是进行中）—— 新建任务直接是「进行中」。
  「待办」与「进行中」的差别其实只是「有没有动过」，而这件事写一条进展就在做了，
  再让用户手动按一次状态是多一步。用户**可选**的状态因此只剩两个：进行中 / 已完成
  （逾期由系统按截止日算）。老库里写着 `todo` 的任务在读入口归一到「进行中」；
  **活动记录里的历史 `from/to: "todo"` 原样保留**（那是当时的事实），时间线照旧显示
  「待办 → 进行中」
- **逾期口径随之放宽**：**只有「已完成」豁免**。以前「进行中」也豁免（系统故意不把
  「正在做但晚了」标成逾期），那是在「待办」还在时写的 —— 删掉它之后继续豁免的话，
  **没有任何任务会变成逾期**，那张卡会永远归零。现在：**逾期 = 未完成 + 过了截止**，
  「进行中」= 未过期且未完成
- **完成后归档**：每个分区可设「不归档 / 立即 / 完成后 N 天」，默认「立即」。
  档位编码里 `0` = 不归档（沿用 v0.x 的语义，才读得懂老数据）、`-1` = 立即
- **管理页的批量归档按钮**跟**归档档位**走（判据是「不全是已归档」），
  放在「导出 ▾」之后、独立置最右；按**当前筛选（跨页）**生效，走二次确认
- **筛选合成一条单选**：`全部状态 / 逾期 / 进行中 / 已完成 / 未归档 / 已归档` ——
  每一枚 = 一个视图，永远只亮一枚。代价说清楚：组合筛选（「已完成 × 已归档」）表达不出来了

### Removed

- **任务页的档位筛选**（本周 / 本月 / 全部）：它想说的是「只列这段时间**动过**的任务」，
  但那读起来像「看哪一段时间」，实际会让任务成批消失 —— 用户看到的「本周」和他库里有的
  成了两套口径。要收窄范围，状态筛选 / 搜索 / 排序 / 分页就够了；看别的时间段靠**在表格上
  拖动**。顺带撤掉了随之无处安放的两处高亮（行里的日期带、条上的分段）
- **v0.x 的几处遗留**：任务页页头说明里的「全年」档位（从来没有过这一档）、
  从旧特性清单抄进来但桌面端**根本不存在**的「延后 / 中止」

---

## [0.2.7] — 2026-08-16

### Added
- **CLI 命令行通道**：`tadado-cli`（14 个命令：list/today/activity/add/edit/done/log/rm/tags/partitions/archive/recurrence/reminder/export/report）；GUI 运行时命令经本地管道转发给运行中实例（单一写者 + 实例身份校验），未运行时 headless 独立执行；JSON 输出 + `--format human`
- **Claude Code skill**：自然语言管理任务（会话上下文/自动消歧/快捷计算六预设推断/安全分级确认），内置今日摘要/快速添加/批量添加/周报/月报工作流
- **周报 / 月报摘要**（`report` 命令 + skill 工作流）：分区优先、任务标签分组、本周工作内容 + 下周计划、无进展标注与优先排列，输出可直接粘贴进汇报
- **AI 助手托盘入口**：一键启动专属 Claude Code / Codex 会话（自动续接上次会话、专用工作区、TADADO_EXE/TADADO_PARTITION 注入）；上下文超 80% 提醒 /compact
- **补齐导入/导出服务模块**：新增 `md_exporter.py` / `md_importer.py`（修复任务视图导入/导出按钮点击即 ImportError 的潜伏问题）
- **活动时间线只读展示**：双击任务编辑对话框内嵌与主界面同款样式的时间线，无记录自动隐藏
- **skill 管理入口**：设置 → AI 助手页签，随附 skill（含版本号）为唯一权威源，可编辑并同步分发到 Claude/Codex

### Changed
- **暖灰视觉重构**：设计令牌全面重标（暖灰阶文字对比度达标、暖调靛青强调色），卡片分层软阴影、圆角/字阶/焦点态统一；热力图四套配色重标（空单元格统一暖纸色、渐变锚定语义色）；深色主题暖灰化
- **设置对话框页签化**：常规 / AI 助手 / 归档与分区 / 关于（帮助文档并入关于页，浏览器打开）
- **关于页重构**：左侧品牌栏 + 分组行链接；版本更新记录浏览器打开（左侧可收起目录 + 版本标题锚点）
- **热力图显示设置就地化**：配色方案与起始年份移至热力图导航栏（🏠 旁），即改即生效；统计指标移至热力图下方右侧
- **Excel 导出下沉**：批量导出逻辑从 UI 控制器抽到 `services/task_exporter.py`，GUI 与 CLI 共用
- **字体平台适配**：Windows 使用系统字体；Linux 先探测系统 CJK/Emoji 字体，缺失才加载内置

### Fixed
- **关键词搜索计数**：`repository.count()` FTS 分支修正为 `rowid IN` + LIKE 兜底，中文关键词搜索总数不再恒为 0
- **配置默认值污染**：`_deep_merge` 深拷贝修复，`set()` 不再污染全局 DEFAULT_CONFIG
- **主题切换残留**：Markdown 编辑器打开时切换主题不再残留旧主题背景；托盘菜单 aboutToShow 重建导致的点击无反应
- **默认分区误跳**：任何配置变更不再触发跳回默认分区（仅默认分区值真正变化时切换）
- **管道可靠性**（Windows 命名管道）：服务端写完响应等客户端先断、客户端写完立即读、read_raw 收到首块即返回，修复响应丢失与互相等待

---

## [0.2.6] — 2026-08-03

### Added
- **任务列表与标签管理面板双向联动**：点击任务行高亮对应标签(加粗+着色+前置排序)；点击标签项任务列表按标签前置；标签列悬浮提示显示完整标签
- **活动分析标签云优化**：活动数为 0 的标签不再预选，仅在存在活动的标签中自动勾选

### Changed
- **延后处理天数调整**：右键菜单延后处理选项从 +1/+2/+5/+7/+10天 改为 +1/+5/+7/+10/+20/+30天
- **快速计算器平铺**：弹窗去除「时间粒度」下拉，6 个截止选项（今天/明天/本周日/一周后/本月末/下月今天）一次性平铺展示，预览格式改为「标签 (日期 时间)」
- **version.py 数据与逻辑拆分**：版本号数据移至 `_version_data.py`，发版时仅更新数据文件不再修改逻辑文件
- **界面精简**：删除任务管理侧栏无用 logo 按钮

### Fixed
- **标签面板选中样式**：selection 使用 accent 半透明背景，避免白底白字不可见
- **标签重命名/合并**：覆盖全部任务（含已归档），批量页面表格同步刷新
- **已勾选 ID 残留**：`load_tasks` 时清空已勾选 ID，避免切换分区后勾选状态泄露

---

## [0.2.5] — 2026-07-10

### Fixed
- **标签面板分区联动**：切换分区时标签管理面板已同步刷新，标签列表、重命名、合并均限定在当前分区范围内

---

## [0.2.4] — 2026-07-06

### Added
- **智能归档**：`archive_days=0` 即时归档，切换分区/改阈值自动追溯，改回非完成自动解归档
- **进度栏联动**：点击时段按钮按活动字段筛选，激活时主列表同步显示已归档任务

### Changed
- **架构优化**：新增 TaskService 数据门面
- **设置精简**：删除提醒/激励语分区，删除可见/自动归档列
- **归档视图**：统计栏/进度栏/活动分析/热力图均含已归档任务，主列表默认隐藏



---

## [0.2.2] — 2026-06-18

### Added
- **已完成置底**：DONE 任务自动排至列表底部，活跃任务在前，设置中可开关
- **双击编辑对话框**：双击任务直接编辑 Markdown 内容，实时解析预览优先级、日期、标题、标签，编辑时显示创建时间
- **日志系统**：每日午夜自动切割日志，保留 3 天，覆盖关键操作路径
- **安装数据保护**：升级安装时排除用户数据库文件防静默覆盖，包数据库覆盖前弹窗确认

### Changed
- **品牌视觉升级**：Tadado 标题 LOGO 渐变配色，遮罩品牌色重设计对齐应用图标

### Fixed
- 修复系统托盘图标与应用图标不统一
- 修复 TaskDialog NameError 导致编辑/新建崩溃
- 修复 raw_md 保存时格式未规范化

### Security
- 编辑/新建时校验截止时间不能早于创建时间，三处入口统一

---

## [0.2.1] — 2026-06-15

### Added
- **Linux 版本**：提供 `linux.tar.gz` 便携包


## [0.2.0] — 2026-06-12

### Changed
- **状态下拉框即时刷新进度**：切换至"已完成"进度自动刷新为 100，切回"进行中"自动设为 80，无需点击"追加进展"
- **截止时间默认值对齐快速计算**：新建任务模板中的截止时间从当前时刻改为快速计算默认值 23:59
- **创建任务默认截止日**：新建任务预填当天日期（对齐 DESIGN.md），移除"最近周五"逻辑
- **快速计算标签文案**："任务类型" → "时间粒度"，"临时" → "天"

### Fixed
- 进度 100% 因列宽不足显示为"00"：表格进度列与输入框拓宽至 55px
- 多条状态→已完成路径遗漏进度=100 自动设定：双击编辑对话框、批量创建、快速输入栏、Markdown 保存均补齐
- 批量更新状态至已完成时同步设置 completed_at 和进度字段
- 单任务保存时截止时间缺 fallback，与多任务保存行为不一致

---

## [0.1.2.4] — 2026-06-11

### Fixed
- 速览栏过滤排除已完成已归档（先前错误排除所有已完成，导致未归档的已完成任务不显示）
- 阿里云盘更新检测正则修复为支持 4 段版本号（3 段版号被截断导致误判无更新）

---

## [0.1.2.3] — 2026-06-11

### Changed
- **提醒重构**：取消托盘弹窗，提醒信息融入速览栏与进度栏轮播展示
- 速览栏标签时间粒度细化：今天到期的任务显示"X 分钟后/X 小时后/HH:MM 截止/已超时"
- 进度栏 hint/轮播模式融合截止紧迫度标识，逾期/今日任务排序加权
- 进度栏 hint 模式支持点击跳转任务，轮播数据与左侧列表同步

### Added
- **关于对话框升级日志**：版本号下方展示当前版本升级内容，层级分明
- 关于对话框新增下载渠道区块（GitHub Releases / 阿里云盘）
- 检查更新结果内联到版本行（检查中 ··· / ✓ 已是最新 / 🆕 新版本）
- 欢迎页 Banner 动态适配分区状态：空分区显示欢迎语，有任务显示日历日期

### Fixed
- Banner 背景文字可读性：新增半透明遮罩层，副标题颜色对比度提升
- 更新检查顺序调整为阿里云盘优先、GitHub 备用，适配国内网络
- 抑制 QSslSocket 设备未打开警告日志
- 启动遮罩期间强制箭头光标，消除鼠标转圈

---

## [0.1.2.2] — 2026-06-09

### Added
- 安装程序支持简体中文界面

### Fixed
- 安装程序中文显示乱码问题

---

## [0.1.2.1] — 2026-06-09

### Added
- 关于对话框支持检查更新，检测到新版本时推荐最佳下载渠道
- 新增阿里云盘下载渠道
- 关于对话框新增联系方式与反馈渠道
- 版本号统一为 4 位格式

### Changed
- 关于对话框重新布局，版本、更新、下载、反馈信息集中展示

### Fixed
- 进度栏时段筛选结果与实际活动数据不一致的问题
- 进度栏与活动报告活动计数不匹配的问题

---

## [0.1.0] — 2026-06

### Added
- Markdown 语法创建和管理任务，支持优先级、截止时间、标签
- 日历热力图 + 活动报告导出
- 分区管理：多分区隔离、密码保护
- 批量操作：全选、右键批量变更状态、删除
- 标签管理：重命名、合并，全局同步
- 到期任务提醒，安静时段免打扰
