# 项目进度（按功能模块）

> 每个模块记录创建时间（Sprint）、优化记录（时间 + 原因），便于追溯和迭代。

## Sprint 总览

| Sprint | 创建时间 | 内容 |
|--------|----------|------|
| 1 | 2026-04 | models (Task/TaskStatus/TaskFilter/TaskRepository)、Markdown 解析器/格式化器、SignalBus、测试 |
| 2 | 2026-04 | app.py、config.py、MainWindow (frameless + Win32)、SystemTray、双主题 QSS、main.py |
| 3 | 2026-04 | TaskInputWidget、FilterBar、TaskListModel/Delegate/View/Panel、TaskEditPanel、AboutDialog |
| 4 | 2026-05 | HeatmapModel、CalendarHeatmapWidget (12月×7×5 矩阵)、Tooltip、ActivityReportPanel、ReportExporter |
| 5 | 2026-05 | SettingsDialog、导入/导出、TaskScheduler、TaskNotifier、TaskArchiver、TaskRecurrence |
| 6 | 2026-05 | 活动时间线、StatusBadgeStrip、草稿模式、编辑区折叠、ProgressDynamicsBar |
| 7 | 2026-05 | 分区管理 (密码+自动锁定)、CalendarPopup/TimePopup、时间线重构、统计联动 |
| 8 | 2026-05 | QuickOverviewBar、BatchToolbar、编辑器分栏重构、DeadlineCalculator、MultiTaskDialog、DropdownWidget |
| 9 | 2026-05 | 三模块→合并且优化：活动看板+工作报告合并为"活动分析"(热力图+报告同页)；批量处理保留；3页导航(Ctrl+1/2/3)；PeriodSelectorBar统一时段控制 |
| 10 | 2026-06 | 图标系统重构(Phosphor风格填充+主题色适配)、菜单栏工具栏合并、活动分析页面重设计、任务树+活动流式内容、搜索/导出(MD/Excel/TXT)、标题栏重构(图标按钮+分区状态栏)、任务管理控制台重设计(左侧面板+归档/清理+导出) |
| 11 | 2026-06 | 设置模块重构：config.py 死配置清理、单页布局、分区表格(5列等宽+右键菜单)、归档按分区独立控制、DB 迁移(5→6)、提醒/锁定时段控件简化 |
| 12 | 2026-06 | 启动性能优化(固定窗口尺寸+延迟非关键初始化)、任务栏最小化按钮(changeEvent+minimize_to_tray联动)、标题栏新增最小化图标 |
| 13 | 2026-06 | 标签管理面板：重命名/合并标签、70:30 QSplitter 布局、SignalBus 标签信号、count() 标签过滤补全、# 前缀自动剥离 |
| 14 | 2026-06 | 主题系统重构：base.qss + expand_qss() 统一模板、移除"跟随系统"、objectName 替代内联 token QSS、新建任务编辑区统一、标签管理分页+空状态、任务选中红色加粗(无底色)+网格线统一+紧急程度独立渲染 |
| 15 | 2026-06 | 紧急程度→优先级重构：整行背景色渲染(红/橙/绿/淡蓝,4级均有着色)、Markdown `- [***]` 语法(`[]`始终表示优先级，兼容旧`[ ]`/`[x]`格式)、design_tokens 新增 urgency_urgent/high/medium/normal、编辑面板+批操作栏+右键菜单三处同步、活动日志记录优先级变更(合并为单条)、排序 urgency→deadline_date→created_at、`_build_filter_with_sort()` 保证排序不丢失、load_task 直接使用 raw_md |
| 16 | 2026-06 | 任务凸显方案统一：术语定义"任务凸显"替代"高亮"、Delegate 移除 Qt 蓝条(Col 0+Col 6 不再响应 State_Selected)、紧急背景始终绘制(凸显任务不跳过)、凸显红色改用 design_tokens.danger、`_on_task_selected` 统一入口(合并 selectRow+scrollTo+信号递归守卫)、分页翻页自动凸显首任务、删死代码 `_auto_select_first` |
| 17 | 2026-06 | 凸显蓝条修复(Col 3 先 super().paint() 覆底再红字)、新建任务排序切换(单/多任务保存时临时切"创建时间"倒序、"新建任务状态"机制+破坏条件+内部守卫)、丢弃批量粗体标记和 move_to_top、Markdown 空括号解析修复(`{1,3}`→`{0,3}`)、文案"截止日"→"截止时间" |
| 18 | 2026-06 | Markdown 解析/格式化鲁棒性提升：正则 `{0,3}`→`*`(括号任意长度)、`*` 数量 clamp [0,3]、状态关键字从 Markdown 输出移除、`_TAG_PATTERN` 加 `(?<!\S)`(标签须空格/行首分隔)、`_TASK_LINE_PATTERN` 状态可选+双日期支持时间、单日期智能分配(有时分→deadline、无状态→deadline)、`clean_title` 基于标签逐一去除、`rowid ASC` tiebreaker 保证多任务插入顺序、多任务创建时间戳统一 |
| 19 | 2026-06 | 筛选栏优先级过滤 + 分区密码修复 + 自动锁定按分区独立设定：TaskFilter 新增 urgencies、FilterBar 新增优先级下拉、批管理侧边栏同步、新建任务自动定位完善(reset 入 guard+activate_preset 前清标志位)、_partition_passwords 从 DB 同步修复密码完全失效、分区表格 6→7 列(自动锁定(分)+密码)、auto_lock 改为 per-partition(默认3分钟)、_activate_partition 移除当前分区密码守卫允许切换、表格样式统一(交替行色+网格线) |
| 20 | 2026-06 | 批操作栏精简 + 右键菜单重构 + 分栏比例修复：BatchToolbar 精简至全选+导出+计数(移除6按钮+信号+辅助方法)；TaskListView 右键菜单重构(移除编辑/详情/复制MD+&快捷键，新增中止/重启/延后处理) + 新增6个batch信号 + _collect_target_ids 修复(checked_task_ids+行选择回退)；Delegate 中止任务保留紧急底色；批量操作后自动清除勾选+状态栏_flash_status记录；分栏比例修复(QSizePolicy.Ignored 两侧面板忽略 sizeHint，强制50/50)；深色主题原生对话框标题栏适配(Win32 DWM API：DWMWA_USE_IMMERSIVE_DARK_MODE + Win11 DWMWA_CAPTION_COLOR 精确匹配 surface_raised) |
| 21 | 2026-06 | 打包数据库方案：新增 `scripts/create_package_db.py` 构建时生成预制 package DB（4 分区：工作/学习/个人/演示空间，15 个生活场景演示任务覆盖全部核心功能）；build.bat 集成生成+恢复流程；src/app.py frozen 守卫跳过 `_ensure_demo_partition()`；详设说明新增 3.2.1 打包数据库章节 |

已移除：Priority 系统、CarouselBanner、TaskDialog、热力图点击交互、ProgressOverviewList、TimelineDetailPanel。测试覆盖：57 用例全部通过。

---

### 任务列表（TaskListView / TaskListModel / TaskListDelegate）
- 📅 创建：Sprint 3 (2026-04) | 优化：Sprint 6 (置顶/粗体)、Sprint 8 (批量操作工具栏)、2026-06-01 (修复批量工具栏信号未连接 + 计数标签更新 + 文案优化)、2026-06-01 (扩展第 9 列"归档")、2026-06-02 (新增延后处理按钮)、2026-06-03 (优先级重构)、2026-06-04 (任务凸显方案统一+新建任务自动定位完善：reset入guard+activate_preset前清标志位+Markdown解析/格式化鲁棒性提升)、2026-06-04 (右键菜单新增"调整分区"：支持批量迁移任务至其他分区，FROM/TO 分区密码双重验证，Repository batch_move_partition 批量更新 partition_id + activity_log)
- ✅ Markdown 格式任务展示（9 列：复选框、序号、创建时间、内容、截止日、进度、状态、标签、归档）
- ✅ 优先级整行背景色渲染（紧急红/重要橙/关注绿/普通淡蓝，凸显任务红色加粗叠加不跳过）
- ✅ 状态行颜色标识（TODO/DOING/DONE/OVERDUE）
- ✅ 行选择 + 多选 + 批量操作工具栏
- ✅ 分页（20/50/100）+ 排序
- ✅ Suspended 列（视觉 dimming）
- 待办：拖拽排序、列宽记忆

### 编辑任务（TaskEditPanel）
- 📅 创建：Sprint 3 (2026-04) | 优化：Sprint 6 (草稿/折叠/时间线)、Sprint 7 (日历/时间弹窗)、Sprint 8 (分栏重构/多任务)、2026-05-30 (折叠按钮一键进入编辑模式)、2026-06-03 (活动时间线进度输入框/进展编辑框 QSS 随主题色动态刷新)
- ✅ Markdown 源码编辑 + 实时预览
- ✅ 草稿模式（蓝色高亮、自动聚焦）
- ✅ 截止日历选择器（CalendarPopup）+ 时间选择器（TimePopup）
- ✅ 活动时间线（activity_log 编辑 + 锁定）
- ✅ 分区选择器
- 待办：标签快捷插入、Markdown 语法提示

### 底部状态栏（StatusBar）
- 📅 创建：Sprint 2 (2026-04) | 优化：Sprint 7 (时钟/锁定计时)、2026-05-30 (时钟格式改为年月日 时分秒 AM/PM，去加粗统一字体)、2026-05-30 (按状态分列统计：逾期 X | 进行中 X | 待办 X | 已完成 X | 共X项)
- ✅ 分区图标 + 分区名称 + 按状态分列统计(逾期/进行中/待办/已完成) + 共计总数 + 激励语
- ✅ 实时时钟（年月日 时分秒 AM/PM，与左侧字体统一不加粗）
- ✅ 闲置自动锁定计时

### 搜索栏（FilterBar）
- 📅 创建：Sprint 3 (2026-04) | 优化：Sprint 8 (DropdownWidget 统一)、2026-06-04 (新增优先级下拉：紧急/重要/关注/普通，TaskFilter.urgencies + Repository urgency WHERE 子句；reset() 清除优先级；批管理侧边栏同步增加优先级过滤)
- ✅ 全文搜索（FTS5）
- ✅ 状态筛选（Dropdown）
- ✅ 优先级筛选（Dropdown）
- ✅ 排序切换

### 统计栏
- 📅 创建：Sprint 6/7/8 (2026-05)
- ✅ StatusBadgeStrip：按状态统计 + 点击筛选
- ✅ ProgressDynamicsBar：昨日/今日/上周/本周/上月/本月进度动态 | 优化：2026-05-30（重写为单列双重模式：未点击显示最近活跃进展，点击后轮播 Top 6；联动速览栏按钮启用状态；修复 signal 未连接）、2026-05-30（新增 4 个活动计数 DB 列 + compute_activity_counts 排序；修复追加进展时进度跳 30%、布局偏移、初始筛选不一致）、2026-06-01（新增"上周""上月"按钮，DB 迁移 3→4 扩展为 6 活动计数字段；按钮栏布局适配 6 按钮，间距/字体/内边距微调）、2026-06-03（period按钮/carouselLabel移除硬编码QSS，base.qss新增#periodBtn规则{{token}}占位符）
- ✅ 速览栏（QuickOverviewBar）：预设时间范围 + 循环轮播 | 优化：2026-05-30（修复轮播不显示）、2026-05-30（修复初始加载未应用默认预设筛选导致首次点击任务列表突变）、2026-06-01（新增"上周""上月"按钮，紧急度计算+过滤器同步扩展，按钮顺序：昨天/今天/上周/本周/上月/本月）、2026-06-03（preset按钮/carousel标签移除硬编码QSS，_render()动态计算紧急颜色+theme-aware背景，changeEvent(StyleChange)自动刷新）
- ✅ TaskEditPanel：修复 _task_model 接线错误（AppConfig → TaskListModel）| 优化：2026-05-30

### 热度日历（CalendarHeatmapWidget）
- 📅 创建：Sprint 4 (2026-05) | 优化：Sprint 7 (数据源切换)、Sprint 8 (报告面板联动)
- ✅ 12 月 × 7 行 × 5 列矩阵布局
- ✅ Accent 单色渐变（activity_log 数据源）
- ✅ 悬浮 tooltip（HeatmapTooltip）
- ✅ 年份导航 + 标签筛选
- ✅ 速览栏联动高亮
- ✅ 活动报告面板（ActivityReportPanel）+ 导出（Markdown/Excel）
- ✅ 折叠面板（HeatmapCollapsePanel）
- ✅ 活动分析页标签交互优化 | 2026-06-01（标签列表→FlowLayout 胶囊云可勾选；左侧搜索框实时筛选标签+紧凑全选按钮；右侧导航栏 ◀▶ 循环切换勾选标签队列；统计卡片标签/数字顺序调整+单位统一）
- ❌ 已移除：点击筛选、拖选范围、右键菜单（数据源不匹配）；滚动到底自动衔接（改用导航按钮手动切换）

### 任务管理控制台（BatchToolbar + 管理视图）
- 📅 创建：Sprint 8 (2026-05) | 重设计：2026-06-01 (从"批量处理"页重构为"任务管理控制台"，解决与编辑视图功能重叠) | 2026-06-02 (新增右侧标签管理面板) | 2026-06-04 (精简至全选+导出+计数，其余按钮移至右键菜单)
- ✅ 左侧管理面板（180px）：关键词搜索 + 状态/优先级下拉 + 手动归档 + 清除已归档
- ✅ 手动归档：立即归档当前分区全部已完成任务（不受 archive_days 阈值限制）
- ✅ 清除已归档：永久删除当前分区已归档任务（需二次确认）
- ✅ BatchToolbar：全选 + 导出(MD/Excel) + 已选计数
- ✅ 右键菜单：更改状态 / 更改优先级 / 延后处理 / 中止 / 重启 / 删除（支持多选）
- ✅ 9 列全宽任务表格 + 分页（20条/页）+ ConfirmBar 确认机制
- ❌ 已移除：FilterBar（改为纯关键词搜索）、导入 MD 按钮、顶部统计区、工具栏内联操作按钮

### 分区管理
- 📅 创建：Sprint 7 (2026-05) | 优化：2026-06-04 (修复密码完全失效：_load_partitions 从 DB 同步密码到 _partition_passwords dict；自动锁定改为按分区独立设定：_partition_auto_lock dict + Repository update_partition_auto_lock()；_activate_partition 移除当前分区密码守卫允许切换)、2026-06-04 (修复严重安全漏洞：切换分区后原分区未立即锁定，_activate_partition 开头新增密码恢复逻辑)、2026-06-04 (修复自动锁定完全不触发：_check_idle_lock + _lock_partition 改用 DB check_partition_password 查询密码，解锁后仍可正常触发重新锁定；设置保存后 _load_partitions 同步 auto_lock_minutes)、2026-06-04 (状态栏分区按钮/菜单 🔒/🔓 双态渲染，与 ✓ 选中标记互不冲突)
- ✅ 分区 CRUD（设置对话框）
- ✅ 分区切换（QToolButton+QMenu，✓ 标记实时更新）
- ✅ 密码保护 + 自动锁定（per-partition，默认 3 分钟）
- ✅ 闲置计时器自动锁定（auto_lock_minutes/2 触发）
- ⚠️ 默认分区设置（仅 UI 显示，运行时使用 last_partition_id）

### 设置对话框（SettingsDialog）
- 📅 创建：Sprint 5 (2026-05) | 优化：Sprint 7 (分区管理标签)、2026-06-04 (移除全局自动锁定下拉；分区表格 6→7 列新增"自动锁定(分)"+"密码"列；密码按钮主题色适配 🔒accent/🔓text_secondary；右键菜单新增"设置密码"；表格样式统一：交替行色+网格线)、2026-06-04 (深色主题原生标题栏适配：showEvent 调用 win32_theme.set_window_dark_mode，Win11 精确匹配 surface_raised #24253a)
- ✅ 通用（最小化到托盘）
- ✅ 显示（主题、热力图起始年份）
- ✅ 任务列表（每页条数、默认排序）
- ✅ 提醒（启用、间隔小时、安静时段）
- ✅ 归档：7 列分区表格（名称\|默认分区\|可见\|自动归档\|归档阈值(天)\|自动锁定(分)\|密码）+ 右键菜单（设默认/重命名/设密码/删除）

### 系统托盘（SystemTray）
- 📅 创建：Sprint 2 (2026-04) | 优化：Sprint 5 (提醒通知)、Sprint 8 (toggle 修复)、2026-06-02 (最小化联动)
- ✅ 托盘图标 + 右键菜单（显示/隐藏、新建任务、退出）
- ✅ 双击托盘显示/隐藏
- ✅ 提醒通知弹窗
- ✅ 最小化按钮联动：`changeEvent` 拦截→`minimize_to_tray` 开启时隐藏到托盘

### 后台服务
- 📅 创建：Sprint 5 (2026-05)
- ✅ TaskScheduler：APScheduler 定时检查到期任务
- ✅ TaskNotifier：托盘提醒（尊重安静时段）
- ✅ TaskArchiver：每日自动归档已完成任务
- ✅ TaskRecurrence：循环任务自动创建下一实例

### 导入/导出
- 📅 创建：Sprint 5 (2026-05)
- ✅ 导入 Markdown 文件
- ✅ 导出 Markdown 文件
- ✅ 活动报告导出（Markdown / Excel）

### 窗口管理
- 📅 创建：Sprint 2 (2026-04) | 优化：Sprint 8 (自适应尺寸)、2026-06-02 (启动优化+最小化按钮)
- ✅ Frameless 自定义标题栏（VS Code 风格）
- ✅ Win32 原生拖拽 + 边框调整大小
- ✅ 全屏切换/最小化/缩小到托盘/关闭按钮（标题栏右侧 4 按钮）
- ✅ 固定默认尺寸 1050×680（`WA_DontShowOnScreen` 阻断中间帧渲染）
- ✅ 最小化行为：`changeEvent` 拦截 `WindowStateChange`，`minimize_to_tray=True` 时→隐藏到托盘
- ✅ 延迟非关键初始化（`_refresh_overdue_on_startup` 延迟 200ms）
- ✅ 构造函数传参优化：`FramelessWindowHint` 通过 `super().__init__()` 传入，消除 `setWindowFlags` 窗口重建

### 主题系统
- 📅 创建：Sprint 2 (2026-04) | 优化：Sprint 7 (design_tokens 体系)
- ✅ Light / Dark 双主题 QSS
- ✅ design_tokens.py 设计令牌体系
- ✅ QPalette 全局设置
- ✅ 主题热切换

### 测试
- 📅 创建：Sprint 1 (2026-04) | 持续更新
- ✅ parser（20 用例）、formatter（7 用例）、repository（21 用例）、task（7 用例）— 共 57 用例全部通过
- 待办：UI 自动化测试（pytest-qt）

### UI 布局优化（2026-05-29 ~ 2026-05-30）

#### 右侧编辑区重构
- ✅ 欢迎页 50%/50% 分区比例（banner 50%、编辑卡 50%），全屏/小窗自适应
- ✅ 分隔线：banner 与"编辑任务"之间增加 1px 分隔线
- ✅ 编辑卡片去外框（bordered=False），编辑区和预览区各自独立边框
- ✅ "首页"与"编辑任务"字体样式统一（font-weight: bold; font-size: 11px;）
- ✅ 新增"预览："标签，与"Markdown："风格一致
- ✅ 编辑区/预览区等高（动态 60~84px），等宽，同边框风格
- ✅ 四个子分区间距优化（ec.setSpacing(5)）

#### 左右面板对齐
- ✅ BatchToolbar 与 ProgressDynamicsBar 结构和样式完全统一（font-size/padding/margins）
- ✅ "首页"文字与左侧表头文字水平对齐（editor_header fixedHeight 同步 QHeaderView）
- ✅ Banner 上边缘与左侧表头下边缘对齐

#### 新建任务流程优化
- ✅ 新建多任务改为内联编辑器（不再弹窗），3 行差异化模板
- ✅ 多任务保存后全部置顶、全部粗体、首行红色粗体
- ✅ 单任务保存后置顶、红色粗体、自动打开
- ✅ 切换分区：首任务自动打开（红色粗体），空分区显示欢迎页
- ✅ 红色粗体 = 当前打开任务（点击什么标红什么）
- ✅ QMessageBox 按钮中文化（保存/放弃/取消）
- ✅ 托盘新建任务免弹窗（窗口隐藏时静默丢弃草稿）
- ✅ 系统托盘 toggle 修复（增加 isMinimized 判断）

#### 编辑器体验优化
- ✅ 编辑已有任务时不显示状态关键字（DOING/TODO），从结构化字段重建显示
- ✅ 复选框统一显示 `[ ]`
- ✅ Splitter 50/50 分屏
- ✅ 操作按钮独立成行、右对齐
- ✅ 编辑器和预览区字体统一（系统默认，与列表一致）
- ✅ "任务：标题"加粗显示
- ✅ 重复任务实时检测 + 红色高亮 + 弹窗提醒
- ✅ 折叠按钮一键展开即进入编辑模式（2026-05-30）

#### 任务列表
- ✅ 首任务自动红色粗体凸显（delegate 手动红色画笔）
- ✅ 批量任务全部粗体标记
- ✅ 紧迫度背景在文字下层渲染（红色不被覆盖）
- ✅ TaskListModel 新增 move_to_top()、set_highlighted_task()、set_bold_tasks()

### 已移除的功能
- ❌ Priority 系统（合并到 TaskStatus）
- ❌ CarouselBanner（被 QuickOverviewBar 替代）
- ❌ TaskDialog（被 TaskEditPanel 替代）
- ❌ 热力图点击/拖选/右键交互（数据源不匹配）

### 设置模块重构 (2026-06-02)
- 🔧 config.py：删除 8 个死配置项（language/auto_start/font_size/heatmap_colors/max_heatmap_tags/statuses/progress_bar/deadline_calculator/hotkeys），删除对应 AppConfig 属性
- 🔧 settings_dialog.py：4 标签页→单页布局，6 功能区块（外观/任务列表/提醒/归档/激励语）
- 🔧 分区表格重构：5 列等宽（名称\|默认分区\|可见\|自动归档\|归档阈值），QCheckBox+QLineEdit(cellWidget)，右键菜单操作
- 🔧 归档：执行时间 02:07→00:00，每分区独立开关+阈值（0=即时归档，9999=不归档），删除校验（有任务禁止删）
- 🔧 repository.py：新增 count_tasks_in_partition / update_partition_archive_enabled
- 🔧 migrations.py：迁移 4→5（提醒/锁定时段列）、5→6（archive_enabled 列）
- 🔧 简化控件：提醒间隔→手动输入小时，安静时段→手输 HH:MM，锁定→下拉(1/5/10/30/60min)，归档阈值→纯文本输入

### 主题系统重构 (2026-06-03)
- 🔧 base.qss 统一 QSS 模板，所有颜色用 `{{token}}` 占位符，expand_qss() 动态替换深浅色值
- 🔧 移除"跟随系统"主题选项，仅保留浅色/深色，默认浅色
- 🔧 objectName 替代内联 token QSS：全局 15+ 个组件全面清理，主题切换后无需刷新
- 🔧 新建任务编辑区统一：create_draft_single/create_draft_multi 镜像结构，QSS 显式 token
- 🔧 标签管理面板：空状态+分页(20/页)，独立于任务列表翻页
- 🔧 任务选中红色加粗：无整行高亮(selection-bg→transparent)、无 hover 高亮、QTableView 原生网格线(垂直+水平统一)
- 🔧 紧急程度底色与红字独立叠加：delegate fillRect(底色) → drawText(红字)，互不干扰
- 🔧 批处理页面 task_selected 信号连接，分页切换刷新
- 🔧 速览栏按钮点击自动选中第一个任务，LOGO 按钮联动"今天"预设

### 已知问题（待修复）
- ⚠️ 启动时 `QFont::setPointSize: Point size <= 0 (-1)` 警告，不影响功能使用。
- ⚠️ **启动残影**：Frameless 窗口在 Win11 上启动时闪现浅色标题栏按钮（`_ □ X`）。经 8 轮排查（见下方记录），确认根因在 PySide6/Qt 底层 `FramelessWindowHint` 代码路径，DWM 层、Qt 层、框架版本层均无法根除。当前保留方案 A（构造函数传参）作为部分优化，残余残影标记为框架层已知限制。

**残影排查记录**（2026-06-05）：
| # | 方案 | 结果 |
|---|------|------|
| 1 | WA_DontShowOnScreen + 提前 resize | 未根除 |
| 2 | DWMWA_CLOAK (DWM 层隐藏) | 未解决 + 增加延迟 |
| 3 | 构造函数传参 FramelessWindowHint | 改善（消除窗口重建残影） |
| 4 | setWindowOpacity(0) 首帧透明 | 无效（opacity 不控 NC 渲染） |
| 5 | PySide6 降级 6.10.3 | 无效 + QFont 警告增多 |
| 6 | DWMWA_NCRENDERING_POLICY 禁用 | 无效 |
| 7 | DWMWA_USE_IMMERSIVE_DARK_MODE | 无效（残影非系统配色） |
| 8 | 放弃 FramelessWindowHint + Win32 手动去标题栏 | 白色边框副作用 + 无效 |
